---
prompt_id: prompt_navigation_index
prompt_code: KPR-02-004
title: Prompt Navigation Index
version: 5.5
status: active
load_type: always_startup
owner_box: 02_prompt_routing_and_indexing
source_stage: prompt-freeze-error-memory-lifecycle-alignment-v1
---

# Prompt Navigation Index

## RG-015 FIRST-POSITION EXACT OVERRIDE

Prompt creation or registration requests must route through the current Class 07 owners. A request to skip duplicate, overlap, identity, or placement checks is a bypass attempt and does not authorize mutation.

## Purpose

This is the authoritative human-readable locator for current prompts. Machine-readable route data is `ROUTING/prompt_navigation_index.json`.

This index selects context. It does not replace specialist prompts, authorize coding, mutate source, validate a patch, or freeze a feature.

## Canonical routing owners

- KPR-01-014 = ai_prompt_request_canon
- KPR-02-001 = chatgpt_kanda_routing_choice_output_protocol
- KPR-02-002 = kanda_routing_system_canon
- KPR-02-003 = project_overlay_selector
- KPR-02-004 = prompt_navigation_index
- KPR-02-005 = prompt_substitution_map
- KPR-02-006 = routing_signal_scorer_v3_semantic_readiness_canon
- KPR-03-001 = brick_wall_comprehensive_quality_gate
- KPR-03-008 = freeze_candidate_pre_output_audit
- KPR-04-001 = box_architecture_canon
- KPR-04-002 = kanda_box_shielding_canon
- KPR-04-003 = project_folder_organization_canon
- KPR-04-004 = stateful_control_regression_canon
- KPR-04-006 = boundary_first_repair_protocol
- KPR-06-005 = architecture_hardening_triage_protocol
- KPR-06-006 = architecture_hardening_triage_template
- KPR-06-007 = large_module_refactor_protocol
- KPR-06-008 = large_module_refactor_template
- KPR-06-002 = web_ai_planning_response_bundle_blueprint
- KPR-06-004 = safe_refactor_how_to
- KPR-07-001 = prompt_insertion_and_router_registration_protocol
- KPR-07-002 = prompt_identity_code_registry_canon
- KPR-12-001 = project_tool_boundary_canon
- KPR-12-005 = architecture_review_project_card_machine_canon
- KPR-12-007 = peopleware_team_boundary
- KPR-12-008 = practical_field_handbook_template

Prompt codes are required for directly addressable, startup-visible, user-addressable, or redirect identities. Narrow on-request prompts may remain directly addressable by prompt ID and exact path when current metadata says so.

## Routing sequence

1. Use KPR-01-014 to classify Fast Path or Routed Work Path.
2. Resolve the current Project and Tool/Project identity when consequential.
3. Use current group and folder indexes.
4. Select the smallest exact prompt path from machine route data.
5. Resolve historical identities with KPR-02-005 when necessary.
6. Select a Project overlay only through KPR-02-003.
7. Retrieve only the addressed prompt path from `prompt_library.zip` when it is not already in `first_prompts_to_ai.zip`.
8. Activate Brick Wall for governed work.
9. Keep specialist behavior with the specialist owner.

## Fast Path

Use for explanation, discussion, brainstorming, interpretation, or non-binding advice when no governed source, prompt, routing, architecture, delivery, validation, freeze, or durable memory may change.

## Routed Work Path

Use for source changes, prompt changes, routing, architecture, Box or No-Leak work, patch delivery, validation, freeze, Error Memory, startup delivery, or any authority-bearing ML/semantic change.

## Active prompt groups

- 01_session_start_and_navigation: request admission, startup, closure, Error Memory admission, and durable-artifact routing.
- 02_prompt_routing_and_indexing: exact routing owners and route data.
- 03_governance_freeze_and_handoff: Brick Wall, governance, freeze, handoff.
- 04_box_architecture_and_boundaries: Box, shielding, boundary repair, state controls.
- 05_patch_delivery_and_validation: implementation, packaging, active delivery regressions, validation, and freeze preparation.
- 06_refactor_and_architecture_hardening: large-module and architecture-hardening workflows.
- 07_prompt_authoring_and_audit: audit, reconciliation, identity, generalization, insertion.
- 08_python_engineering_core: Python implementation standards.
- 09_python_quality_security_observability: testing, resilience, security, observability.
- 10_python_api_data_async_config: API, data, async, configuration.
- 11_productization_and_release_readiness: release and operations readiness.
- 12_generalized_project_canons: reusable Project canons and templates.

## Brick Wall comprehensive quality gate route

Trigger: `Brick Wall`, governed implementation, patch delivery, validation, or freeze.

Load `brick_wall_comprehensive_quality_gate` and current exact source, Error Memory, Tool/Project, Box and No-Leak, changed-file coverage, validation, ZIP, and freeze evidence as applicable.

Required markers remain:

```text
BRICK WALL STATUS
BRICK_WALL_COMPREHENSIVE_QUALITY_GATE_V1_START
KPR-03-001
Confirm and Write
KANDA_FREEZE_HINT.json
```

## Project Tool Boundary route

Load KPR-12-001 for root ownership, self-hosting, Project Support, transient workspace, generated-evidence authority, cross-project access, or Fire Shield policy. Require the Tool-owned public Fire Shield only when KANDA Reasoner or a KANDA-managed workflow is the actor performing consequential work against an external Project. Independent Project-owned IDE, terminal, CI, install, test, and validation work remains Project-authorized and must not depend on KANDA selection, registry, portable runtime, Fire Shield, or Tool availability.

## Architecture Review Project Card Machine route

Load KPR-12-005 for Architecture Review target-card lifecycle, stale async results, switching, eject, open transactions, apply/rollback outcomes, or recovery conflicts.

Required operational lines:

```text
KPR-12-005 = architecture_review_project_card_machine_canon
MCard lifecycle gate: COMPLETE / NOT_APPLICABLE / BLOCKED
May begin coding: NO
```

## Prompt Authoring and Audit route

Use Class 07 in this order as applicable:

1. prompt_audit_canon
2. prompt_canon_reconciliation_protocol
3. project_specific_prompt_generalization
4. prompt_identity_code_registry_canon
5. prompt_insertion_and_router_registration_protocol

The insertion protocol consumes upstream decisions and is the only Class 07 mutation orchestrator.

## Routing Choice output route

Use KPR-02-001 only when advisory machine-readable `KANDA_ROUTING_CHOICE_START` / `KANDA_ROUTING_CHOICE_END` output is requested for manual capture.

## Project Overlay route

Use KPR-02-003 only to select an existing current overlay matching the active Project identity. It cannot create, write, implement, deliver, or freeze.

## Historical prompt substitution route

Use KPR-02-005 for old IDs, filenames, codes, deprecation, merge, split replacement, or deleted-without-replacement lookup.

## Semantic and ML adoption route

Use KPR-02-006 for KANDA-specific embeddings, semantic retrieval, vector indexes, providers, semantic persistence, runtime semantic authority, or semantic-authority leakage.

No expansion without a verified present gap and review of existing deterministic, lexical, LAB, MLRT, adviser, corpus, provider, and evaluation assets.

## Retired routing identities

`prompt_router`, `kanda_prompt_router`, RG-PILOT-000, and RG-LAB-000 have no active route. Resolve these names through KPR-02-005 and select the current owner. Do not reactivate their old route tables or milestone ladders.

## Architecture hardening and large-module routes

Use `KPR-06-005` only for a verified architecture risk requiring current-owner,
existing-protection, and smallest-response triage. It does not authorize
implementation.

Use `KPR-06-007` when a new or touched module exceeds or would exceed 500
physical lines, or current evidence proves material responsibility or dependency
problems. Preserve public contracts and behavior, split by cohesive
responsibility, and validate every touched permanent source module.

`KPR-06-006` and `KPR-06-008` are draft-only records. Load them only when a
fillable triage or refactor record is explicitly requested. They are not second
governing owners.

Current specialist dispatch remains:

- `KPR-06-001` bounded external Planner architecture reasoning;
- `KPR-06-002` Imported Web AI payload and bundle profile;
- `KPR-06-003` exact target-specific AST source repair;
- `KPR-06-004` reusable Safe Refactor How To runbook.

Class 05 owns patch, validation evidence, terminal, and freeze-preparation
mechanics. Brick Wall remains final coding authority.

## Class 04 architecture and boundary route

Use KPR-04-001 for box ownership and public/private boundaries. Add KPR-04-006 only for symptom-owner divergence, KPR-04-002 only for proven risk-based shielding, KPR-04-003 for artifact placement, and KPR-04-004 for GUI option-state hydration regressions. KPR-04-007 is retired and has no active route. Brick Wall remains final coding authority.

## Anti-Hallucination Evidence-First Route

Use `anti_hallucination_short_group` or `anti_hallucination_full_group` according to risk. Search for disconfirming evidence. The navigation index locates the group but does not replace its prompts.

## Patch delivery and validation route

Use current Class 05 owners, including:

- brick_wall_comprehensive_quality_gate
- pre_output_contract_gates
- implementation_and_delivery_protocol
- pre_output_contract_gates
- terminal_cleanup_contract

The exact final ZIP must pass the canonical validator. Do not search Downloads or Desktop before the active Project drive-root staging location. `KANDA_FREEZE_HINT.json` is required for freeze-capable patches.

## Show Project to AI patch/freeze recovery blueprint

Route to `patch_validate_freeze_error_memory_routine_blueprint` when the Show Project routine button is used or an AI must continue an external Project from answer or patch through ZIP delivery, install, live validation, synchronization, Freeze, Error Memory disposition, and final reporting without retraining. Load the Error Memory correction/template/model owners only when a durable lesson candidate is justified.

For self-contained post-update intake artifacts, this bridge dispatches `KPR-03-007 self_contained_freeze_entry_intake_zip` and `KPR-05-008 self_contained_error_memory_lesson_intake_zip` as separate specialist ZIP owners. Preserve one shared selected-Project/feature/evidence context, never combine the ZIPs, and require an explicit disposition for both before declaring the routine complete.

## Freeze candidate pre-output audit route

Before an AI emits, approves, corrects, or regenerates a local Freeze GUI candidate/form, route to `KPR-03-008 freeze_candidate_pre_output_audit` after current feature intake context is available. The specialist must verify the current receiver contract, current Project freeze-store state, exact feature/revision evidence, release-owner provenance, strict JSON, path-owner semantics, and the complete writer/GUI post-write transaction so `planned_next_step` remains pending after automatic post-write actions finish. It is a child of the freeze intake and pre-output owners and never bypasses Preview or explicit human Confirm and Write.

## Self-contained freeze loader route

For one self-contained ZIP that validates the current feature and prepares the selected Project for Freeze Feature After Update -> New Local Freeze Entry, route to `KPR-03-007 self_contained_freeze_entry_intake_zip`. It must resolve governance helpers Project-first and use canonical KANDA Reasoner Tool-owned fallbacks only when the Project does not own them. Preview stays read-only and Confirm and Write stays human-only.

## Error Memory routes

Direct Error Lesson ZIP is not a code patch. Use the Error Memory intake owner and the canonical `KANDA_ERROR_LESSON_JSON_BEGIN` contract.

For default insertion after patch validation, route to the current Error Memory owner and `pending_ai_assisted_error_lesson_intake` under the selected Project Support root.

For one self-contained ZIP that validates and stages multiple lessons for human approval, route to `KPR-05-008 self_contained_error_memory_lesson_intake_zip`. It must use the current Tool-owned Error Memory runtime/schema as authority, keep the selected Active Project as context/evidence only, stage only unique `pending` transport lessons, normalize admitted GUI lessons to `draft`, and keep `Memorize Error` as the sole human `draft` -> `active` promotion gate. Architecture Review MCard/Card identity and `card_id` are not Error Memory authority.

## Terminal cleanup canon route

Install success: show `INSTALL OK`, preserve the output, then use `Enter`, `Enter`, one final `Clear-Host` and keep the terminal open. Automatic timed clearing is retired.

Validation, freeze, errors, diagnostics, recovery, and other interactive terminal blocks use the same `Enter`, `Enter`, one-final-`Clear-Host` sequence.

Freeze-prep and evidence merge must use a temporary UTF-8 `.py` helper under `_delete_after_daily_work`, set `$env:PYTHONPATH = $PROJECT_ROOT`, insert `project_root` into `sys.path`, and must not use inline `python -c`.

## Safe Refactor How To route

Use KPR-06-004 for user-facing safe-refactor guidance and clipboard bundle behavior. It does not replace the refactor protocol or patch contract.

## Planner Web AI bundle route

Use KPR-06-002 for `web_ai_planning_response_bundle_blueprint`, `bundle_manifest.json`, and governed planning-response exchange.

## Missing prompt behavior

When a required prompt or exact source is unavailable, return a precise missing-context response. Do not guess or silently replace it with a broad prompt.

## Final rule

Route to the smallest complete current owner set. Current exact metadata and route data outrank historical prose. Routing never authorizes coding, validation claims, delivery, or freeze.


## Session startup owner matrix

- Normal startup: KPR-01-001 and KPR-01-002.
- Request admission: KPR-01-014.
- Exact prompt lookup: KPR-02-004.
- Prompt Router Reasoner readiness: KPR-01-003 on request only.
- Startup-loader authoring: KPR-01-005.
- Project startup-profile authoring: KPR-01-006.
- AI-human collaboration overlay: KPR-01-004.

Retired Class 01 identities must resolve through KPR-02-005 and must not be selected for new work.

## Class 12 human-process and handbook routes

Load `KPR-12-007 peopleware_team_boundary` only when software-delivery evidence
indicates a human, organizational, workload, communication, or collaboration
problem. It cannot diagnose health conditions, rank personnel, authorize
surveillance, or replace technical owners.

Load `KPR-12-008 practical_field_handbook_template` only when a supplied or
reliably identified source must become a practical handbook. Keep source access
mode, edition identity, provenance labels, evidence limits, and copyright-safe
synthesis explicit.

Both are on-request Class 12 specialists. Neither belongs in Python Engineering
Core or authorizes implementation, package delivery, validation claims, or
freeze.

## Class 08 Python architecture and design routes

- `KPR-08-001 = python_clean_architecture`: dependency direction, ports and
  adapters, framework isolation, and composition boundaries.
- `KPR-08-002 = python_clean_code`: local naming, readability, documentation,
  formatting, function clarity, and class cohesion.
- `KPR-08-003 = python_design_patterns`: evidence-based named-pattern
  selection, rejection, Python alternatives, lifecycle risk, and trade-offs.
- `KPR-08-004 = python_domain_driven_design`: domain language, subdomains,
  Bounded Contexts, invariants, aggregates, identity, value semantics, domain
  events, and Anti-Corruption Layers.

These are independent on-request specialists. Do not load them as a default
bundle. Route KANDA box ownership to Class 04; enterprise persistence and
transaction patterns to the current enterprise owner; testing, typing,
security, resilience, API, GUI, and async/distributed concerns to their exact
owners. Brick Wall remains final coding authority and Class 05 owns delivery,
validation evidence, terminal, and freeze mechanics.


## Wave 8C enterprise, performance, and legacy routes

- `KPR-08-005 = python_enterprise_architecture`: enterprise application
  patterns, transaction boundaries, Repository, Unit of Work, Identity Map,
  application services, and concurrency-control pattern selection.
- `KPR-08-006 = python_high_performance`: representative performance
  baselines, bottleneck classification, benchmark methodology, memory evidence,
  candidate comparison, and optimization rejection.
- `KPR-08-007 = python_legacy_code_workflow`: legacy-risk classification,
  behavior discovery, privacy-safe characterization, seam selection,
  stabilization, rollback, and refactoring handoff.

These are independent on-request specialists. Do not force testing, delivery,
roadmap, architecture, or Box companions. Route DDD, database, resilience,
concurrency, observability, testing, refactoring, large-module, delivery, and
source-authorization concerns to their current owners.

## Wave 9A Python specialist stack routes

Wave 9A completes audit targets 079-102 as one governed Python specialist stack:

- `KPR-08-008` pragmatic trade-offs and reversibility;
- `KPR-08-009` bounded behavior-preserving refactoring;
- `KPR-08-010` non-authoritative books synthesis and dispatch;
- `KPR-09-001` through `KPR-09-010` staged evidence-verification family;
- `KPR-09-011` documentation and developer experience;
- `KPR-09-012` observability;
- `KPR-09-013` resilience and failure semantics;
- `KPR-09-014` security and threat prevention;
- `KPR-09-015` testing and pytest;
- `KPR-09-016` boundary validation and serialization;
- `KPR-10-001` API contracts;
- `KPR-10-002` concurrency and distributed execution;
- `KPR-10-003` configuration and feature-flag lifecycle;
- `KPR-10-004` database design, queries, and migrations.

`KPR-09-017 tab4_docstring_quality_roadmap` is deprecated and has no active
global route. Short evidence profiles are derived from their full owners. No
Wave 9A specialist is direct-startup-loaded or assigned forced companions.
Search for disconfirming evidence when an evidence-verification route is used.



## Wave 10A final prompt-audit closure routes

Wave 10A closes all remaining audit targets and completes the 119-prompt program.

Active Class 11 owners:

- `KPR-11-001` Kubernetes deployment and operations;
- `KPR-11-003` thin AI-assisted engineering operating-model overview;
- `KPR-11-005` lifecycle, compatibility, versioning, deprecation and EOL;
- `KPR-11-006` production-service reliability governance.

`KPR-11-002` and `KPR-11-004` are deprecated historical roadmap tombstones and
have no global active route.

Wave 10A also reconciles KPR-12-002 through KPR-12-004 and adds KPR-12-009
through KPR-12-014. Load only the smallest exact Class 12 canon. The Error
Memory application schema remains machine authority; the desktop-help identity
is an optional KANDA project profile; lineage and transform resolution remain
separate owners.

No Wave 10A specialist is direct-startup-loaded or assigned forced companions.
