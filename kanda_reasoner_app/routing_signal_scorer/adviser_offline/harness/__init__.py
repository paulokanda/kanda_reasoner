"""Offline Adviser pure comparison harness.

The harness compares already-supplied teacher answers and candidate outputs. It
never calls a candidate scorer, loads prompts, writes reports, or changes runtime
routing behavior.
"""
