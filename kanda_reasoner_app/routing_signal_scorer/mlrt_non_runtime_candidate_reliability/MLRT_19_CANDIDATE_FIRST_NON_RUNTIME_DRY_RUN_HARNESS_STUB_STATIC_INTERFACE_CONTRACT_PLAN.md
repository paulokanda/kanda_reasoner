# MLRT-19 Candidate First Non-Runtime Dry-Run Harness Stub Static Interface Contract Plan v1

Feature ID: `routing_signal_scorer_v3_mlrt_candidate_first_non_runtime_dry_run_harness_stub_static_interface_contract_plan_v1`

Primary box: `kanda_reasoner_app/routing_signal_scorer/mlrt_non_runtime_candidate_reliability`

Status: governed documentation-only first non-runtime dry-run harness stub static-interface contract planning milestone.

## Purpose

MLRT-19 defines the future static interface contract plan for a non-runtime harness stub after MLRT-18 Candidate First Non-Runtime Dry-Run Harness Stub Source Surface Plan has been locally validated, frozen, and exposed with `FREEZE_MEMORY_STATUS: OK`.

This is roadmap progress toward a future first non-runtime dry-run harness stub static interface contract, but it is still not source code, not a static interface, not a source surface, and not a harness implementation. MLRT-19 does not create Python source files, does not create a static interface, does not create a source surface, does not create static interface files, does not create source-surface files, does not create harness code, does not create runner code, does not create harness stub files, does not create implementation modules, does not create executable validators, does not create schema code, does not create contract schema files, does not implement a boundary checker, does not execute a dry run, does not execute a candidate, does not create candidate outputs, does not execute cases, does not score cases, does not compare routes, does not generate reports, does not validate candidate reliability, and does not unlock ML implementation.

## Roadmap position

MLRT-19 is not a loop. It narrows the next blocker after MLRT-18:

```text
MLRT-13 - first non-runtime dry-run execution planning
MLRT-14 - future harness skeleton planning
MLRT-15 - future harness contract planning
MLRT-16 - future harness implementation boundary planning
MLRT-17 - future harness stub implementation planning
MLRT-18 - future harness stub source-surface planning
MLRT-19 - future harness stub static-interface contract planning
```

The purpose of this step is to define what a future static interface contract may be allowed to describe before any abort-contract milestone is considered.

## Hard boundary

MLRT-19 is documentation-only. It does not create a source file. It does not create Python source files. It does not create a static interface. It does not create a source surface. It does not create static interface files. It does not create source-surface files. It does not create a harness. It does not create a runner. It does not create harness stub files. It does not create implementation modules. It does not create executable validators. It does not create schema code. It does not create a harness contract file. It does not create contract schema files. It does not implement a boundary checker. It does not execute a dry run. It does not execute a candidate. It does not create dry-run execution records. It does not create candidate outputs. It does not run cases. It does not score cases. It does not compare routes. It does not generate reports. It does not validate candidate reliability. It does not unlock ML implementation.

MLRT-19 must not:

- create Python source files
- create source-surface files
- create source-surface directories
- create static interface files
- create static interface directories
- create harness code
- create runner code
- create harness stub files
- create implementation modules
- create executable validators
- create schema code
- create contract schema files
- create static interface implementation files
- create boundary checker implementation files
- create dry-run execution records
- run a dry run
- execute a candidate
- create candidate outputs
- run cases
- score cases
- compare routes
- generate reports
- persist reports
- validate candidate reliability
- unlock ML implementation
- create or accept a candidate package
- install a candidate package
- import a candidate package
- perform static review
- create static review records
- create static review evidence records
- create evidence envelopes
- create static review outcome records
- create outcome gate records
- perform dry-run readiness review
- create dry-run readiness records
- create dry-run protocol records
- create dry-run protocol execution records
- create dry-run input records
- create dry-run input manifests
- create dry-run output records
- create dry-run output manifests
- create output capture envelopes
- create output capture rejection records
- create output rejection gate records
- run an output rejection gate
- create contract-conformance records
- create contract-conformance evidence
- run a contract-conformance check
- create contract-conformance rejection records
- run a contract-conformance rejection gate
- grant route authority
- load prompts
- read live prompt-library files
- read live freeze memory
- read live router canon
- import runtime router modules
- call providers
- call embedding models
- use vector stores
- use network calls
- use subprocess calls
- start batch mode
- persist ML decisions or create persistent ML decisions
- mutate corpus, fixtures, router canon, prompt library, freeze memory, gold registry, startup pack, approval state, activation state, field-test state, runtime logs, or persistent ML decision storage
- create activation keys
- enable field-test mode
- create runtime Pilot behavior
- create Copilot behavior

The critical boundary error budget remains `0`.

## Planned static-interface contract identity

A future static-interface contract, if later authorized by a separate governed milestone, must define only a standard-library-only, import-isolated, deterministic, in-memory-only, non-runtime adapter boundary. It must not be a router interface, not a route executor, not a prompt loader, not a provider adapter, not an embedding adapter, not a vector-store adapter, not a persistence writer, not an approval writer, not an activation mechanism, not a Pilot mechanism, and not a Copilot mechanism.

A future static-interface contract plan may describe only:

- future static input descriptor names
- future static output envelope names
- future abort contract names
- future immutable planning flags
- future human-review-required flag
- future no-live-read flag
- future no-authority flag
- future no-execution flag
- future no-reliability-claim flag
- future no-persistence flag

MLRT-19 does not create these future descriptors, envelopes, flags, or interfaces as code. It only defines that any future abort contract must be planned before any code is written.

## Planned static input contract

A future static input contract may accept only caller-supplied, in-memory, already-sanitized static metadata. It must not read files, scan directories, import candidate packages, import runtime router modules, inspect prompt libraries, inspect freeze memory, inspect router canon, inspect the gold registry, use providers, use embeddings, use vector stores, use network calls, use subprocess calls, write reports, or persist state.

The future static input contract may plan only names for:

- static candidate identity reference
- static candidate hash reference
- static MLRT freeze-reference bundle
- static source-surface reference
- static interface contract reference
- static boundary declaration reference
- static no-live-read declaration
- static no-authority declaration
- static human-review declaration

These are future planned names only. MLRT-19 does not implement them.

## Planned static output contract

A future static output contract may return only non-authoritative planning envelopes. The planned output contract must state that it is advisory, static, deterministic, in-memory only, and human-review-required. It must state that no dry run has executed, no candidate has executed, no candidate output exists, no case has run, no score exists, no route comparison exists, no report exists, no reliability claim is made, and ML implementation remains locked.

The planned output contract must not contain route decisions, prompt loading instructions, execution commands, install commands, freeze writes, approval writes, readiness approvals, reliability approvals, activation signals, field-test signals, runtime Pilot instructions, or Copilot instructions.

## Planned abort-contract dependency

A future static interface contract must not proceed to implementation until a separate abort-contract milestone defines its abort vocabulary. That later abort contract must remain non-authoritative and must abort before any planning envelope is returned if it detects forbidden fields, missing freeze references, hash mismatches, unexpected live reads, unexpected runtime imports, provider calls, embedding calls, network calls, subprocess calls, batch mode, persistence, activation, field-test state, Pilot behavior, Copilot behavior, route-authority fields, prompt-loading fields, approval-write fields, reliability-claim fields, execution-command fields, report-persistence fields, or any unknown implementation surface.

MLRT-19 does not define executable abort behavior. It only requires the later abort-contract planning milestone.

## Planned import policy

A future static interface contract must be standard-library-only and import-isolated. It may not import project runtime router modules, prompt-library modules, freeze-memory readers, provider libraries, embedding libraries, vector-store clients, network clients, subprocess helpers, persistence writers, GUI modules, approval writers, activation modules, Pilot modules, or Copilot modules.

The future static interface contract must operate only on caller-supplied in-memory data. It must not read live project files. It must not inspect live prompt-library files. It must not inspect live freeze memory. It must not inspect live router canon. It must not inspect the gold registry. It must not inspect candidate package files. It must not reach outside the provided static bundle.

## No implementation doctrine

MLRT-19 must not be interpreted as authorization to implement a static interface. It only defines the future harness stub static-interface contract plan. A later governed milestone must explicitly define the abort contract before any source-surface code is written. A later code milestone, if ever approved, must remain non-runtime and non-authoritative.

## No reliability claim doctrine

MLRT-19 makes no reliability claim. It does not evaluate a candidate. It does not execute a candidate. It does not execute dry-run cases. It does not score any output. It does not compare routes. It does not create evidence. It does not create reports. It does not approve readiness. It does not approve reliability. It does not unlock ML implementation.

## Positive label doctrine

The only positive MLRT-19 label is:

```text
MLRT_FIRST_NON_RUNTIME_DRY_RUN_HARNESS_STUB_STATIC_INTERFACE_CONTRACT_PLAN_READY_FOR_ABORT_CONTRACT_PLANNING_ONLY
```

This label means only that MLRT-19 static-interface contract planning documentation exists and may be followed by MLRT-20 abort-contract planning after freeze with `FREEZE_MEMORY_STATUS: OK`.

It does not mean source files exist. It does not mean a source surface exists. It does not mean a static interface exists. It does not mean static interface files exist. It does not mean a harness exists. It does not mean runner code exists. It does not mean harness stub files exist. It does not mean implementation modules exist. It does not mean a boundary checker exists. It does not mean a contract schema exists. It does not mean a dry run has executed. It does not mean candidate outputs exist. It does not mean cases were executed. It does not mean scores exist. It does not mean reports exist. It does not mean candidate reliability is validated. It does not mean ML implementation is unlocked.

## Forbidden authority fields

The future static interface contract plan must reject or reserve these fields as forbidden authority signals:

```text
route_decision
load_prompt
execute_route
execute_dry_run
execute_candidate
run_case
score_case
compare_route
create_report
persist_report
approve_readiness
approve_reliability
approve_dry_run_execution
record_human_approval
write_freeze_memory
write_gold_registry
write_prompt_library
write_router_canon
write_startup_pack
activate_pilot
activate_copilot
enable_field_test
call_provider
call_embedding_model
network_call
subprocess_call
start_batch_mode
persist_ml_decision
runtime_command
pilot_instruction
copilot_instruction
```

These fields must not appear as permissions, return commands, state transitions, or authority-bearing outputs.

## Next safe milestone

After MLRT-19 is installed, validated, frozen, and exposed with `FREEZE_MEMORY_STATUS: OK`, the next safe milestone is:

```text
MLRT-20 Candidate First Non-Runtime Dry-Run Harness Stub Static Interface Abort Contract Plan
```

MLRT-20 must remain documentation-only unless a later governed scope explicitly approves a narrower non-runtime change. It must not create source files, a source surface, a static interface, a harness, a runner, harness stub files, executable validators, schema code, dry-run execution records, candidate outputs, case execution, scoring, route comparison, reports, reliability validation, ML implementation unlock, route authority, prompt loading, provider calls, embeddings, persistence, activation, field-test mode, runtime Pilot behavior, or Copilot behavior.
