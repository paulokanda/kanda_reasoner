# MLRT-20 Candidate First Non-Runtime Dry-Run Harness Stub Static Interface Abort Contract Plan v1

Feature ID: `routing_signal_scorer_v3_mlrt_candidate_first_non_runtime_dry_run_harness_stub_static_interface_abort_contract_plan_v1`

Primary box: `kanda_reasoner_app/routing_signal_scorer/mlrt_non_runtime_candidate_reliability`

Status: governed documentation-only first non-runtime dry-run harness stub static-interface abort-contract planning milestone.

## Purpose

MLRT-20 defines the future abort-contract planning doctrine for a non-runtime harness stub static interface after MLRT-19 Candidate First Non-Runtime Dry-Run Harness Stub Static Interface Contract Plan has been locally validated, frozen, and exposed with `FREEZE_MEMORY_STATUS: OK`.

This is still not testing. It is also not source code, not an executable abort gate, not a static interface, not a source surface, not a harness implementation, and not a dry-run runner. MLRT-20 exists to ensure that future tests cannot begin until a later governed milestone has defined how unsafe inputs, unsafe fields, missing freeze references, forbidden authority signals, and unexpected runtime surfaces must abort before any future planning envelope or dry-run action is produced.

MLRT-20 does not create Python source files, does not create abort-contract code, does not create rejection-contract code, does not create a static interface, does not create source-surface files, does not create static-interface files, does not create harness code, does not create runner code, does not create harness stub files, does not create implementation modules, does not create boundary-checker code, does not create executable validators, does not create schema code, does not create contract schema files, does not execute a dry run, does not execute a candidate, does not create candidate outputs, does not execute cases, does not score cases, does not compare routes, does not generate reports, does not validate candidate reliability, and does not unlock ML implementation.

## When tests start

Real candidate/dry-run tests must not start at MLRT-20.

Tests may start only after separate governed milestones create and freeze all required non-runtime test surfaces, including at minimum:

- a non-runtime static harness source surface, if later allowed
- a static interface, if later allowed
- an executable abort gate, if later allowed
- an executable rejection gate, if later allowed
- immutable input fixtures
- immutable expected-output contracts
- output capture envelopes
- contract-conformance checks
- dry-run execution records
- a no-authority, no-prompt-loading, no-provider, no-persistence execution boundary
- a human-reviewed test start gate

Until those later milestones exist and are frozen with `FREEZE_MEMORY_STATUS: OK`, every MLRT step remains planning only and candidate reliability remains unvalidated.

## Roadmap position

MLRT-20 is not a loop. It narrows the next blocker after MLRT-19:

```text
MLRT-13 - first non-runtime dry-run execution planning
MLRT-14 - future harness skeleton planning
MLRT-15 - future harness contract planning
MLRT-16 - future harness implementation boundary planning
MLRT-17 - future harness stub implementation planning
MLRT-18 - future harness stub source-surface planning
MLRT-19 - future harness stub static-interface contract planning
MLRT-20 - future harness stub static-interface abort-contract planning
```

The purpose of this step is to define the future abort contract before any rejection-contract planning, executable interface, harness stub, or non-runtime test execution can be considered.

## Hard boundary

MLRT-20 is documentation-only. It does not create a source file. It does not create Python source files. It does not create abort-contract code. It does not create rejection-contract code. It does not create an executable abort gate. It does not create an executable rejection gate. It does not create a static interface. It does not create static-interface files. It does not create a source surface. It does not create source-surface files. It does not create a harness. It does not create a runner. It does not create harness stub files. It does not create implementation modules. It does not create executable validators. It does not create schema code. It does not create contract schema files. It does not implement a boundary checker. It does not execute a dry run. It does not execute a candidate. It does not create dry-run execution records. It does not create candidate outputs. It does not run cases. It does not score cases. It does not compare routes. It does not generate reports. It does not validate candidate reliability. It does not unlock ML implementation.

MLRT-20 must not:

- create Python source files
- create abort-contract code
- create rejection-contract code
- create executable abort gates
- create executable rejection gates
- create source-surface files
- create source-surface directories
- create static interface files
- create static interface directories
- create harness code
- create runner code
- create harness stub files
- create implementation modules
- create boundary-checker code
- create executable validators
- create schema code
- create contract schema files
- create static interface implementation files
- create dry-run execution records
- run a dry run
- execute a candidate
- create candidate outputs
- run cases
- score cases
- compare routes
- generate reports
- persist reports
- validate candidate reliability
- unlock ML implementation
- create or accept a candidate package
- install a candidate package
- import a candidate package
- perform static review
- create static review records
- create static review evidence records
- create evidence envelopes
- create static review outcome records
- create outcome gate records
- perform dry-run readiness review
- create dry-run readiness records
- create dry-run protocol records
- create dry-run protocol execution records
- create dry-run input records
- create dry-run input manifests
- create dry-run output records
- create dry-run output manifests
- create output capture envelopes
- create output capture rejection records
- create output rejection gate records
- run an output rejection gate
- create contract-conformance records
- create contract-conformance evidence
- run a contract-conformance check
- create contract-conformance rejection records
- run a contract-conformance rejection gate
- grant route authority
- load prompts
- read live prompt-library files
- read live freeze memory
- read live router canon
- import runtime router modules
- call providers
- call embedding models
- use vector stores
- use network calls
- use subprocess calls
- start batch mode
- persist ML decisions or create persistent ML decisions
- mutate corpus, fixtures, router canon, prompt library, freeze memory, gold registry, startup pack, approval state, activation state, field-test state, runtime logs, or persistent ML decision storage
- create activation keys
- enable field-test mode
- create runtime Pilot behavior
- create Copilot behavior

The critical boundary error budget remains `0`.

## Planned abort-contract identity

A future abort contract, if later authorized by a separate governed milestone, must be standard-library-only, import-isolated, deterministic, in-memory-only, non-runtime, and non-authoritative. It must abort before any planning envelope, static output, candidate output, route comparison, report, or execution action is produced when forbidden inputs or forbidden fields are present.

The future abort contract must be a safety gate, not a test engine. It must not execute candidates, load prompts, compare routes, write approvals, write freeze memory, persist reports, call providers, use embeddings, use vector stores, activate field testing, activate Pilot, or activate Copilot.

## Planned abort triggers

A future abort contract must reject or abort on:

- missing MLRT freeze reference
- stale MLRT freeze reference
- mismatched feature title
- mismatched feature ID
- mismatched candidate hash reference
- missing human-review-required flag
- missing no-authority declaration
- missing no-live-read declaration
- missing no-execution declaration
- missing no-persistence declaration
- missing no-reliability-claim declaration
- route-authority fields
- prompt-loading fields
- provider-call fields
- embedding-call fields
- vector-store fields
- network-call fields
- subprocess-call fields
- persistence fields
- batch-mode fields
- approval-write fields
- freeze-write fields
- router-canon mutation fields
- prompt-library mutation fields
- gold-registry mutation fields
- activation fields
- field-test fields
- Pilot fields
- Copilot fields
- dry-run execution command fields
- candidate execution command fields
- case execution command fields
- scoring command fields
- report generation command fields
- report persistence command fields
- reliability-claim fields
- unknown authority-bearing fields

MLRT-20 does not implement these abort triggers as code. It documents that any later implementation must abort before action.

## Planned abort output doctrine

A future abort contract may only return a non-authoritative abort envelope. That envelope must state that processing stopped before any dry run, candidate execution, case execution, scoring, route comparison, report generation, report persistence, approval, activation, or reliability claim.

The planned abort output must not contain route decisions, prompt loading instructions, execution commands, install commands, freeze writes, approval writes, readiness approvals, reliability approvals, activation signals, field-test signals, runtime Pilot instructions, or Copilot instructions.

## Planned rejection-contract dependency

A future rejection contract is a separate later planning milestone. MLRT-20 only defines abort-contract planning. A rejection contract may later classify failed static envelopes, but it must not execute code, run dry runs, evaluate candidates, score outputs, approve readiness, or claim reliability.

A future rejection contract must remain downstream of the abort contract and must not bypass abort-on-forbidden-field behavior.

## Planned import policy

A future abort contract must be standard-library-only and import-isolated. It may not import project runtime router modules, prompt-library modules, freeze-memory readers, provider libraries, embedding libraries, vector-store clients, network clients, subprocess helpers, persistence writers, GUI modules, approval writers, activation modules, Pilot modules, or Copilot modules.

The future abort contract must operate only on caller-supplied in-memory data. It must not read live project files. It must not inspect live prompt-library files. It must not inspect live freeze memory. It must not inspect live router canon. It must not inspect the gold registry. It must not inspect candidate package files. It must not reach outside the provided static bundle.

## No implementation doctrine

MLRT-20 must not be interpreted as authorization to implement an abort contract. It only defines future abort-contract planning for the static-interface contract path. A later governed milestone must explicitly authorize any code, and that later code must remain non-runtime and non-authoritative.

## No testing doctrine

MLRT-20 must not be interpreted as authorization to start tests. It creates no test runner, no test fixtures, no dry-run execution records, no output capture records, no scoring, no route comparison, no reports, and no reliability evidence. Candidate reliability remains unvalidated.

## No reliability claim doctrine

MLRT-20 makes no reliability claim. It does not evaluate a candidate. It does not execute a candidate. It does not execute dry-run cases. It does not score any output. It does not compare routes. It does not create evidence. It does not create reports. It does not approve readiness. It does not approve reliability. It does not unlock ML implementation.

## Positive label doctrine

The only positive MLRT-20 label is:

```text
MLRT_FIRST_NON_RUNTIME_DRY_RUN_HARNESS_STUB_STATIC_INTERFACE_ABORT_CONTRACT_PLAN_READY_FOR_REJECTION_CONTRACT_PLANNING_ONLY
```

This label means only that MLRT-20 abort-contract planning documentation exists and may be followed by MLRT-21 rejection-contract planning after freeze with `FREEZE_MEMORY_STATUS: OK`.

It does not mean source files exist. It does not mean an abort contract exists. It does not mean a rejection contract exists. It does not mean executable abort gates exist. It does not mean executable rejection gates exist. It does not mean a source surface exists. It does not mean a static interface exists. It does not mean static interface files exist. It does not mean a harness exists. It does not mean runner code exists. It does not mean harness stub files exist. It does not mean implementation modules exist. It does not mean a boundary checker exists. It does not mean a contract schema exists. It does not mean a dry run has executed. It does not mean candidate outputs exist. It does not mean cases were executed. It does not mean scores exist. It does not mean reports exist. It does not mean candidate reliability is validated. It does not mean ML implementation is unlocked.

## Forbidden authority fields

The future abort-contract plan must reject or reserve these fields as forbidden authority signals:

```text
route_decision
load_prompt
execute_route
execute_dry_run
execute_candidate
run_case
score_case
compare_route
create_report
persist_report
approve_readiness
approve_reliability
approve_dry_run_execution
record_human_approval
write_freeze_memory
write_gold_registry
write_prompt_library
write_router_canon
write_startup_pack
activate_pilot
activate_copilot
enable_field_test
call_provider
call_embedding_model
network_call
subprocess_call
start_batch_mode
persist_ml_decision
runtime_command
pilot_instruction
copilot_instruction
```

These fields must not appear as permissions, return commands, state transitions, or authority-bearing outputs.

## Next safe milestone

After MLRT-20 is installed, validated, frozen, and exposed with `FREEZE_MEMORY_STATUS: OK`, the next safe milestone is:

```text
MLRT-21 Candidate First Non-Runtime Dry-Run Harness Stub Static Interface Rejection Contract Plan
```

MLRT-21 must remain documentation-only unless a later governed scope explicitly approves a narrower non-runtime change. It must not start tests. It must not create source files, abort-contract code, rejection-contract code, a source surface, a static interface, a harness, a runner, harness stub files, executable validators, schema code, dry-run execution records, candidate outputs, case execution, scoring, route comparison, reports, reliability validation, ML implementation unlock, route authority, prompt loading, provider calls, embeddings, persistence, activation, field-test mode, runtime Pilot behavior, or Copilot behavior.
