Handoff — Freeze Hint Intake / Local Freeze Formulary Workflow
Current project

Project root:

E:\kanda_reasoner

Active workflow area:

Freeze Feature After Update
project_freeze_after_update\freeze_hint_intake
project_freeze_after_update\frozen_features_memory
kanda_reasoner_app\freeze_hint_intake
kanda_reasoner_app\freeze_after_update_gui
What we were fixing

The Freeze Feature After Update tab / formulary workflow had several regressions:

It could open stale/deprecated feature data instead of the current feature.
It could preserve an old validated latest hint forever.
It could reopen already-frozen hints.
It could crash on malformed/scalar source metadata in latest_freeze_hint.json.
Windows temp-folder cleanup could make focused tests fail after product assertions had already passed.
The user wanted the correct workflow:
AI sends ZIP with KANDA_FREEZE_HINT.json.
Project stores JSON locally under project_freeze_after_update\freeze_hint_intake.
Freeze Feature After Update formulary reads from that local JSON.
Human reviews.
Preview writes nothing.
Confirm and Write creates the local freeze memory entry.
The used hint is marked consumed.
Startup freeze context is refreshed.
Patches/repairs completed during this session
1. freeze_hint_feature_id_alias_merge_v1

Status: validated.

Fixed:

freeze_tab_freeze_hint_intake_restore_v1
freeze-tab-freeze-hint-intake-restore-v1

These underscore/hyphen variants are now treated as aliases during validation evidence merge.

Validation markers:

VALIDATION OK: freeze_hint_feature_id_alias_merge_v1
CONTRACT_TEST_OK: freeze_hint_intake accepts underscore/hyphen feature_id aliases during validation evidence merge
STATUS: FREEZE_HINT_FEATURE_ID_ALIAS_MERGE_ACTIVE
2. freeze_tab_freeze_hint_intake_restore_v1

Status: validated.

Fixed the Freeze Feature After Update tab so New Local Freeze Entry reads project-local freeze hint intake before falling back to safe starter placeholders.

Validation markers:

VALIDATION OK: freeze_tab_freeze_hint_intake_restore_v1
CONTRACT_TEST_OK: Freeze tab reads project-local KANDA_FREEZE_HINT.json intake data before safe starter fallback
STATUS: FREEZE_TAB_FREEZE_HINT_INTAKE_RESTORED
3. freeze_hint_validated_latest_preserve_v1

Status: validated.

Fixed the issue where local validation evidence could be lost when the same hint/source was rescanned.

Validation markers:

VALIDATION OK: freeze_hint_validated_latest_preserve_v1
CONTRACT_TEST_OK: freeze_hint_intake preserves already validated latest hint before scanning unrelated staged ZIPs
STATUS: FREEZE_HINT_VALIDATED_LATEST_PRESERVE_ACTIVE
4. freeze_hint_skip_already_frozen_v1

Status: patch created and partly superseded by later fixes.

Original intent:

Do not reopen hints whose feature is already frozen.
Mark already-frozen latest hints as used.

This revealed live-state issues and was refined by the later entry-file and newest-staged fixes.

5. freeze_hint_skip_entry_file_frozen_v1

Status: patch created and partly superseded/refined by later fixes.

Original intent:

Detect already-frozen hints not only from freeze_index.json, but also from actual entry files under:
project_freeze_after_update\frozen_features_memory\entries

This helped identify that the rule order was still wrong because older validated latest hints could win before newer staged hints.

6. freeze_hint_prefer_newest_staged_v1

Status: validated.

This was a major logic repair.

Correct rule order established:

1. Consume/skip already-frozen or used hints.
2. Prefer newest unused staged KANDA_FREEZE_HINT.json.
3. Preserve validation evidence only when rescanning the same hint/source.
4. Use old latest only when no usable staged hint exists.
5. Fall back to safe starter only when no current hint exists.

Validation markers:

VALIDATION OK: freeze_hint_prefer_newest_staged_v1
CONTRACT_TEST_OK: freeze_hint_intake prefers newest unused staged hint instead of preserving older validated latest forever
STATUS: FREEZE_HINT_PREFER_NEWEST_STAGED_ACTIVE

Validation output confirmed:

Current New Local Freeze Entry title: Freeze Hint Prefer Newest Staged v1
7. freeze_hint_source_shape_tolerance_v1

Status: patch created and installed, then revealed one test cleanup issue.

Fixed real code crash:

ValueError: dictionary update sequence element #0 has length 1; 2 is required

Cause:

latest_freeze_hint.json had scalar source metadata like:

"source": "formulary_data_zip"

but the contract expected a dictionary.

Fixed behavior:

Scalar/non-dict source metadata is tolerated.
It is treated as absent/legacy source metadata instead of crashing.
Product behavior remains unchanged.
8. freeze_hint_windows_temp_cleanup_tolerance_v1

Status: validated and frozen.

Fixed Windows test harness cleanup failure:

OSError: [WinError 145] The directory is not empty

This was not a product behavior failure. Product assertions had passed; Windows temp cleanup raced/failed.

Changed test harness tolerance only:

Updated:
- tests/test_freeze_hint_skip_entry_file_frozen.py
- tests/test_freeze_hint_skip_already_frozen.py

Not changed:
- product code
- GUI behavior
- freeze writer behavior
- project freeze memory logic

Validation markers:

VALIDATION OK: freeze_hint_windows_temp_cleanup_tolerance_v1
CONTRACT_TEST_OK: freeze_hint_intake Windows temp cleanup tolerance keeps focused validations stable without changing product code
STATUS: FREEZE_HINT_WINDOWS_TEMP_CLEANUP_TOLERANCE_ACTIVE
Final local freeze write completed

The user then used the Freeze Feature After Update local workflow.

Observed workflow output:

Local freeze entry fields filled from project-local freeze hint intake when available; otherwise a safe starter was used. No files were written.
Local freeze entry preview is ready and writable. No files were written.
LOCAL FREEZE WRITE OK

Freeze written:

Freeze ID: freeze-20260616-freeze-hint-windows-temp-cleanup-tolerance-v1

Written files:

E:\kanda_reasoner\project_freeze_after_update\frozen_features_memory\entries\freeze-20260616-freeze-hint-windows-temp-cleanup-tolerance-v1.md
E:\kanda_reasoner\project_freeze_after_update\frozen_features_memory\freeze_index.json
E:\kanda_reasoner\project_freeze_after_update\frozen_features_memory\project_frozen_implemented_steps.md

The output also confirmed:

Freeze hint intake record marked as used for this freeze.

Startup/AI context refresh also ran:

AI COMPLIANCE REFRESH AFTER LOCAL WRITE
AI-send exposure refreshed for current freeze memory.
Startup freeze context refreshed for next AI programming session.
Context file inside startup ZIP: 09_active_project_freeze_context.md
Current state after freeze

Core logic is OK.

Confirmed:

Core local freeze/formulary logic: OK
Hint consumed after write: OK
Freeze memory written: OK
Startup context refreshed: OK

Current freeze memory count after write:

Index entries: 32
Entry files: 32
Active/non-superseded entries: 31

The latest frozen feature is present:

freeze-20260616-freeze-hint-windows-temp-cleanup-tolerance-v1
Remaining warning

There is one warning:

FREEZE_MEMORY_STATUS: STALE_EXPOSURE
Existing AI-send exposure artifacts are older than current freeze memory source files.

Interpretation:

This does not mean the local freeze write failed.
This does not mean freeze memory is bad.
It means the generated AI-send ZIP/exposure pack may be older than the newest freeze memory source files.
Before relying on the AI-send pack, refresh/regenerate exposure.

Current stale artifact mentioned:

project_freeze_after_update/files_to_send_ai/freeze_feature_ai_send_pack_20260616_121657.zip
Next safe action

Do this first in the next chat/session:

Ask the user to click:
Freeze Feature After Update
→ New Local Freeze Entry
Verify it does not reopen:
Freeze Hint Windows Temp Cleanup Tolerance v1

because that hint was marked used after the local freeze write.

Expected behavior:

It should open the next newest unused staged hint, if one exists.
Or safe starter placeholders, if no current unused hint exists.
If the stale exposure warning matters, regenerate/refresh the AI-send exposure pack from the Freeze Feature After Update tab or the existing freeze exposure tool.
Important rules to preserve

Do not regress these rules:

KANDA_FREEZE_HINT.json must remain patch ZIP root delivery metadata and must not be installed into the active project root.
Freeze hint intake state must remain under:
project_freeze_after_update\freeze_hint_intake

Project-specific frozen memory must remain under:
project_freeze_after_update\frozen_features_memory

Do not store project-specific frozen memory inside:
project_freeze_ledger

Preview Freeze Entry must remain read-only and must not write files.
Confirm and Write must require explicit human confirmation before writing governed freeze memory.
After Confirm and Write, the used freeze hint intake record must be marked used.
After local freeze write, startup freeze context must be refreshed.
External AI review remains advanced/fallback, not the normal local freeze path.
Box Architecture reminder

Reusable engine/blueprint:

project_freeze_ledger

Active selected project memory:

<active_project_root>\project_freeze_after_update\frozen_features_memory

Active selected project intake state:

<active_project_root>\project_freeze_after_update\freeze_hint_intake

Generated/export/review artifacts:

<active_project_root>\project_freeze_after_update\files_to_send_ai

Do not mix these boxes.

Patch delivery rules still active

For future patches:

Provide ZIP at drive root first, for example:
E:\patch_name.zip
Installer must move ZIP into:
E:\kanda_reasoner_delete_after_daily_work\
Do not extract/install from project root.
Do not install KANDA_FREEZE_HINT.json into project root.
Temporary validation scripts go only under delete_after_daily_work.
ZIP must contain only updated files plus root-level KANDA_FREEZE_HINT.json when applicable.
Install success may clear after 5 seconds.
Validation output should remain visible until copied.
What is frozen now

The latest frozen entry is:

freeze-20260616-freeze-hint-windows-temp-cleanup-tolerance-v1

This freezes the Windows temp cleanup tolerance test harness behavior, not a product-code change.

It protects:

tests/test_freeze_hint_skip_entry_file_frozen.py
tests/test_freeze_hint_skip_already_frozen.py
kanda_reasoner_app/freeze_hint_intake/contract.py
project_freeze_after_update/freeze_hint_intake/
project_freeze_after_update/frozen_features_memory/
Summary for next AI

The local freeze/formulary workflow is working now. The final successful path was:

data-only JSON ZIP installed under project_freeze_after_update\freeze_hint_intake
→ New Local Freeze Entry read local JSON
→ Preview generated without writing
→ Confirm and Write wrote freeze memory
→ hint marked used
→ AI/startup context refreshed

Only unresolved warning:

STALE_EXPOSURE

Refresh AI-send exposure before relying on generated AI-send ZIPs.