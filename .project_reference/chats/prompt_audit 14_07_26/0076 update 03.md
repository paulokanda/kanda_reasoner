Today 8:11 AM
first_prompts_to_ai(36).zip
Zip Archive
prompt_library(36).zip
Zip Archive
tell_AI_read_before_all(36).md
File
zz_read_only_if_modifying_startup_delivery(17).md
File

kanda_reasoner__source_archive_part01_of_01(38).zip
Zip Archive
kanda_reasoner__png_assets_part01_of_01(37).zip
Zip Archive
kanda_reasoner__error_memory_manifest(38).json
File
kanda_reasoner__error_memory_full(38).zip
Zip Archive
kanda_reasoner__error_memory_ai_prompt(38).md
File
kanda_reasoner__error_lessons_compact(38).json
File
kanda_reasoner__ai_handoff_upload_readme(38).txt
Document
kanda_reasoner__ai_handoff_upload(38).zip
Zip Archive
kanda_reasoner__ai_handoff_all_in_one(37).zip
Zip Archive
_RUN_COLLECTOR_STATUS(38).txt
Document

prompt_audit 14_07_26(2).zip
Zip Archive
000 HOW TO(4).md
File
0076 update 02.md
File
0075 q40 HANDOFF brick wall end(1).md
File
continue from here

Pasted text(133).txt
Document
Pasted text (2)(15).txt
Document
$ErrorActionPreference = "Stop"

$PROJECT_ROOT = "E:\kanda_reasoner"

$PATCH_NAME = 
    "kanda_prompt_audit_wave5a_patch_lifecycle_core_v1"

$FEATURE_ID = 
    "prompt-audit-wave5a-patch-lifecycle-core-v1"

$EXPECTED_ZIP_SHA256 = 
    "d7c73787ebc90f04e691a92ec9b932224603d556c7ef1f83afc5dd739a81e61d"

$Phase = "initialize"
$ValidationFailed = $false

try {
    $Phase = "resolve_paths"

    $ProjectItem = Get-Item 
        -LiteralPath $PROJECT_ROOT 
        -ErrorAction Stop

    $PROJECT_ROOT = $ProjectItem.FullName

    $DRIVE_ROOT = [System.IO.Path]::GetPathRoot(
        $PROJECT_ROOT
    )

    if ([string]::IsNullOrWhiteSpace($DRIVE_ROOT)) {
        throw "The project drive root could not be resolved."
    }

    $WORK_DIR = Join-Path 
        $DRIVE_ROOT 
        ($ProjectItem.Name + "_delete_after_daily_work")

    $STAGED_ZIP = Join-Path 
        $WORK_DIR 
        ($PATCH_NAME + ".zip")

    $EXTRACT_ROOT = Join-Path 
        $WORK_DIR 
        $PATCH_NAME

    $VALIDATE_SCRIPT = Join-Path 
        $EXTRACT_ROOT 
        "VALIDATE.ps1"

    $SUPPORT_ROOT = Join-Path 
        $DRIVE_ROOT 
        ($ProjectItem.Name + "_show_project_to_AI")

    $EVIDENCE_DIR = Join-Path 
        $SUPPORT_ROOT 
        "project_validation_evidence"

    $EVIDENCE_FILE = Join-Path 
        $EVIDENCE_DIR 
        ($FEATURE_ID + ".txt")

    if (-not (
        Test-Path 
            -LiteralPath $PROJECT_ROOT 
            -PathType Container
    )) {
        throw "Project root was not found: $PROJECT_ROOT"
    }

    if (-not (
        Test-Path 
            -LiteralPath $STAGED_ZIP 
            -PathType Leaf
    )) {
        throw "Staged Wave 5A ZIP was not found: $STAGED_ZIP"
    }

    if (-not (
        Test-Path 
            -LiteralPath $EXTRACT_ROOT 
            -PathType Container
    )) {
        throw "Extracted Wave 5A package was not found: $EXTRACT_ROOT"
    }

    if (-not (
        Test-Path 
            -LiteralPath $VALIDATE_SCRIPT 
            -PathType Leaf
    )) {
        throw "VALIDATE.ps1 was not found: $VALIDATE_SCRIPT"
    }

    $Phase = "verify_staged_zip"

    $StagedHash = (
        Get-FileHash 
            -LiteralPath $STAGED_ZIP 
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ("STAGED ZIP SHA256: " + $StagedHash)

    if ($StagedHash -ne $EXPECTED_ZIP_SHA256) {
        throw (
            "Staged ZIP hash mismatch. Expected: " +
            $EXPECTED_ZIP_SHA256 +
            " Actual: " +
            $StagedHash
        )
    }

    Write-Host "WAVE5A VALIDATION ZIP IDENTITY: PASS"
    Write-Host ""

    $Phase = "run_packaged_validation"

    & $VALIDATE_SCRIPT 
        -ProjectRoot $PROJECT_ROOT 
        -PatchZip $STAGED_ZIP 
        -NoTerminalCleanup

    $Phase = "verify_durable_evidence"

    if (-not (
        Test-Path 
            -LiteralPath $EVIDENCE_FILE 
            -PathType Leaf
    )) {
        throw (
            "Wave 5A validation evidence was not created: " +
            $EVIDENCE_FILE
        )
    }

    $EvidenceLines = @(
        [System.IO.File]::ReadAllLines(
            $EVIDENCE_FILE
        )
    )

    $RequiredEvidenceLines = @(
        "WAVE5A_CHANGED_FILE_COVERAGE: PASS",
        "WAVE5A_CLASS05_CORE_OWNER_RECONCILIATION: PASS",
        "WAVE5A_GOVERNED_RETIREMENTS: PASS",
        "WAVE5A_APPLICATION_DUPLICATE_REDIRECT: PASS",
        "WAVE5A_SOURCE_PAYLOAD_BYTE_IDENTITY: PASS",
        "WAVE5A_ARCHITECTURE_ERRORS_ZERO: PASS",
        (
            "PATCH ZIP SHA256: " +
            $EXPECTED_ZIP_SHA256
        ),
        (
            "VALIDATION OK: " +
            $FEATURE_ID
        ),
        "STATUS: IN_SYNC"
    )

    foreach ($RequiredLine in $RequiredEvidenceLines) {
        if ($EvidenceLines -notcontains $RequiredLine) {
            throw (
                "Durable validation evidence is missing: " +
                $RequiredLine
            )
        }
    }

    $ArchitectureLines = @(
        $EvidenceLines |
            Where-Object {
                $_ -like "ARCHITECTURE SUMMARY:*"
            }
    )

    if ($ArchitectureLines.Count -ne 1) {
        throw (
            "Durable evidence must contain exactly one " +
            "ARCHITECTURE SUMMARY line."
        )
    }

    Write-Host ""
    Write-Host "WAVE5A DURABLE EVIDENCE FILE: PASS"
    Write-Host ("EVIDENCE FILE: " + $EVIDENCE_FILE)
    Write-Host $ArchitectureLines[0]
    Write-Host "WAVE5A WINDOWS VALIDATION: PASS"
    Write-Host ("VALIDATED ZIP: " + $STAGED_ZIP)
    Write-Host ("VALIDATION PACKAGE: " + $EXTRACT_ROOT)
}
catch {
    $ValidationFailed = $true

    Write-Host ""
    Write-Host "WAVE5A WINDOWS VALIDATION ERROR"
    Write-Host ("PHASE: " + $Phase)
    Write-Host (
        "ERROR TYPE: " +
        $_.Exception.GetType().FullName
    )
    Write-Host (
        "ERROR MESSAGE: " +
        $_.Exception.Message
    )

    if ($_.InvocationInfo.PositionMessage) {
        Write-Host (
            "INVOCATION: " +
            $_.InvocationInfo.PositionMessage
        )
    }

    Write-Host ""
    Write-Host "Freeze remains blocked."
}

Write-Host ""
Read-Host "Press Enter to clear terminal"
Read-Host "Press Enter again to clear"
Clear-Host

if ($ValidationFailed) {
    return
}


Pasted text(134).txt
Document

continue

Pasted text(135).txt
Document

Pasted text(136).txt
Document
Pasted text (2)(16).txt
Document

Pasted text(137).txt
Document

Pasted text(138).txt
Document
go next

Pasted text(139).txt
Document

Pasted text(140).txt
Document
Pasted text (2)(17).txt
Document

Pasted text(141).txt
Document
ERRORS:
- Missing mandatory field: validated_files
- Missing mandatory field: validation_evidence_summary
- FREEZE BLOCKED - no recognizable validation evidence found in validation_evidence_summary.

WARNINGS:
- Validation evidence is short; paste full validation output when available.
---
freeze_id: "freeze-20260717-current-validated-feature-replace-with-exact-feature-title"
feature_title: "Current validated feature - replace with exact feature title"
box: "Replace with the primary box for the current feature"
status: "frozen"
date: "2026-07-17"
entry: "project_freeze_after_update/frozen_features_memory/entries/freeze-20260717-current-validated-feature-replace-with-exact-feature-title.md"
protected_paths:
  - "project_freeze_after_update/frozen_features_memory"
do_not_touch_summary:
  - "Do not freeze without current feature validation evidence."
  - "Preserve the current feature behavior validated by the user."
  - "Keep project-specific frozen memory under <project>_show_project_to_AI/project_freeze_after_update/frozen_features_memory."
  - "Do not store project-specific frozen memory inside project_freeze_ledger."
  - "Do not replace current feature data with stale legacy workflow data."
superseded_by: null
---

# freeze-20260717-current-validated-feature-replace-with-exact-feature-title

## freeze identity

Freeze ID: freeze-20260717-current-validated-feature-replace-with-exact-feature-title

Feature title: Current validated feature - replace with exact feature title

Date: 2026-07-17

Primary box: Replace with the primary box for the current feature

Box type: Replace with the current box type

Status: frozen after explicit human confirmation and validation evidence

## summary

Auto-filled by the Freeze Feature After Update tab as a safe current-feature starter. This draft intentionally avoids stale legacy freeze-workflow titles, paths, and validation markers.

## validated files

- None recorded.

## generated files

- None recorded.

## protected paths

- project_freeze_after_update/frozen_features_memory

## do-not-regress rules

- Do not freeze without current feature validation evidence.
- Preserve the current feature behavior validated by the user.
- Keep project-specific frozen memory under <project>_show_project_to_AI/project_freeze_after_update/frozen_features_memory.
- Do not store project-specific frozen memory inside project_freeze_ledger.
- Do not replace current feature data with stale legacy workflow data.

## validation evidence

text



## known warnings

Starter draft only. Replace placeholders with the current validated feature data. No validation evidence has been inferred or invented. No unused KANDA_FREEZE_HINT.json sidecar was found in staged patch ZIPs. Newest matching freeze hint sidecars were already consumed: kanda_prompt_audit_wave6a_core_architecture_refactor_owners_v1.zip, kanda_prompt_audit_wave5b_router_terminal_compatibility_v1.zip, kanda_prompt_audit_wave5a_patch_lifecycle_core_v1.zip Stopped at the newest consumed or already-frozen freeze hint sidecar instead of falling through to older stale sidecars. Latest saved freeze hint record has already been used.

## planned next step

Replace placeholders with current feature evidence, then Preview Freeze Entry before Confirm and Write.

## local freeze writer provenance

Generated by 
local_freeze_writer_engine_v1 at 2026-07-17T14:31:25Z.

This entry was created locally for the selected active project. The reusable project_freeze_ledger engine remains blueprint logic only and is not the owner of this project-specific memory.

Pasted text(142).txt
Document

Pasted text(143).txt
Document

Pasted text(144).txt
Document
go

Pasted text(145).txt
Document
Pasted text (2)(18).txt
Document

Pasted text(146).txt
Document

Pasted text(147).txt
Document

Pasted text(148).txt
Document
go

Pasted text(149).txt
Document

Pasted text(150).txt
Document

Pasted text(151).txt
Document

Wave 8A passed the full packaged validation chain. All focused and inherited regressions passed, durable evidence was written, architecture reports Errors: 0, and the package remains STATUS: IN_SYNC.

Press Enter twice to leave the validation prompt, then run this from a clean PS E:\kanda_reasoner> prompt.

$ErrorActionPreference = "Stop"

$PROJECT_ROOT = "E:\kanda_reasoner"

$PATCH_NAME = `
    "kanda_prompt_audit_wave8a_human_handbook_reclassification_v1"

$FEATURE_ID = `
    "prompt-audit-wave8a-human-handbook-reclassification-v1"

$EXPECTED_ZIP_SHA256 = `
    "aeb13fcbe22d20a5a9f445286db3a7048e1ff5f51369eb05d7cd03a522c973af"

$Phase = "initialize"
$FreezeFailed = $false
$FreezeAborted = $false
$FreezeMayAlreadyExist = $false
$PreviousPythonPath = $env:PYTHONPATH

try {
    $Phase = "resolve_paths"

    $ProjectItem = Get-Item `
        -LiteralPath $PROJECT_ROOT `
        -ErrorAction Stop

    $PROJECT_ROOT = $ProjectItem.FullName

    $DRIVE_ROOT = [System.IO.Path]::GetPathRoot(
        $PROJECT_ROOT
    )

    if ([string]::IsNullOrWhiteSpace($DRIVE_ROOT)) {
        throw "The project drive root could not be resolved."
    }

    $WORK_DIR = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_delete_after_daily_work")

    $STAGED_ZIP = Join-Path `
        $WORK_DIR `
        ($PATCH_NAME + ".zip")

    $SUPPORT_ROOT = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_show_project_to_AI")

    $EVIDENCE_FILE = Join-Path `
        $SUPPORT_ROOT `
        "project_validation_evidence\$FEATURE_ID.txt"

    $HELPER = Join-Path `
        $WORK_DIR `
        "human_confirmed_wave8a_freeze.py"

    $STATE_FILE = Join-Path `
        $WORK_DIR `
        "wave8a_reviewed_freeze_preview.json"

    foreach ($RequiredFile in @(
        $STAGED_ZIP,
        $EVIDENCE_FILE
    )) {
        if (-not (
            Test-Path `
                -LiteralPath $RequiredFile `
                -PathType Leaf
        )) {
            throw "Required file was not found: $RequiredFile"
        }
    }

    $StagedHash = (
        Get-FileHash `
            -LiteralPath $STAGED_ZIP `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ("STAGED ZIP SHA256: " + $StagedHash)

    if ($StagedHash -ne $EXPECTED_ZIP_SHA256) {
        throw (
            "Staged Wave 8A ZIP hash mismatch. Expected: " +
            $EXPECTED_ZIP_SHA256 +
            " Actual: " +
            $StagedHash
        )
    }

    Write-Host "WAVE8A FREEZE ZIP IDENTITY: PASS"

    if (Test-Path -LiteralPath $STATE_FILE) {
        Remove-Item `
            -LiteralPath $STATE_FILE `
            -Force
    }

    $Phase = "write_freeze_helper"

    $PythonSource = @'
"""Prepare and write one human-confirmed Wave 8A freeze entry."""

from __future__ import annotations

import argparse
import hashlib
import json
import sys
import zipfile
from pathlib import Path
from typing import Any, Mapping


FEATURE_ID = (
    "prompt-audit-wave8a-human-handbook-reclassification-v1"
)

FEATURE_TITLE = (
    "Prompt Audit Wave 8A - "
    "Human Factors and Handbook Reclassification"
)

PRIMARY_BOX = "12_generalized_project_canons"

EXPECTED_ZIP_SHA256 = (
    "aeb13fcbe22d20a5a9f445286db3a7048e1ff5f51369eb05d7cd03a522c973af"
)

REQUIRED_EVIDENCE_MARKERS = (
    "WAVE8A_CHANGED_FILE_COVERAGE: PASS",
    "WAVE8A_GOVERNED_RETIREMENTS: PASS",
    "WAVE8A_CLASS08_NON_PYTHON_REMOVAL: PASS",
    "WAVE8A_CLASS12_RECLASSIFICATION: PASS",
    "WAVE8A_PEOPLEWARE_PRIVACY_BOUNDARY: PASS",
    "WAVE8A_HANDBOOK_PROVENANCE_BOUNDARY: PASS",
    "WAVE8A_ROUTING_COMPLETENESS: PASS",
    "WAVE8A_NO_STARTUP_PROMOTION: PASS",
    "WAVE8A_SOURCE_PAYLOAD_BYTE_IDENTITY: PASS",
    "WAVE8A_ARCHITECTURE_ERRORS_ZERO: PASS",
    "PATCH ZIP SHA256: " + EXPECTED_ZIP_SHA256,
    "VALIDATION OK: " + FEATURE_ID,
    "STATUS: IN_SYNC",
)


def print_items(title: str, values: Any) -> None:
    items = list(values or [])

    if not items:
        return

    print(title)

    for item in items:
        print("- " + str(item))


def require_ok(
    result: Mapping[str, Any],
    operation: str,
) -> None:
    if result.get("ok"):
        return

    print_items("ERRORS:", result.get("errors"))
    print_items("WARNINGS:", result.get("warnings"))

    raise RuntimeError(operation + " failed.")


def sha256_file(path: Path) -> str:
    digest = hashlib.sha256()

    with path.open("rb") as handle:
        while True:
            chunk = handle.read(1024 * 1024)

            if not chunk:
                break

            digest.update(chunk)

    return digest.hexdigest()


def support_root_for(project_root: Path) -> Path:
    return (
        Path(project_root.anchor)
        / (project_root.name + "_show_project_to_AI")
    )


def matching_frozen_entries(
    project_root: Path,
) -> list[Path]:
    entries_root = (
        support_root_for(project_root)
        / "project_freeze_after_update"
        / "frozen_features_memory"
        / "entries"
    )

    if not entries_root.is_dir():
        return []

    return sorted(
        path
        for path in entries_root.glob("*.md")
        if "wave-8a" in path.name.lower()
        or "wave8a" in path.name.lower()
    )


def import_contracts(
    project_root: Path,
) -> dict[str, Any]:
    project_text = str(project_root)

    if project_text not in sys.path:
        sys.path.insert(0, project_text)

    from kanda_reasoner_app.freeze_after_update.contract import (
        preview_freeze_entry,
        refresh_ai_compliance_context,
        validate_freeze_entry_preview,
        write_confirmed_freeze_entry,
    )

    from kanda_reasoner_app.freeze_hint_intake.contract import (
        mark_latest_freeze_hint_used,
        merge_validation_evidence_into_latest_hint,
        resolve_freeze_hint_autofill_state,
    )

    return {
        "preview_freeze_entry": preview_freeze_entry,
        "refresh_ai_compliance_context": (
            refresh_ai_compliance_context
        ),
        "validate_freeze_entry_preview": (
            validate_freeze_entry_preview
        ),
        "write_confirmed_freeze_entry": (
            write_confirmed_freeze_entry
        ),
        "mark_latest_freeze_hint_used": (
            mark_latest_freeze_hint_used
        ),
        "merge_validation_evidence_into_latest_hint": (
            merge_validation_evidence_into_latest_hint
        ),
        "resolve_freeze_hint_autofill_state": (
            resolve_freeze_hint_autofill_state
        ),
    }


def load_exact_hint(
    patch_zip: Path,
) -> dict[str, Any]:
    if sha256_file(patch_zip) != EXPECTED_ZIP_SHA256:
        raise RuntimeError(
            "The Wave 8A ZIP changed before Preview."
        )

    with zipfile.ZipFile(patch_zip, "r") as archive:
        names = archive.namelist()

        if names.count("KANDA_FREEZE_HINT.json") != 1:
            raise RuntimeError(
                "The ZIP must contain exactly one "
                "KANDA_FREEZE_HINT.json."
            )

        hint = json.loads(
            archive.read(
                "KANDA_FREEZE_HINT.json"
            ).decode("utf-8-sig")
        )

    if str(hint.get("feature_id") or "") != FEATURE_ID:
        raise RuntimeError(
            "Freeze sidecar feature ID does not match Wave 8A."
        )

    if str(hint.get("feature_title") or "") != FEATURE_TITLE:
        raise RuntimeError(
            "Freeze sidecar title does not match Wave 8A."
        )

    if str(hint.get("primary_box") or "") != PRIMARY_BOX:
        raise RuntimeError(
            "Freeze sidecar primary box does not match Wave 8A."
        )

    return hint


def build_validation_summary(
    evidence_file: Path,
) -> str:
    evidence_text = evidence_file.read_text(
        encoding="utf-8-sig",
        errors="strict",
    )

    for marker in REQUIRED_EVIDENCE_MARKERS:
        if marker not in evidence_text:
            raise RuntimeError(
                "Durable evidence is missing required marker: "
                + marker
            )

    architecture_lines = [
        line.strip()
        for line in evidence_text.splitlines()
        if line.startswith("ARCHITECTURE SUMMARY:")
    ]

    if len(architecture_lines) != 1:
        raise RuntimeError(
            "Durable evidence must contain exactly one "
            "ARCHITECTURE SUMMARY line."
        )

    if "Errors: 0" not in architecture_lines[0]:
        raise RuntimeError(
            "Architecture evidence does not report zero errors."
        )

    supplemental = (
        "WAVE8A WINDOWS INSTALL: PASS",
        "WAVE8A WINDOWS VALIDATION: PASS",
        "WAVE8A DURABLE EVIDENCE FILE: PASS",
    )

    return (
        evidence_text.rstrip()
        + "\n"
        + "\n".join(supplemental)
        + "\n"
    )


def as_lines(values: Any) -> str:
    return "\n".join(
        str(value).strip()
        for value in list(values or [])
        if str(value).strip()
    )


def build_explicit_inputs(
    hint: Mapping[str, Any],
    validation_summary: str,
) -> dict[str, str]:
    protected_paths = list(
        hint.get("protected_paths") or []
    )

    memory_path = (
        "<project>_show_project_to_AI/"
        "project_freeze_after_update/"
        "frozen_features_memory"
    )

    if memory_path not in protected_paths:
        protected_paths.append(memory_path)

    rules = list(
        hint.get("do_not_regress_rules") or []
    )

    governance_rules = (
        (
            "Project-specific frozen memory must remain under "
            "<project>_show_project_to_AI/"
            "project_freeze_after_update/"
            "frozen_features_memory."
        ),
        (
            "Do not store project-specific frozen memory "
            "inside project_freeze_ledger."
        ),
        (
            "Preview Freeze Entry must remain read-only and "
            "must not write files."
        ),
        (
            "Confirm and Write must require explicit human "
            "confirmation before writing governed freeze memory."
        ),
        (
            "After a local freeze write, refresh the AI startup "
            "freeze context."
        ),
        (
            "External AI review remains advanced or fallback, "
            "not the normal local freeze path."
        ),
    )

    for rule in governance_rules:
        if rule not in rules:
            rules.append(rule)

    inputs = {
        "feature_title": FEATURE_TITLE,
        "primary_box": PRIMARY_BOX,
        "box_type": str(
            hint.get("box_type")
            or "one_primary_box_with_bounded_supporting_touches"
        ),
        "validated_files": as_lines(
            hint.get("validated_files")
        ),
        "generated_files": as_lines(
            hint.get("generated_files")
        ),
        "protected_paths": as_lines(
            protected_paths
        ),
        "do_not_regress_rules": as_lines(
            rules
        ),
        "validation_evidence_summary": validation_summary,
        "known_warnings": (
            "Windows-local architecture validation reports "
            "three warnings and zero errors."
        ),
        "planned_next_step": (
            "Write this reviewed Wave 8A entry only after "
            "explicit human confirmation, then refresh the "
            "AI startup freeze context."
        ),
        "notes": (
            str(hint.get("notes") or "").strip()
            + " Source patch ZIP: "
            + str(hint.get("source_patch_zip") or "").strip()
            + ". The validated form is supplied as an explicit "
            "preview-session snapshot so stale pre-validation "
            "intake text cannot replace current evidence."
        ),
    }

    if not inputs["validated_files"].strip():
        raise RuntimeError(
            "Explicit validated_files is empty."
        )

    if not inputs[
        "validation_evidence_summary"
    ].strip():
        raise RuntimeError(
            "Explicit validation evidence is empty."
        )

    return inputs


def write_state(
    path: Path,
    state: Mapping[str, Any],
) -> None:
    text = json.dumps(
        state,
        ensure_ascii=True,
        indent=2,
        sort_keys=True,
        default=str,
    )

    path.write_text(
        text + "\n",
        encoding="utf-8",
        newline="\n",
    )


def prepare(
    project_root: Path,
    patch_zip: Path,
    evidence_file: Path,
    state_file: Path,
) -> int:
    existing = matching_frozen_entries(project_root)

    if existing:
        print("FREEZE PREPARATION BLOCKED")
        print("A Wave 8A frozen entry already exists:")

        for path in existing:
            print("- " + str(path))

        print("Do not create a duplicate freeze entry.")
        return 2

    hint = load_exact_hint(patch_zip)

    validation_summary = build_validation_summary(
        evidence_file
    )

    explicit_inputs = build_explicit_inputs(
        hint,
        validation_summary,
    )

    contracts = import_contracts(project_root)

    merge_result = contracts[
        "merge_validation_evidence_into_latest_hint"
    ](
        project_root,
        validation_summary,
        feature_id=FEATURE_ID,
        feature_title=FEATURE_TITLE,
    )

    if merge_result.get("ok"):
        print("WAVE8A INTAKE EVIDENCE REPAIR: PASS")

    if not merge_result.get("ok"):
        print("WAVE8A INTAKE EVIDENCE REPAIR: WARNING")

        print_items(
            "ERRORS:",
            merge_result.get("errors"),
        )

        print_items(
            "WARNINGS:",
            merge_result.get("warnings"),
        )

        print(
            "The explicit Preview snapshot remains "
            "authoritative."
        )

    autofill = contracts[
        "resolve_freeze_hint_autofill_state"
    ](
        project_root,
        {},
        preview_form_inputs=explicit_inputs,
    )

    require_ok(
        autofill,
        "resolve_freeze_hint_autofill_state",
    )

    if (
        str(autofill.get("source_kind") or "")
        != "preview_session_snapshot"
    ):
        raise RuntimeError(
            "Wave 8A did not resolve from the explicit "
            "Preview snapshot."
        )

    if autofill.get("confirm_write_enabled") is not True:
        raise RuntimeError(
            "Wave 8A Confirm and Write is not enabled."
        )

    form_inputs = dict(
        autofill.get("form_inputs") or {}
    )

    if str(
        form_inputs.get("feature_title") or ""
    ).strip() != FEATURE_TITLE:
        raise RuntimeError(
            "Resolved feature title is not Wave 8A."
        )

    if str(
        form_inputs.get("primary_box") or ""
    ).strip() != PRIMARY_BOX:
        raise RuntimeError(
            "Resolved primary box is not Class 12."
        )

    preview = contracts[
        "preview_freeze_entry"
    ](
        project_root,
        form_inputs,
    )

    require_ok(
        preview,
        "preview_freeze_entry",
    )

    if preview.get("is_writable") is not True:
        raise RuntimeError(
            "The Wave 8A Preview is not writable."
        )

    validation = contracts[
        "validate_freeze_entry_preview"
    ](
        project_root,
        preview,
    )

    require_ok(
        validation,
        "validate_freeze_entry_preview",
    )

    markdown = str(
        preview.get("markdown") or ""
    )

    required_preview_markers = (
        FEATURE_TITLE,
        PRIMARY_BOX,
        "WAVE8A_CLASS08_NON_PYTHON_REMOVAL: PASS",
        "WAVE8A_CLASS12_RECLASSIFICATION: PASS",
        "WAVE8A_PEOPLEWARE_PRIVACY_BOUNDARY: PASS",
        "WAVE8A_HANDBOOK_PROVENANCE_BOUNDARY: PASS",
        "VALIDATION OK: " + FEATURE_ID,
        "STATUS: IN_SYNC",
    )

    for marker in required_preview_markers:
        if marker not in markdown:
            raise RuntimeError(
                "Wave 8A Preview is missing marker: "
                + marker
            )

    stale_markers = (
        (
            "Current validated feature - replace with "
            "exact feature title"
        ),
        (
            "Replace with the primary box for the "
            "current feature"
        ),
        (
            "Windows-local installation and packaged "
            "validation remain required"
        ),
    )

    for marker in stale_markers:
        if marker in markdown:
            raise RuntimeError(
                "Stale or starter text entered the "
                "Wave 8A Preview: "
                + marker
            )

    state = {
        "schema_version": "1.0",
        "feature_id": FEATURE_ID,
        "feature_title": FEATURE_TITLE,
        "primary_box": PRIMARY_BOX,
        "project_root": str(project_root),
        "patch_zip_sha256": EXPECTED_ZIP_SHA256,
        "preview": preview,
    }

    write_state(
        state_file,
        state,
    )

    state_hash = hashlib.sha256(
        state_file.read_bytes()
    ).hexdigest()

    print("")
    print("WAVE8A FREEZE PREVIEW")
    print("=====================")
    print(markdown)
    print("")
    print("PREVIEW SOURCE: EXPLICIT SESSION SNAPSHOT")
    print("EXPLICIT VALIDATED FILES: PASS")
    print("EXPLICIT VALIDATION EVIDENCE: PASS")
    print("STALE PENDING TEXT ABSENT: PASS")
    print("STARTER FALLBACK ABSENT: PASS")
    print("PREVIEW VALIDATION: PASS")
    print("PREVIEW IS WRITABLE: PASS")
    print("FEATURE ID: " + FEATURE_ID)
    print("PRIMARY BOX: " + PRIMARY_BOX)
    print("PREVIEW STATE: " + str(state_file))
    print("PREVIEW STATE SHA256: " + state_hash)
    print("")
    print("NO FREEZE ENTRY HAS BEEN WRITTEN.")

    return 0


def write(
    project_root: Path,
    state_file: Path,
) -> int:
    existing = matching_frozen_entries(project_root)

    if existing:
        print("FREEZE WRITE BLOCKED")
        print("A Wave 8A frozen entry already exists:")

        for path in existing:
            print("- " + str(path))

        print("Do not retry or create a duplicate entry.")
        return 3

    state = json.loads(
        state_file.read_text(
            encoding="utf-8-sig",
        )
    )

    if str(state.get("schema_version") or "") != "1.0":
        raise RuntimeError(
            "Preview state schema is invalid."
        )

    if str(state.get("feature_id") or "") != FEATURE_ID:
        raise RuntimeError(
            "Preview state feature ID is invalid."
        )

    if str(state.get("primary_box") or "") != PRIMARY_BOX:
        raise RuntimeError(
            "Preview state primary box is invalid."
        )

    if (
        str(state.get("patch_zip_sha256") or "")
        != EXPECTED_ZIP_SHA256
    ):
        raise RuntimeError(
            "Preview state ZIP identity is invalid."
        )

    recorded_root = Path(
        str(state.get("project_root") or "")
    ).resolve()

    if recorded_root != project_root:
        raise RuntimeError(
            "Preview state project root changed."
        )

    preview = dict(
        state.get("preview") or {}
    )

    contracts = import_contracts(project_root)

    validation = contracts[
        "validate_freeze_entry_preview"
    ](
        project_root,
        preview,
    )

    require_ok(
        validation,
        "validate_freeze_entry_preview",
    )

    result = contracts[
        "write_confirmed_freeze_entry"
    ](
        project_root,
        preview,
        confirmation=True,
    )

    require_ok(
        result,
        "write_confirmed_freeze_entry",
    )

    freeze_id = str(
        result.get("freeze_id") or ""
    ).strip()

    print("")
    print("LOCAL FREEZE WRITE OK")
    print("FREEZE ID: " + freeze_id)

    print_items(
        "WRITTEN PATHS:",
        result.get("written_paths"),
    )

    intake_result = contracts[
        "mark_latest_freeze_hint_used"
    ](
        project_root,
        freeze_id=freeze_id,
    )

    if intake_result.get("ok"):
        print("FREEZE HINT MARKED USED: PASS")

    if not intake_result.get("ok"):
        print("FREEZE HINT MARKED USED: WARNING")

        print_items(
            "ERRORS:",
            intake_result.get("errors"),
        )

        print_items(
            "WARNINGS:",
            intake_result.get("warnings"),
        )

    compliance = contracts[
        "refresh_ai_compliance_context"
    ](
        project_root
    )

    if compliance.get("ok"):
        print("AI COMPLIANCE REFRESH: PASS")

    if compliance.get("startup_context_refreshed"):
        print(
            "STARTUP FREEZE CONTEXT REFRESH: PASS"
        )

    if compliance.get("startup_zip"):
        print(
            "STARTUP ZIP: "
            + str(compliance.get("startup_zip"))
        )

    if compliance.get(
        "read_before_all_instruction"
    ):
        print(
            "READ-BEFORE-ALL FILE: "
            + str(
                compliance.get(
                    "read_before_all_instruction"
                )
            )
        )

    if not compliance.get("ok"):
        print("AI COMPLIANCE REFRESH: WARNING")

        print_items(
            "ERRORS:",
            compliance.get("errors"),
        )

        print_items(
            "WARNINGS:",
            compliance.get("warnings"),
        )

        print(
            "The Wave 8A freeze entry was written. "
            "Do not rerun the freeze write."
        )

        return 4

    if not intake_result.get("ok"):
        print(
            "The Wave 8A freeze entry was written, but "
            "intake cleanup needs review. Do not rerun it."
        )

        return 4

    print("STATUS: FROZEN")
    return 0


def main() -> int:
    parser = argparse.ArgumentParser()

    parser.add_argument(
        "--mode",
        required=True,
        choices=("prepare", "write"),
    )

    parser.add_argument(
        "--project-root",
        required=True,
    )

    parser.add_argument(
        "--patch-zip",
        default="",
    )

    parser.add_argument(
        "--evidence-file",
        default="",
    )

    parser.add_argument(
        "--state-file",
        required=True,
    )

    args = parser.parse_args()

    project_root = Path(
        args.project_root
    ).expanduser().resolve()

    state_file = Path(
        args.state_file
    ).expanduser().resolve()

    if args.mode == "prepare":
        return prepare(
            project_root,
            Path(args.patch_zip).expanduser().resolve(),
            Path(args.evidence_file).expanduser().resolve(),
            state_file,
        )

    return write(
        project_root,
        state_file,
    )


if __name__ == "__main__":
    raise SystemExit(main())
'@

    $Utf8NoBom = New-Object `
        System.Text.UTF8Encoding($false)

    [System.IO.File]::WriteAllText(
        $HELPER,
        $PythonSource,
        $Utf8NoBom
    )

    $env:PYTHONPATH = $PROJECT_ROOT

    $Phase = "compile_freeze_helper"

    & python `
        -m py_compile `
        $HELPER

    if ($LASTEXITCODE -ne 0) {
        throw (
            "Wave 8A freeze helper compilation returned " +
            "exit code " +
            $LASTEXITCODE
        )
    }

    Write-Host "WAVE8A FREEZE HELPER COMPILE: PASS"
    Write-Host ""

    $Phase = "prepare_read_only_preview"

    & python `
        $HELPER `
        --mode prepare `
        --project-root $PROJECT_ROOT `
        --patch-zip $STAGED_ZIP `
        --evidence-file $EVIDENCE_FILE `
        --state-file $STATE_FILE

    $PrepareExitCode = $LASTEXITCODE

    if ($PrepareExitCode -ne 0) {
        throw (
            "Wave 8A Preview returned exit code " +
            $PrepareExitCode
        )
    }

    $StateHashBefore = (
        Get-FileHash `
            -LiteralPath $STATE_FILE `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ""
    Write-Host "Review the Wave 8A Preview above carefully."
    Write-Host "No freeze entry has been written yet."
    Write-Host ""

    $Confirmation = Read-Host `
        "Type exactly CONFIRM WAVE8A FREEZE to write it"

    if ($Confirmation -cne "CONFIRM WAVE8A FREEZE") {
        $FreezeAborted = $true

        Write-Host ""
        Write-Host "FREEZE ABORTED"
        Write-Host "No freeze entry was written."
    }

    if (-not $FreezeAborted) {
        $Phase = "verify_reviewed_preview_identity"

        $StateHashAfter = (
            Get-FileHash `
                -LiteralPath $STATE_FILE `
                -Algorithm SHA256
        ).Hash.ToLowerInvariant()

        if ($StateHashAfter -ne $StateHashBefore) {
            throw (
                "The reviewed Preview state changed. " +
                "Freeze write is blocked."
            )
        }

        $Phase = "human_confirmed_write_and_refresh"

        & python `
            $HELPER `
            --mode write `
            --project-root $PROJECT_ROOT `
            --state-file $STATE_FILE

        $WriteExitCode = $LASTEXITCODE

        if ($WriteExitCode -eq 4) {
            $FreezeMayAlreadyExist = $true

            throw (
                "The Wave 8A freeze entry was written, but " +
                "post-write cleanup or refresh was incomplete. " +
                "Do not rerun the freeze write."
            )
        }

        if ($WriteExitCode -ne 0) {
            throw (
                "Confirmed Wave 8A freeze returned exit code " +
                $WriteExitCode
            )
        }

        Remove-Item `
            -LiteralPath $STATE_FILE `
            -Force

        Write-Host ""
        Write-Host "WAVE8A HUMAN-CONFIRMED FREEZE: PASS"
        Write-Host "STARTUP FREEZE CONTEXT: REFRESHED"
    }
}
catch {
    $FreezeFailed = $true

    Write-Host ""
    Write-Host "WAVE8A FREEZE ERROR"
    Write-Host ("PHASE: " + $Phase)
    Write-Host (
        "ERROR TYPE: " +
        $_.Exception.GetType().FullName
    )
    Write-Host (
        "ERROR MESSAGE: " +
        $_.Exception.Message
    )

    if ($_.InvocationInfo.PositionMessage) {
        Write-Host (
            "INVOCATION: " +
            $_.InvocationInfo.PositionMessage
        )
    }

    if ($FreezeMayAlreadyExist) {
        Write-Host ""
        Write-Host (
            "The Wave 8A freeze entry may already exist. " +
            "Do not rerun the write phase."
        )
    }
}

$env:PYTHONPATH = $PreviousPythonPath

Write-Host ""
Read-Host "Press Enter to clear terminal"
Read-Host "Press Enter again to clear"
Clear-Host

Before confirming, verify that the Preview contains:

PREVIEW SOURCE: EXPLICIT SESSION SNAPSHOT
EXPLICIT VALIDATED FILES: PASS
EXPLICIT VALIDATION EVIDENCE: PASS
STALE PENDING TEXT ABSENT: PASS
STARTER FALLBACK ABSENT: PASS
PREVIEW VALIDATION: PASS
PREVIEW IS WRITABLE: PASS
FEATURE ID: prompt-audit-wave8a-human-handbook-reclassification-v1
PRIMARY BOX: 12_generalized_project_canons
WAVE8A_CLASS08_NON_PYTHON_REMOVAL: PASS
WAVE8A_CLASS12_RECLASSIFICATION: PASS
WAVE8A_PEOPLEWARE_PRIVACY_BOUNDARY: PASS
WAVE8A_HANDBOOK_PROVENANCE_BOUNDARY: PASS
VALIDATION OK: prompt-audit-wave8a-human-handbook-reclassification-v1
STATUS: IN_SYNC

Then type this exact phrase inside PowerShell:

CONFIRM WAVE8A FREEZE

Successful completion must contain:

LOCAL FREEZE WRITE OK
FREEZE HINT MARKED USED: PASS
AI COMPLIANCE REFRESH: PASS
STARTUP FREEZE CONTEXT REFRESH: PASS
STATUS: FROZEN
WAVE8A HUMAN-CONFIRMED FREEZE: PASS
STARTUP FREEZE CONTEXT: REFRESHED

Do not rerun the write if LOCAL FREEZE WRITE OK appears, even if a later refresh step reports a warning.

Pasted text(152).txt
Document

Wave 8A is fully complete and frozen.

Confirmed markers:

LOCAL FREEZE WRITE OK
FREEZE HINT MARKED USED: PASS
AI COMPLIANCE REFRESH: PASS
STARTUP FREEZE CONTEXT REFRESH: PASS
STATUS: FROZEN
WAVE8A HUMAN-CONFIRMED FREEZE: PASS
STARTUP FREEZE CONTEXT: REFRESHED

Frozen entry:

E:\kanda_reasoner_show_project_to_AI\project_freeze_after_update\frozen_features_memory\entries\freeze-20260717-prompt-audit-wave-8a-human-factors-and-handbook-reclassification.md

The freeze index, consolidated implemented-steps record, startup ZIP, and read-before-all context were refreshed successfully.

Completed: 73 of 119 prompts
Remaining: 46 prompts

Wave 8A and all preceding frozen waves must now remain protected. The next wave should be derived from the exact post-Wave-8A source tree.

go

Wave 8B ready

Wave 8B reconciles audit prompts 072–075 as four distinct Class 08 specialists:

    KPR-08-001 — Python Clean Architecture

    KPR-08-002 — Python Clean Code

    KPR-08-003 — Python Design Patterns

    KPR-08-004 — Python Domain-Driven Design

The release removes broad routing aliases and overlapping authority, but does not merge or retire any of the four prompts. All remain on_request and absent from direct startup loading.
Download

SHA-256:
96876610e2d915af366ce95c02c6117cc586c7c33d551a11ae38e38cd1dacb3b

Size:
97,671 bytes

ZIP members:
24

Payload files:
15

Governed deletions:
0

Final release tests

ZIP contract: PASS
Installer contract: PASS
Fresh installation: PASS
Complete packaged validation: PASS
Idempotent reinstall: PASS
Unknown predecessor rejection before mutation: PASS
Unknown predecessor no mutation: PASS
Induced post-mutation validation failure: PASS
Transactional source rollback: PASS
Rollback managed-path identity: PASS
Startup synchronization after rollback: PASS
Deterministic byte-identical rebuild: PASS
Final immutable ZIP identity: PASS

The complete regression chain passed through Wave 8A, Wave 6C, all earlier frozen waves, Q36, the module-quality canon, and all historical large-module compatibility validators.

ARCHITECTURE SUMMARY:
Total issues: 103 | Errors: 0 | Warnings: 103 | Other: 0

VALIDATION OK:
prompt-audit-wave8b-python-architecture-design-boundaries-v1

STATUS: IN_SYNC

The disposable-environment warnings match the predecessor baseline; Wave 8B introduced no new architecture warning.
Progress

Currently frozen: 73 of 119
Handled by Wave 8B: 4
After Windows validation and Freeze: 77 of 119
Remaining afterward: 42

Windows installation

Place the ZIP at:

E:\kanda_prompt_audit_wave8b_python_architecture_design_boundaries_v1.zip

Run this from a clean PS E:\kanda_reasoner> prompt. Press Ctrl+C first if PowerShell displays >>.

$ErrorActionPreference = "Stop"

$PROJECT_ROOT = "E:\kanda_reasoner"

$PATCH_NAME = `
    "kanda_prompt_audit_wave8b_python_architecture_design_boundaries_v1"

$EXPECTED_ZIP_SHA256 = `
    "96876610e2d915af366ce95c02c6117cc586c7c33d551a11ae38e38cd1dacb3b"

$Phase = "initialize"
$InstallFailed = $false

try {
    $Phase = "resolve_project_paths"

    $ProjectItem = Get-Item `
        -LiteralPath $PROJECT_ROOT `
        -ErrorAction Stop

    $PROJECT_ROOT = $ProjectItem.FullName

    $DRIVE_ROOT = [System.IO.Path]::GetPathRoot(
        $PROJECT_ROOT
    )

    if ([string]::IsNullOrWhiteSpace($DRIVE_ROOT)) {
        throw "The project drive root could not be resolved."
    }

    $ROOT_ZIP = Join-Path `
        $DRIVE_ROOT `
        ($PATCH_NAME + ".zip")

    $WORK_DIR = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_delete_after_daily_work")

    $STAGED_ZIP = Join-Path `
        $WORK_DIR `
        ($PATCH_NAME + ".zip")

    $EXTRACT_ROOT = Join-Path `
        $WORK_DIR `
        $PATCH_NAME

    if (-not (
        Test-Path `
            -LiteralPath $ROOT_ZIP `
            -PathType Leaf
    )) {
        throw "Place the downloaded Wave 8B ZIP at: $ROOT_ZIP"
    }

    $Phase = "verify_downloaded_zip"

    $RootHash = (
        Get-FileHash `
            -LiteralPath $ROOT_ZIP `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ("ROOT ZIP SHA256: " + $RootHash)

    if ($RootHash -ne $EXPECTED_ZIP_SHA256) {
        throw (
            "Downloaded ZIP hash mismatch. Expected: " +
            $EXPECTED_ZIP_SHA256 +
            " Actual: " +
            $RootHash
        )
    }

    $Phase = "stage_zip"

    New-Item `
        -ItemType Directory `
        -Path $WORK_DIR `
        -Force |
        Out-Null

    Copy-Item `
        -LiteralPath $ROOT_ZIP `
        -Destination $STAGED_ZIP `
        -Force

    $StagedHash = (
        Get-FileHash `
            -LiteralPath $STAGED_ZIP `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    if ($StagedHash -ne $EXPECTED_ZIP_SHA256) {
        throw (
            "Staged ZIP hash mismatch. Expected: " +
            $EXPECTED_ZIP_SHA256 +
            " Actual: " +
            $StagedHash
        )
    }

    Write-Host "STAGED ZIP HASH: PASS"

    Remove-Item `
        -LiteralPath $ROOT_ZIP `
        -Force

    $Phase = "extract_package"

    if (Test-Path -LiteralPath $EXTRACT_ROOT) {
        Remove-Item `
            -LiteralPath $EXTRACT_ROOT `
            -Recurse `
            -Force
    }

    Expand-Archive `
        -LiteralPath $STAGED_ZIP `
        -DestinationPath $EXTRACT_ROOT `
        -Force

    $INSTALL_SCRIPT = Join-Path `
        $EXTRACT_ROOT `
        "INSTALL.ps1"

    if (-not (
        Test-Path `
            -LiteralPath $INSTALL_SCRIPT `
            -PathType Leaf
    )) {
        throw "INSTALL.ps1 was not found after extraction."
    }

    $Phase = "install_wave8b"

    & $INSTALL_SCRIPT `
        -ProjectRoot $PROJECT_ROOT `
        -PatchZip $STAGED_ZIP `
        -NoTerminalCleanup

    Write-Host ""
    Write-Host "WAVE8B PACKAGE RECEIVER: PASS"
    Write-Host ("STAGED ZIP: " + $STAGED_ZIP)
    Write-Host ("EXTRACT ROOT: " + $EXTRACT_ROOT)
}
catch {
    $InstallFailed = $true

    Write-Host ""
    Write-Host "WAVE8B WINDOWS INSTALL ERROR"
    Write-Host ("PHASE: " + $Phase)
    Write-Host (
        "ERROR TYPE: " +
        $_.Exception.GetType().FullName
    )
    Write-Host (
        "ERROR MESSAGE: " +
        $_.Exception.Message
    )

    if ($_.InvocationInfo.PositionMessage) {
        Write-Host (
            "INVOCATION: " +
            $_.InvocationInfo.PositionMessage
        )
    }

    Write-Host ""
    Write-Host "Validation and Freeze remain blocked."
}

if ($InstallFailed) {
    Write-Host ""
    Read-Host "Press Enter to clear terminal"
    Read-Host "Press Enter again to clear"
    Clear-Host
    return
}

Write-Host ""
Write-Host "Installation succeeded. Terminal will clear in 2 seconds..."
Start-Sleep -Seconds 2
Clear-Host

Successful installation must contain:

WAVE8B_SOURCE_BASELINE_HASHES
WAVE8B_BASELINE_PREFLIGHT: PASS
WAVE8B_INSTALLED_HASHES: PASS
WAVE8B_PY_COMPILE: PASS
WAVE8B_STARTUP_SYNC: PASS
WAVE8B_FOCUSED_INSTALL_CHECK: PASS
INSTALL_PACKAGE_OK: prompt-audit-wave8b-python-architecture-design-boundaries-v1
STATUS: IN_SYNC
INSTALL OK: prompt-audit-wave8b-python-architecture-design-boundaries-v1
WAVE8B WINDOWS INSTALL: PASS
WAVE8B PACKAGE RECEIVER: PASS

Do not run Freeze yet. The next step is the packaged Wave 8B Windows validation.

Pasted text(153).txt
Document

Wave 8B installed successfully. The exact ZIP hash, baseline preflight, installed hashes, Python compilation, startup synchronization, focused validation, and final STATUS: IN_SYNC all passed.

Run the packaged Windows validation next. Freeze remains blocked until it passes.

Use a clean PS E:\kanda_reasoner> prompt. Press Ctrl+C first if PowerShell displays >>.

$ErrorActionPreference = "Stop"

$PROJECT_ROOT = "E:\kanda_reasoner"

$PATCH_NAME = `
    "kanda_prompt_audit_wave8b_python_architecture_design_boundaries_v1"

$FEATURE_ID = `
    "prompt-audit-wave8b-python-architecture-design-boundaries-v1"

$EXPECTED_ZIP_SHA256 = `
    "96876610e2d915af366ce95c02c6117cc586c7c33d551a11ae38e38cd1dacb3b"

$Phase = "initialize"
$ValidationFailed = $false
$PreviousPythonPath = $env:PYTHONPATH

try {
    $Phase = "resolve_paths"

    $ProjectItem = Get-Item `
        -LiteralPath $PROJECT_ROOT `
        -ErrorAction Stop

    $PROJECT_ROOT = $ProjectItem.FullName

    $DRIVE_ROOT = [System.IO.Path]::GetPathRoot(
        $PROJECT_ROOT
    )

    if ([string]::IsNullOrWhiteSpace($DRIVE_ROOT)) {
        throw "The project drive root could not be resolved."
    }

    $WORK_DIR = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_delete_after_daily_work")

    $STAGED_ZIP = Join-Path `
        $WORK_DIR `
        ($PATCH_NAME + ".zip")

    $EXTRACT_ROOT = Join-Path `
        $WORK_DIR `
        $PATCH_NAME

    $VALIDATE_SCRIPT = Join-Path `
        $EXTRACT_ROOT `
        "VALIDATE.ps1"

    $SUPPORT_ROOT = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_show_project_to_AI")

    $EVIDENCE_FILE = Join-Path `
        $SUPPORT_ROOT `
        "project_validation_evidence\$FEATURE_ID.txt"

    foreach ($RequiredPath in @(
        $STAGED_ZIP,
        $VALIDATE_SCRIPT
    )) {
        if (-not (
            Test-Path `
                -LiteralPath $RequiredPath `
                -PathType Leaf
        )) {
            throw "Required file was not found: $RequiredPath"
        }
    }

    $Phase = "verify_staged_zip"

    $StagedHash = (
        Get-FileHash `
            -LiteralPath $STAGED_ZIP `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ("STAGED ZIP SHA256: " + $StagedHash)

    if ($StagedHash -ne $EXPECTED_ZIP_SHA256) {
        throw (
            "Staged ZIP hash mismatch. Expected: " +
            $EXPECTED_ZIP_SHA256 +
            " Actual: " +
            $StagedHash
        )
    }

    Write-Host "WAVE8B VALIDATION ZIP IDENTITY: PASS"
    Write-Host ""

    $Phase = "run_packaged_validation"

    $env:PYTHONPATH = $PROJECT_ROOT

    & $VALIDATE_SCRIPT `
        -ProjectRoot $PROJECT_ROOT `
        -PatchZip $STAGED_ZIP `
        -NoTerminalCleanup

    $Phase = "verify_durable_evidence"

    if (-not (
        Test-Path `
            -LiteralPath $EVIDENCE_FILE `
            -PathType Leaf
    )) {
        throw (
            "Wave 8B durable evidence was not created: " +
            $EVIDENCE_FILE
        )
    }

    $EvidenceLines = @(
        [System.IO.File]::ReadAllLines(
            $EVIDENCE_FILE
        )
    )

    $RequiredEvidenceLines = @(
        "WAVE8B_CHANGED_FILE_COVERAGE: PASS",
        "WAVE8B_NO_GOVERNED_DELETIONS: PASS",
        "WAVE8B_CLASS08_CANONICAL_IDENTITIES: PASS",
        "WAVE8B_CLEAN_ARCHITECTURE_BOUNDARY: PASS",
        "WAVE8B_CLEAN_CODE_LOCAL_SCOPE: PASS",
        "WAVE8B_PATTERN_SELECTION_BOUNDARY: PASS",
        "WAVE8B_DDD_MODELING_BOUNDARY: PASS",
        "WAVE8B_ROUTING_COMPLETENESS: PASS",
        "WAVE8B_NO_FORCED_COMPANIONS: PASS",
        "WAVE8B_NO_STARTUP_PROMOTION: PASS",
        "WAVE8B_SOURCE_PAYLOAD_BYTE_IDENTITY: PASS",
        "WAVE8B_ARCHITECTURE_ERRORS_ZERO: PASS",
        (
            "PATCH ZIP SHA256: " +
            $EXPECTED_ZIP_SHA256
        ),
        (
            "VALIDATION OK: " +
            $FEATURE_ID
        ),
        "STATUS: IN_SYNC"
    )

    foreach ($RequiredLine in $RequiredEvidenceLines) {
        if ($EvidenceLines -notcontains $RequiredLine) {
            throw (
                "Durable validation evidence is missing: " +
                $RequiredLine
            )
        }
    }

    $ArchitectureLines = @(
        $EvidenceLines |
            Where-Object {
                $_ -like "ARCHITECTURE SUMMARY:*"
            }
    )

    if ($ArchitectureLines.Count -ne 1) {
        throw (
            "Durable evidence must contain exactly one " +
            "ARCHITECTURE SUMMARY line."
        )
    }

    if ($ArchitectureLines[0] -notmatch "Errors:\s*0") {
        throw (
            "Architecture evidence does not report zero errors: " +
            $ArchitectureLines[0]
        )
    }

    Write-Host ""
    Write-Host "WAVE8B DURABLE EVIDENCE FILE: PASS"
    Write-Host ("EVIDENCE FILE: " + $EVIDENCE_FILE)
    Write-Host $ArchitectureLines[0]
    Write-Host "WAVE8B WINDOWS VALIDATION: PASS"
    Write-Host ("VALIDATED ZIP: " + $STAGED_ZIP)
    Write-Host ("VALIDATION PACKAGE: " + $EXTRACT_ROOT)
}
catch {
    $ValidationFailed = $true

    Write-Host ""
    Write-Host "WAVE8B WINDOWS VALIDATION ERROR"
    Write-Host ("PHASE: " + $Phase)
    Write-Host (
        "ERROR TYPE: " +
        $_.Exception.GetType().FullName
    )
    Write-Host (
        "ERROR MESSAGE: " +
        $_.Exception.Message
    )

    if ($_.InvocationInfo.PositionMessage) {
        Write-Host (
            "INVOCATION: " +
            $_.InvocationInfo.PositionMessage
        )
    }

    Write-Host ""
    Write-Host "Freeze remains blocked."
}

$env:PYTHONPATH = $PreviousPythonPath

Write-Host ""
Read-Host "Press Enter to clear terminal"
Read-Host "Press Enter again to clear"
Clear-Host

if ($ValidationFailed) {
    return
}

Required final markers include:

ZIP CONTRACT: PASS
INSTALL SCRIPT CONTRACT: PASS
WAVE8B_SOURCE_PAYLOAD_BYTE_IDENTITY: PASS
WAVE8B_CHANGED_FILE_COVERAGE: PASS
WAVE8B_NO_GOVERNED_DELETIONS: PASS
WAVE8B_PY_COMPILE: PASS
WAVE8B_STARTUP_STATUS_IN_SYNC: PASS
WAVE8B_PYTHON_ARCHITECTURE_DESIGN_BOUNDARIES_REGRESSION_SET: PASS
WAVE8A_HUMAN_HANDBOOK_RECLASSIFICATION_REGRESSION_SET: PASS
WAVE6C_WEB_AI_EXCHANGE_BOUNDARIES_REGRESSION_SET: PASS
WAVE6B_CLASS06_CONSOLIDATION_RETIREMENTS_REGRESSION_SET: PASS
WAVE6A_CORE_ARCHITECTURE_REFACTOR_OWNERS_REGRESSION_SET: PASS
Q36_MODULE_SIZE_COHESION_REGRESSION_SET: PASS
CODE_MODULE_QUALITY_CANON: PASS
WAVE8B_CLASS08_CANONICAL_IDENTITIES: PASS
WAVE8B_CLEAN_ARCHITECTURE_BOUNDARY: PASS
WAVE8B_CLEAN_CODE_LOCAL_SCOPE: PASS
WAVE8B_PATTERN_SELECTION_BOUNDARY: PASS
WAVE8B_DDD_MODELING_BOUNDARY: PASS
WAVE8B_ROUTING_COMPLETENESS: PASS
WAVE8B_NO_FORCED_COMPANIONS: PASS
WAVE8B_NO_STARTUP_PROMOTION: PASS
WAVE8B_ARCHITECTURE_ERRORS_ZERO: PASS
WAVE8B_DURABLE_VALIDATION_EVIDENCE: PASS
VALIDATION OK: prompt-audit-wave8b-python-architecture-design-boundaries-v1
STATUS: IN_SYNC
WAVE8B DURABLE EVIDENCE FILE: PASS
WAVE8B WINDOWS VALIDATION: PASS

The Q36 and historical large-module compatibility segment may take a while. Do not interrupt it while output continues.

Pasted text(154).txt
Document

Wave 8B validation is fully clean: every inherited regression passed, durable evidence exists, architecture reports zero errors, and the package remains IN_SYNC. I’m reading the exact Wave 8B freeze sidecar now so the human-confirmed write uses the package’s own title, box, protected paths, and rules.

Wave 8B passed the complete validation chain. All focused and inherited regressions passed, durable evidence exists, architecture reports Errors: 0, and the package remains STATUS: IN_SYNC.

Press Enter twice to leave the validation prompt, then run this from a clean PS E:\kanda_reasoner> prompt.

$ErrorActionPreference = "Stop"

$PROJECT_ROOT = "E:\kanda_reasoner"

$PATCH_NAME = `
    "kanda_prompt_audit_wave8b_python_architecture_design_boundaries_v1"

$FEATURE_ID = `
    "prompt-audit-wave8b-python-architecture-design-boundaries-v1"

$EXPECTED_ZIP_SHA256 = `
    "96876610e2d915af366ce95c02c6117cc586c7c33d551a11ae38e38cd1dacb3b"

$Phase = "initialize"
$FreezeFailed = $false
$FreezeAborted = $false
$FreezeMayAlreadyExist = $false
$PreviousPythonPath = $env:PYTHONPATH

try {
    $Phase = "resolve_paths"

    $ProjectItem = Get-Item `
        -LiteralPath $PROJECT_ROOT `
        -ErrorAction Stop

    $PROJECT_ROOT = $ProjectItem.FullName

    $DRIVE_ROOT = [System.IO.Path]::GetPathRoot(
        $PROJECT_ROOT
    )

    if ([string]::IsNullOrWhiteSpace($DRIVE_ROOT)) {
        throw "The project drive root could not be resolved."
    }

    $WORK_DIR = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_delete_after_daily_work")

    $STAGED_ZIP = Join-Path `
        $WORK_DIR `
        ($PATCH_NAME + ".zip")

    $SUPPORT_ROOT = Join-Path `
        $DRIVE_ROOT `
        ($ProjectItem.Name + "_show_project_to_AI")

    $EVIDENCE_FILE = Join-Path `
        $SUPPORT_ROOT `
        "project_validation_evidence\$FEATURE_ID.txt"

    $HELPER = Join-Path `
        $WORK_DIR `
        "human_confirmed_wave8b_freeze.py"

    $STATE_FILE = Join-Path `
        $WORK_DIR `
        "wave8b_reviewed_freeze_preview.json"

    foreach ($RequiredFile in @(
        $STAGED_ZIP,
        $EVIDENCE_FILE
    )) {
        if (-not (
            Test-Path `
                -LiteralPath $RequiredFile `
                -PathType Leaf
        )) {
            throw "Required file was not found: $RequiredFile"
        }
    }

    $StagedHash = (
        Get-FileHash `
            -LiteralPath $STAGED_ZIP `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ("STAGED ZIP SHA256: " + $StagedHash)

    if ($StagedHash -ne $EXPECTED_ZIP_SHA256) {
        throw (
            "Staged Wave 8B ZIP hash mismatch. Expected: " +
            $EXPECTED_ZIP_SHA256 +
            " Actual: " +
            $StagedHash
        )
    }

    Write-Host "WAVE8B FREEZE ZIP IDENTITY: PASS"

    if (Test-Path -LiteralPath $STATE_FILE) {
        Remove-Item `
            -LiteralPath $STATE_FILE `
            -Force
    }

    $Phase = "write_freeze_helper"

    $PythonSource = @'
"""Prepare and write one human-confirmed Wave 8B freeze entry."""

from __future__ import annotations

import argparse
import hashlib
import json
import sys
import zipfile
from pathlib import Path
from typing import Any, Mapping


FEATURE_ID = (
    "prompt-audit-wave8b-python-architecture-design-boundaries-v1"
)

FEATURE_TITLE = (
    "Prompt Audit Wave 8B - "
    "Python Architecture and Design Boundaries"
)

PRIMARY_BOX = "08_python_engineering_core"

EXPECTED_ZIP_SHA256 = (
    "96876610e2d915af366ce95c02c6117cc586c7c33d551a11ae38e38cd1dacb3b"
)

REQUIRED_EVIDENCE_MARKERS = (
    "WAVE8B_CHANGED_FILE_COVERAGE: PASS",
    "WAVE8B_NO_GOVERNED_DELETIONS: PASS",
    "WAVE8B_CLASS08_CANONICAL_IDENTITIES: PASS",
    "WAVE8B_CLEAN_ARCHITECTURE_BOUNDARY: PASS",
    "WAVE8B_CLEAN_CODE_LOCAL_SCOPE: PASS",
    "WAVE8B_PATTERN_SELECTION_BOUNDARY: PASS",
    "WAVE8B_DDD_MODELING_BOUNDARY: PASS",
    "WAVE8B_ROUTING_COMPLETENESS: PASS",
    "WAVE8B_NO_FORCED_COMPANIONS: PASS",
    "WAVE8B_NO_STARTUP_PROMOTION: PASS",
    "WAVE8B_SOURCE_PAYLOAD_BYTE_IDENTITY: PASS",
    "WAVE8B_ARCHITECTURE_ERRORS_ZERO: PASS",
    "PATCH ZIP SHA256: " + EXPECTED_ZIP_SHA256,
    "VALIDATION OK: " + FEATURE_ID,
    "STATUS: IN_SYNC",
)


def print_items(title: str, values: Any) -> None:
    items = list(values or [])

    if not items:
        return

    print(title)

    for item in items:
        print("- " + str(item))


def require_ok(
    result: Mapping[str, Any],
    operation: str,
) -> None:
    if result.get("ok"):
        return

    print_items("ERRORS:", result.get("errors"))
    print_items("WARNINGS:", result.get("warnings"))

    raise RuntimeError(operation + " failed.")


def sha256_file(path: Path) -> str:
    digest = hashlib.sha256()

    with path.open("rb") as handle:
        while True:
            chunk = handle.read(1024 * 1024)

            if not chunk:
                break

            digest.update(chunk)

    return digest.hexdigest()


def support_root_for(project_root: Path) -> Path:
    return (
        Path(project_root.anchor)
        / (project_root.name + "_show_project_to_AI")
    )


def matching_frozen_entries(
    project_root: Path,
) -> list[Path]:
    entries_root = (
        support_root_for(project_root)
        / "project_freeze_after_update"
        / "frozen_features_memory"
        / "entries"
    )

    if not entries_root.is_dir():
        return []

    return sorted(
        path
        for path in entries_root.glob("*.md")
        if "wave-8b" in path.name.lower()
        or "wave8b" in path.name.lower()
    )


def import_contracts(
    project_root: Path,
) -> dict[str, Any]:
    project_text = str(project_root)

    if project_text not in sys.path:
        sys.path.insert(0, project_text)

    from kanda_reasoner_app.freeze_after_update.contract import (
        preview_freeze_entry,
        refresh_ai_compliance_context,
        validate_freeze_entry_preview,
        write_confirmed_freeze_entry,
    )

    from kanda_reasoner_app.freeze_hint_intake.contract import (
        mark_latest_freeze_hint_used,
        merge_validation_evidence_into_latest_hint,
        resolve_freeze_hint_autofill_state,
    )

    return {
        "preview_freeze_entry": preview_freeze_entry,
        "refresh_ai_compliance_context": (
            refresh_ai_compliance_context
        ),
        "validate_freeze_entry_preview": (
            validate_freeze_entry_preview
        ),
        "write_confirmed_freeze_entry": (
            write_confirmed_freeze_entry
        ),
        "mark_latest_freeze_hint_used": (
            mark_latest_freeze_hint_used
        ),
        "merge_validation_evidence_into_latest_hint": (
            merge_validation_evidence_into_latest_hint
        ),
        "resolve_freeze_hint_autofill_state": (
            resolve_freeze_hint_autofill_state
        ),
    }


def load_exact_hint(
    patch_zip: Path,
) -> dict[str, Any]:
    if sha256_file(patch_zip) != EXPECTED_ZIP_SHA256:
        raise RuntimeError(
            "The Wave 8B ZIP changed before Preview."
        )

    with zipfile.ZipFile(patch_zip, "r") as archive:
        names = archive.namelist()

        if names.count("KANDA_FREEZE_HINT.json") != 1:
            raise RuntimeError(
                "The ZIP must contain exactly one "
                "KANDA_FREEZE_HINT.json."
            )

        hint = json.loads(
            archive.read(
                "KANDA_FREEZE_HINT.json"
            ).decode("utf-8-sig")
        )

    if str(hint.get("feature_id") or "") != FEATURE_ID:
        raise RuntimeError(
            "Freeze sidecar feature ID does not match Wave 8B."
        )

    if str(hint.get("feature_title") or "") != FEATURE_TITLE:
        raise RuntimeError(
            "Freeze sidecar title does not match Wave 8B."
        )

    if str(hint.get("primary_box") or "") != PRIMARY_BOX:
        raise RuntimeError(
            "Freeze sidecar primary box does not match Wave 8B."
        )

    return hint


def build_validation_summary(
    evidence_file: Path,
) -> str:
    evidence_text = evidence_file.read_text(
        encoding="utf-8-sig",
        errors="strict",
    )

    for marker in REQUIRED_EVIDENCE_MARKERS:
        if marker not in evidence_text:
            raise RuntimeError(
                "Durable evidence is missing required marker: "
                + marker
            )

    architecture_lines = [
        line.strip()
        for line in evidence_text.splitlines()
        if line.startswith("ARCHITECTURE SUMMARY:")
    ]

    if len(architecture_lines) != 1:
        raise RuntimeError(
            "Durable evidence must contain exactly one "
            "ARCHITECTURE SUMMARY line."
        )

    if "Errors: 0" not in architecture_lines[0]:
        raise RuntimeError(
            "Architecture evidence does not report zero errors."
        )

    supplemental = (
        "WAVE8B WINDOWS INSTALL: PASS",
        "WAVE8B WINDOWS VALIDATION: PASS",
        "WAVE8B DURABLE EVIDENCE FILE: PASS",
    )

    return (
        evidence_text.rstrip()
        + "\n"
        + "\n".join(supplemental)
        + "\n"
    )


def as_lines(values: Any) -> str:
    return "\n".join(
        str(value).strip()
        for value in list(values or [])
        if str(value).strip()
    )


def build_explicit_inputs(
    hint: Mapping[str, Any],
    validation_summary: str,
) -> dict[str, str]:
    protected_paths = list(
        hint.get("protected_paths") or []
    )

    memory_path = (
        "<project>_show_project_to_AI/"
        "project_freeze_after_update/"
        "frozen_features_memory"
    )

    if memory_path not in protected_paths:
        protected_paths.append(memory_path)

    rules = list(
        hint.get("do_not_regress_rules") or []
    )

    governance_rules = (
        (
            "Project-specific frozen memory must remain under "
            "<project>_show_project_to_AI/"
            "project_freeze_after_update/"
            "frozen_features_memory."
        ),
        (
            "Do not store project-specific frozen memory "
            "inside project_freeze_ledger."
        ),
        (
            "Preview Freeze Entry must remain read-only and "
            "must not write files."
        ),
        (
            "Confirm and Write must require explicit human "
            "confirmation before writing governed freeze memory."
        ),
        (
            "After a local freeze write, refresh the AI startup "
            "freeze context."
        ),
        (
            "External AI review remains advanced or fallback, "
            "not the normal local freeze path."
        ),
    )

    for rule in governance_rules:
        if rule not in rules:
            rules.append(rule)

    inputs = {
        "feature_title": FEATURE_TITLE,
        "primary_box": PRIMARY_BOX,
        "box_type": str(
            hint.get("box_type")
            or "one_primary_box_with_bounded_supporting_touches"
        ),
        "validated_files": as_lines(
            hint.get("validated_files")
        ),
        "generated_files": as_lines(
            hint.get("generated_files")
        ),
        "protected_paths": as_lines(
            protected_paths
        ),
        "do_not_regress_rules": as_lines(
            rules
        ),
        "validation_evidence_summary": validation_summary,
        "known_warnings": (
            "Windows-local architecture validation reports "
            "three warnings and zero errors."
        ),
        "planned_next_step": (
            "Write this reviewed Wave 8B entry only after "
            "explicit human confirmation, then refresh the "
            "AI startup freeze context."
        ),
        "notes": (
            str(hint.get("notes") or "").strip()
            + " Source patch ZIP: "
            + str(hint.get("source_patch_zip") or "").strip()
            + ". The validated form is supplied as an explicit "
            "preview-session snapshot so stale pre-validation "
            "intake text cannot replace current evidence."
        ),
    }

    if not inputs["validated_files"].strip():
        raise RuntimeError(
            "Explicit validated_files is empty."
        )

    if not inputs[
        "validation_evidence_summary"
    ].strip():
        raise RuntimeError(
            "Explicit validation evidence is empty."
        )

    return inputs


def write_state(
    path: Path,
    state: Mapping[str, Any],
) -> None:
    text = json.dumps(
        state,
        ensure_ascii=True,
        indent=2,
        sort_keys=True,
        default=str,
    )

    path.write_text(
        text + "\n",
        encoding="utf-8",
        newline="\n",
    )


def prepare(
    project_root: Path,
    patch_zip: Path,
    evidence_file: Path,
    state_file: Path,
) -> int:
    existing = matching_frozen_entries(project_root)

    if existing:
        print("FREEZE PREPARATION BLOCKED")
        print("A Wave 8B frozen entry already exists:")

        for path in existing:
            print("- " + str(path))

        print("Do not create a duplicate freeze entry.")
        return 2

    hint = load_exact_hint(patch_zip)

    validation_summary = build_validation_summary(
        evidence_file
    )

    explicit_inputs = build_explicit_inputs(
        hint,
        validation_summary,
    )

    contracts = import_contracts(project_root)

    merge_result = contracts[
        "merge_validation_evidence_into_latest_hint"
    ](
        project_root,
        validation_summary,
        feature_id=FEATURE_ID,
        feature_title=FEATURE_TITLE,
    )

    if merge_result.get("ok"):
        print("WAVE8B INTAKE EVIDENCE REPAIR: PASS")

    if not merge_result.get("ok"):
        print("WAVE8B INTAKE EVIDENCE REPAIR: WARNING")

        print_items(
            "ERRORS:",
            merge_result.get("errors"),
        )

        print_items(
            "WARNINGS:",
            merge_result.get("warnings"),
        )

        print(
            "The explicit Preview snapshot remains "
            "authoritative."
        )

    autofill = contracts[
        "resolve_freeze_hint_autofill_state"
    ](
        project_root,
        {},
        preview_form_inputs=explicit_inputs,
    )

    require_ok(
        autofill,
        "resolve_freeze_hint_autofill_state",
    )

    if (
        str(autofill.get("source_kind") or "")
        != "preview_session_snapshot"
    ):
        raise RuntimeError(
            "Wave 8B did not resolve from the explicit "
            "Preview snapshot."
        )

    if autofill.get("confirm_write_enabled") is not True:
        raise RuntimeError(
            "Wave 8B Confirm and Write is not enabled."
        )

    form_inputs = dict(
        autofill.get("form_inputs") or {}
    )

    if str(
        form_inputs.get("feature_title") or ""
    ).strip() != FEATURE_TITLE:
        raise RuntimeError(
            "Resolved feature title is not Wave 8B."
        )

    if str(
        form_inputs.get("primary_box") or ""
    ).strip() != PRIMARY_BOX:
        raise RuntimeError(
            "Resolved primary box is not Class 08."
        )

    preview = contracts[
        "preview_freeze_entry"
    ](
        project_root,
        form_inputs,
    )

    require_ok(
        preview,
        "preview_freeze_entry",
    )

    if preview.get("is_writable") is not True:
        raise RuntimeError(
            "The Wave 8B Preview is not writable."
        )

    validation = contracts[
        "validate_freeze_entry_preview"
    ](
        project_root,
        preview,
    )

    require_ok(
        validation,
        "validate_freeze_entry_preview",
    )

    markdown = str(
        preview.get("markdown") or ""
    )

    required_preview_markers = (
        FEATURE_TITLE,
        PRIMARY_BOX,
        "WAVE8B_CLASS08_CANONICAL_IDENTITIES: PASS",
        "WAVE8B_CLEAN_ARCHITECTURE_BOUNDARY: PASS",
        "WAVE8B_CLEAN_CODE_LOCAL_SCOPE: PASS",
        "WAVE8B_PATTERN_SELECTION_BOUNDARY: PASS",
        "WAVE8B_DDD_MODELING_BOUNDARY: PASS",
        "VALIDATION OK: " + FEATURE_ID,
        "STATUS: IN_SYNC",
    )

    for marker in required_preview_markers:
        if marker not in markdown:
            raise RuntimeError(
                "Wave 8B Preview is missing marker: "
                + marker
            )

    stale_markers = (
        (
            "Current validated feature - replace with "
            "exact feature title"
        ),
        (
            "Replace with the primary box for the "
            "current feature"
        ),
        (
            "Windows-local installation and packaged "
            "validation remain required"
        ),
    )

    for marker in stale_markers:
        if marker in markdown:
            raise RuntimeError(
                "Stale or starter text entered the "
                "Wave 8B Preview: "
                + marker
            )

    state = {
        "schema_version": "1.0",
        "feature_id": FEATURE_ID,
        "feature_title": FEATURE_TITLE,
        "primary_box": PRIMARY_BOX,
        "project_root": str(project_root),
        "patch_zip_sha256": EXPECTED_ZIP_SHA256,
        "preview": preview,
    }

    write_state(
        state_file,
        state,
    )

    state_hash = hashlib.sha256(
        state_file.read_bytes()
    ).hexdigest()

    print("")
    print("WAVE8B FREEZE PREVIEW")
    print("=====================")
    print(markdown)
    print("")
    print("PREVIEW SOURCE: EXPLICIT SESSION SNAPSHOT")
    print("EXPLICIT VALIDATED FILES: PASS")
    print("EXPLICIT VALIDATION EVIDENCE: PASS")
    print("STALE PENDING TEXT ABSENT: PASS")
    print("STARTER FALLBACK ABSENT: PASS")
    print("PREVIEW VALIDATION: PASS")
    print("PREVIEW IS WRITABLE: PASS")
    print("FEATURE ID: " + FEATURE_ID)
    print("PRIMARY BOX: " + PRIMARY_BOX)
    print("PREVIEW STATE: " + str(state_file))
    print("PREVIEW STATE SHA256: " + state_hash)
    print("")
    print("NO FREEZE ENTRY HAS BEEN WRITTEN.")

    return 0


def write(
    project_root: Path,
    state_file: Path,
) -> int:
    existing = matching_frozen_entries(project_root)

    if existing:
        print("FREEZE WRITE BLOCKED")
        print("A Wave 8B frozen entry already exists:")

        for path in existing:
            print("- " + str(path))

        print("Do not retry or create a duplicate entry.")
        return 3

    state = json.loads(
        state_file.read_text(
            encoding="utf-8-sig",
        )
    )

    if str(state.get("schema_version") or "") != "1.0":
        raise RuntimeError(
            "Preview state schema is invalid."
        )

    if str(state.get("feature_id") or "") != FEATURE_ID:
        raise RuntimeError(
            "Preview state feature ID is invalid."
        )

    if str(state.get("primary_box") or "") != PRIMARY_BOX:
        raise RuntimeError(
            "Preview state primary box is invalid."
        )

    if (
        str(state.get("patch_zip_sha256") or "")
        != EXPECTED_ZIP_SHA256
    ):
        raise RuntimeError(
            "Preview state ZIP identity is invalid."
        )

    recorded_root = Path(
        str(state.get("project_root") or "")
    ).resolve()

    if recorded_root != project_root:
        raise RuntimeError(
            "Preview state project root changed."
        )

    preview = dict(
        state.get("preview") or {}
    )

    contracts = import_contracts(project_root)

    validation = contracts[
        "validate_freeze_entry_preview"
    ](
        project_root,
        preview,
    )

    require_ok(
        validation,
        "validate_freeze_entry_preview",
    )

    result = contracts[
        "write_confirmed_freeze_entry"
    ](
        project_root,
        preview,
        confirmation=True,
    )

    require_ok(
        result,
        "write_confirmed_freeze_entry",
    )

    freeze_id = str(
        result.get("freeze_id") or ""
    ).strip()

    print("")
    print("LOCAL FREEZE WRITE OK")
    print("FREEZE ID: " + freeze_id)

    print_items(
        "WRITTEN PATHS:",
        result.get("written_paths"),
    )

    intake_result = contracts[
        "mark_latest_freeze_hint_used"
    ](
        project_root,
        freeze_id=freeze_id,
    )

    if intake_result.get("ok"):
        print("FREEZE HINT MARKED USED: PASS")

    if not intake_result.get("ok"):
        print("FREEZE HINT MARKED USED: WARNING")

        print_items(
            "ERRORS:",
            intake_result.get("errors"),
        )

        print_items(
            "WARNINGS:",
            intake_result.get("warnings"),
        )

    compliance = contracts[
        "refresh_ai_compliance_context"
    ](
        project_root
    )

    if compliance.get("ok"):
        print("AI COMPLIANCE REFRESH: PASS")

    if compliance.get("startup_context_refreshed"):
        print(
            "STARTUP FREEZE CONTEXT REFRESH: PASS"
        )

    if compliance.get("startup_zip"):
        print(
            "STARTUP ZIP: "
            + str(compliance.get("startup_zip"))
        )

    if compliance.get(
        "read_before_all_instruction"
    ):
        print(
            "READ-BEFORE-ALL FILE: "
            + str(
                compliance.get(
                    "read_before_all_instruction"
                )
            )
        )

    if not compliance.get("ok"):
        print("AI COMPLIANCE REFRESH: WARNING")

        print_items(
            "ERRORS:",
            compliance.get("errors"),
        )

        print_items(
            "WARNINGS:",
            compliance.get("warnings"),
        )

        print(
            "The Wave 8B freeze entry was written. "
            "Do not rerun the freeze write."
        )

        return 4

    if not intake_result.get("ok"):
        print(
            "The Wave 8B freeze entry was written, but "
            "intake cleanup needs review. Do not rerun it."
        )

        return 4

    print("STATUS: FROZEN")
    return 0


def main() -> int:
    parser = argparse.ArgumentParser()

    parser.add_argument(
        "--mode",
        required=True,
        choices=("prepare", "write"),
    )

    parser.add_argument(
        "--project-root",
        required=True,
    )

    parser.add_argument(
        "--patch-zip",
        default="",
    )

    parser.add_argument(
        "--evidence-file",
        default="",
    )

    parser.add_argument(
        "--state-file",
        required=True,
    )

    args = parser.parse_args()

    project_root = Path(
        args.project_root
    ).expanduser().resolve()

    state_file = Path(
        args.state_file
    ).expanduser().resolve()

    if args.mode == "prepare":
        return prepare(
            project_root,
            Path(args.patch_zip).expanduser().resolve(),
            Path(args.evidence_file).expanduser().resolve(),
            state_file,
        )

    return write(
        project_root,
        state_file,
    )


if __name__ == "__main__":
    raise SystemExit(main())
'@

    $Utf8NoBom = New-Object `
        System.Text.UTF8Encoding($false)

    [System.IO.File]::WriteAllText(
        $HELPER,
        $PythonSource,
        $Utf8NoBom
    )

    $env:PYTHONPATH = $PROJECT_ROOT

    $Phase = "compile_freeze_helper"

    & python `
        -m py_compile `
        $HELPER

    if ($LASTEXITCODE -ne 0) {
        throw (
            "Wave 8B freeze helper compilation returned " +
            "exit code " +
            $LASTEXITCODE
        )
    }

    Write-Host "WAVE8B FREEZE HELPER COMPILE: PASS"
    Write-Host ""

    $Phase = "prepare_read_only_preview"

    & python `
        $HELPER `
        --mode prepare `
        --project-root $PROJECT_ROOT `
        --patch-zip $STAGED_ZIP `
        --evidence-file $EVIDENCE_FILE `
        --state-file $STATE_FILE

    $PrepareExitCode = $LASTEXITCODE

    if ($PrepareExitCode -ne 0) {
        throw (
            "Wave 8B Preview returned exit code " +
            $PrepareExitCode
        )
    }

    $StateHashBefore = (
        Get-FileHash `
            -LiteralPath $STATE_FILE `
            -Algorithm SHA256
    ).Hash.ToLowerInvariant()

    Write-Host ""
    Write-Host "Review the Wave 8B Preview above carefully."
    Write-Host "No freeze entry has been written yet."
    Write-Host ""

    $Confirmation = Read-Host `
        "Type exactly CONFIRM WAVE8B FREEZE to write it"

    if ($Confirmation -cne "CONFIRM WAVE8B FREEZE") {
        $FreezeAborted = $true

        Write-Host ""
        Write-Host "FREEZE ABORTED"
        Write-Host "No freeze entry was written."
    }

    if (-not $FreezeAborted) {
        $Phase = "verify_reviewed_preview_identity"

        $StateHashAfter = (
            Get-FileHash `
                -LiteralPath $STATE_FILE `
                -Algorithm SHA256
        ).Hash.ToLowerInvariant()

        if ($StateHashAfter -ne $StateHashBefore) {
            throw (
                "The reviewed Preview state changed. " +
                "Freeze write is blocked."
            )
        }

        $Phase = "human_confirmed_write_and_refresh"

        & python `
            $HELPER `
            --mode write `
            --project-root $PROJECT_ROOT `
            --state-file $STATE_FILE

        $WriteExitCode = $LASTEXITCODE

        if ($WriteExitCode -eq 4) {
            $FreezeMayAlreadyExist = $true

            throw (
                "The Wave 8B freeze entry was written, but " +
                "post-write cleanup or refresh was incomplete. " +
                "Do not rerun the freeze write."
            )
        }

        if ($WriteExitCode -ne 0) {
            throw (
                "Confirmed Wave 8B freeze returned exit code " +
                $WriteExitCode
            )
        }

        Remove-Item `
            -LiteralPath $STATE_FILE `
            -Force

        Write-Host ""
        Write-Host "WAVE8B HUMAN-CONFIRMED FREEZE: PASS"
        Write-Host "STARTUP FREEZE CONTEXT: REFRESHED"
    }
}
catch {
    $FreezeFailed = $true

    Write-Host ""
    Write-Host "WAVE8B FREEZE ERROR"
    Write-Host ("PHASE: " + $Phase)
    Write-Host (
        "ERROR TYPE: " +
        $_.Exception.GetType().FullName
    )
    Write-Host (
        "ERROR MESSAGE: " +
        $_.Exception.Message
    )

    if ($_.InvocationInfo.PositionMessage) {
        Write-Host (
            "INVOCATION: " +
            $_.InvocationInfo.PositionMessage
        )
    }

    if ($FreezeMayAlreadyExist) {
        Write-Host ""
        Write-Host (
            "The Wave 8B freeze entry may already exist. " +
            "Do not rerun the write phase."
        )
    }
}

$env:PYTHONPATH = $PreviousPythonPath

Write-Host ""
Read-Host "Press Enter to clear terminal"
Read-Host "Press Enter again to clear"
Clear-Host

Before confirming, verify that the Preview contains:

PREVIEW SOURCE: EXPLICIT SESSION SNAPSHOT
EXPLICIT VALIDATED FILES: PASS
EXPLICIT VALIDATION EVIDENCE: PASS
STALE PENDING TEXT ABSENT: PASS
STARTER FALLBACK ABSENT: PASS
PREVIEW VALIDATION: PASS
PREVIEW IS WRITABLE: PASS
FEATURE ID: prompt-audit-wave8b-python-architecture-design-boundaries-v1
PRIMARY BOX: 08_python_engineering_core
WAVE8B_CLASS08_CANONICAL_IDENTITIES: PASS
WAVE8B_CLEAN_ARCHITECTURE_BOUNDARY: PASS
WAVE8B_CLEAN_CODE_LOCAL_SCOPE: PASS
WAVE8B_PATTERN_SELECTION_BOUNDARY: PASS
WAVE8B_DDD_MODELING_BOUNDARY: PASS
VALIDATION OK: prompt-audit-wave8b-python-architecture-design-boundaries-v1
STATUS: IN_SYNC

Then type this exact phrase inside PowerShell:

CONFIRM WAVE8B FREEZE

Successful completion must contain:

LOCAL FREEZE WRITE OK
FREEZE HINT MARKED USED: PASS
AI COMPLIANCE REFRESH: PASS
STARTUP FREEZE CONTEXT REFRESH: PASS
STATUS: FROZEN
WAVE8B HUMAN-CONFIRMED FREEZE: PASS
STARTUP FREEZE CONTEXT: REFRESHED

Do not rerun the write if LOCAL FREEZE WRITE OK appears, even if a later refresh step reports a warning.

Pasted text(155).txt
Document

The marker lines were accidentally pasted into PowerShell as commands, so PowerShell reported harmless CommandNotFoundException messages. Those lines were only meant to be visually checked, not executed.

The actual Wave 8B freeze flow then ran correctly and completed successfully.

Confirmed:

LOCAL FREEZE WRITE OK
FREEZE HINT MARKED USED: PASS
AI COMPLIANCE REFRESH: PASS
STARTUP FREEZE CONTEXT REFRESH: PASS
STATUS: FROZEN
WAVE8B HUMAN-CONFIRMED FREEZE: PASS
STARTUP FREEZE CONTEXT: REFRESHED

Frozen entry:

E:\kanda_reasoner_show_project_to_AI\project_freeze_after_update\frozen_features_memory\entries\freeze-20260717-prompt-audit-wave-8b-python-architecture-and-design-boundaries.md

The freeze index, implemented-steps record, startup ZIP, and read-before-all file were refreshed.

Completed: 77 of 119 prompts
Remaining: 42 prompts

Wave 8B is fully complete. Do not rerun its freeze command.
