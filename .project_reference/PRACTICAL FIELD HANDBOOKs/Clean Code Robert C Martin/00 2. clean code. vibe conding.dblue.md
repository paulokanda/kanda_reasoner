Clean Code Handbook for EEG Python
Clean Code Book Overview
Clean Code Handbook for EEG Python

We need to produce a practical field handbook for "Clean Code" by Robert C. Martin, tailored to Python, System Design, ML, DevOps, Architecture, but with user's specific context: beginner, writes Python backend APIs solo, neurophysiologist specializing in EEG, hobbyist programmer turning into prompt programmer. Wants to create perfect architected EEG apps for Quantitative EEG, and also create YouTube classes for neurophysiologists with no Python/programming knowledge. So the handbook content should be adapted to that audience, using examples from EEG data processing, quantitative EEG, etc. Must follow structure exactly: 10 sections. Domain: Python (primary), with mentions of system design, ML, DevOps, Architecture as relevant. Current year: assume 2026? But we can just say "current best practices" without specifying year, or use 2025. Let's be generic but modern.

Output rules: practical, concrete, opinionated. No foo/bar. Use realistic naming like eeg_processing, calculate_alpha_power, etc.

We need to produce full handbook. But output window may be long; we should aim to complete in one response. If too long, we can pause, but likely manageable within 8000-10000 tokens. Let's try to write concisely but thorough.

I will now write the handbook.
Clean Code — Practical Field Handbook for EEG & Python Developers
1. ONE-PARAGRAPH ESSENCE

Core thesis: Code is read far more often than it’s written, so clarity, honesty, and minimal surprise matter more than cleverness or speed. For you, building EEG apps for quantitative analysis (alpha power, connectivity, artifact rejection), this means every function name, variable, and module boundary should tell a neurophysiologist (including future you) exactly what it does with the signal data — no hidden state, no magic numbers for filter parameters, no eight‑step init ritual. Why it matters today: With AI coding assistants (like the one you use), it’s easier than ever to generate messy, redundant code. Clean Code principles become the discipline that turns generated snippets into maintainable, scientifically reproducible EEG pipelines. The book’s advice on naming, small functions, and dependency management directly prevents the “spaghetti‑of‑notebooks” disaster common in research code.
2. MENTAL MODEL MAP
text

                     ┌─────────────────┐
                     │  Meaningful Names │
                     │  (EEG channels,   │
                     │   functions)      │
                     └────────┬─────────┘
                              │
                              ▼
┌─────────────────┐     ┌─────────────┐     ┌─────────────────┐
│   Small Functions │◄────┤  One Level  │────►│  No Side Effects │
│ (one transform   │     │ of Abstraction│     │ (pure processing │
│  per function)   │     └─────────────┘     │  of epochs)      │
└────────┬────────┘                          └────────┬────────┘
         │                                            │
         ▼                                            ▼
┌─────────────────┐                          ┌─────────────────┐
│  Error Handling  │                          │   Objects vs    │
│ (exceptions, not │                          │  Data Structures│
│  return codes)   │                          │ (EEGContainer   │
└────────┬────────┘                          │  vs raw arrays) │
         │                                    └────────┬────────┘
         ▼                                             │
┌─────────────────┐     ┌─────────────┐     ┌───────────▼─────────┐
│   Boundaries    │────►│   Unit Tests │◄────│   Dependency Rule  │
│ (adapters for   │     │ (given/when/ │     │ (EEG readers,      │
│  MNE, NumPy)    │     │  then)       │     │  filters injected) │
└─────────────────┘     └─────────────┘     └─────────────────────┘

How they connect:
Meaningful names enable small functions (you can’t name what does five things). Small functions naturally follow one level of abstraction (no mixing of “load file” with “filter 0.5‑40Hz”). Together with no side effects you get testable units. Error handling becomes separate, not scattered. Boundaries isolate external libraries (MNE, SciPy) so you can swap or mock them. Unit tests drive all of the above. Finally, dependency rule (high‑level EEG processing should not depend on low‑level file I/O details) keeps the architecture clean.
3. CHAPTER-BY-CHAPTER FIELD NOTES

    Note: Chapters refer to Clean Code (original). I map each to EEG‑Python context.

Chapter 2: Meaningful Names

    Core idea: A name should reveal intent, avoid disinformation, and be pronounceable/searchable.

    Single most actionable rule: Use epoch_start_ms not t; alpha_power_uV2 not ap.

    Example (EEG):
    python

    # Instead of:
    def proc(d, f):
        return [x * f for x in d]

    # Do:
    def apply_bandpass_filter(eeg_uv: np.ndarray, lowcut_hz: float, highcut_hz: float) -> np.ndarray:
        """Filter each channel in place (but better: return new array)."""
        ...

    Anti‑pattern: Hungarian notation (e.g., flt_alpha for a float, lst_channels for a list) — Python types + readable names are enough.

    Difficulty: Easy — but requires discipline to rename during refactoring.

Chapter 3: Functions

    Core idea: Functions should be small (≤20 lines), do one thing, and have no side effects.

    Actionable rule: Extract every “and” in the description into its own function.

    Example:
    python

    # Bad – does three things
    def preprocess_eeg(raw):
        raw.filter(1, 40)
        raw.notch_filter(50)
        raw.set_montage('standard_1020')
        return raw

    # Good – each step is its own function, composed
    def preprocess_eeg(raw):
        return (apply_bandpass(raw, 1, 40)
                .pipe(apply_notch, 50)
                .pipe(set_montage, 'standard_1020'))

    Anti‑pattern: Functions that modify global configuration (e.g., plt.rcParams) or input arguments without returning a clear copy.

    Difficulty: Medium — requires resisting the urge to inline short steps.

Chapter 4: Comments

    Core idea: Comments are failures of expression; use them only for legal, warning, or TODO notes.

    Actionable rule: If you need a comment, try to rewrite the code to make the comment obsolete.

    Example:
    python

    # Bad – comment explains non‑obvious code
    # Subtract baseline from -200 to 0 ms
    baseline = np.mean(epochs[:, :, 200:400], axis=2)
    epochs_corrected = epochs - baseline[..., np.newaxis]

    # Good – function name replaces comment
    def subtract_baseline(epochs: np.ndarray, baseline_start_ms: int, baseline_end_ms: int):
        ...

    Anti‑pattern: Block comments describing what a whole module does — that’s what docstrings are for.

    Difficulty: Medium (tricky when explaining neurophysiological constants e.g., why notch at 50Hz).

Chapter 5: Formatting

    Core idea: Code formatting should enhance readability, not personal taste; vertical density (blank lines) separates concepts.

    Actionable rule: Related functions (e.g., compute_fft, compute_psd) go together; callers above callees.

    Example:
    python

    # High‑level EEG pipeline at top of file
    def run_quantitative_eeg(raw_path):
        raw = load_eeg(raw_path)
        cleaned = artifact_reject(raw)
        bands = extract_frequency_bands(cleaned)
        return compute_connectivity(bands)

    # Then each helper follows in order of appearance
    def load_eeg(path): ...
    def artifact_reject(raw): ...

    Anti‑pattern: 500‑line files with no blank lines or with random imports interspersed.

    Difficulty: Easy — automate with Black.

Chapter 6: Objects and Data Structures

    Core idea: Objects hide data behind abstractions (methods); data structures expose data (no methods). Use objects for behavior, data structures for simple records.

    Actionable rule: For EEG metadata (sampling rate, channel names), use a dataclass; for signal processing, use functions or objects with private data.

    Example:
    python

    # Data structure – just holds info
    @dataclass
    class EEGMetadata:
        sfreq_hz: float
        channel_names: list[str]
        montage: str

    # Object – hides complexity of filtering
    class FilteredEEG:
        def __init__(self, raw_data, metadata):
            self._data = raw_data
            self._meta = metadata
        
        def alpha_power(self) -> float:
            # internal knowledge of 8‑12Hz
            ...

    Anti‑pattern: Hybrids that expose internal arrays but also have processing methods that mutate them.

    Difficulty: Medium (beginners often over‑expose internal state).

Chapter 7: Error Handling

    Core idea: Use exceptions instead of return codes; write try‑catch‑finally blocks that separate error handling from business logic.

    Actionable rule: Catch specific exceptions (not Exception) and only at module boundaries (e.g., file loader).

    Example:
    python

    # Bad
    def load_eeg(path):
        if not os.path.exists(path):
            return None
        ...

    # Good
    def load_eeg(path):
        try:
            return mne.io.read_raw_edf(path, preload=False)
        except FileNotFoundError:
            raise EEGFileError(f"File not found: {path}")
        except mne.io.RawArrayError as e:
            raise EEGFormatError(f"Corrupt EDF: {e}")

    Anti‑pattern: Swallowing exceptions with bare except: then returning None — leads to silent failures in qEEG metrics.

    Difficulty: Easy — but many researchers ignore custom exceptions.

Chapter 8: Boundaries

    Core idea: Isolate third‑party code (MNE, NumPy, pandas) with wrappers or adapters so you can change libraries without breaking your domain logic.

    Actionable rule: Create a tiny interface for the functionality you need, then implement it with the external library.

    Example:
    python

    # Your domain expects this
    class EEGReader:
        def read(self, path: str) -> Epochs:
            ...

    # Adapter for MNE
    class MNEReader(EEGReader):
        def read(self, path):
            raw = mne.io.read_raw_edf(path)
            return self._to_epochs(raw)

    # Later you can switch to PyNEO without changing qEEG code

    Anti‑pattern: Spreading import mne through 20 files — then upgrading MNE breaks everything.

    Difficulty: Medium (requires discipline and foresight).

Chapter 9: Unit Tests

    Core idea: Keep tests clean, fast, and independent. Use one assert per test, and follow Arrange‑Act‑Assert pattern.

    Actionable rule: Name tests as test_[method/feature]_[scenario]_[expected].

    Example:
    python

    def test_alpha_power_returns_positive_float_for_eyes_closed():
        # Arrange
        eeg = MockEyesClosedData()
        processor = AlphaBandProcessor()
        
        # Act
        power = processor.compute(eeg)
        
        # Assert
        assert power > 0.0
        assert isinstance(power, float)

    Anti‑pattern: Tests that depend on real EEG files (slow, brittle) instead of synthetic or mocked data.

    Difficulty: Hard for beginners because they haven’t learned mocking/pytest fixtures.

Chapter 10: Classes

    Core idea: Classes should be small (SRP – single responsibility) and have high cohesion (methods use many instance variables).

    Actionable rule: A class should have one reason to change — e.g., EEGArtifactDetector not EEGPreprocessorAndAnalyzerAndPlotter.

    Example:
    python

    # Good separation
    class ArtifactThresholdDetector:
        def __init__(self, threshold_uv: float):
            self.threshold = threshold_uv
        
        def detect(self, epoch: np.ndarray) -> bool:
            return np.max(np.abs(epoch)) > self.threshold

    class ArtifactRejection:
        def __init__(self, detector):
            self.detector = detector
        
        def reject(self, epochs):
            return [e for e in epochs if not self.detector.detect(e)]

    Anti‑pattern: God classes with 50 methods and “Manager” or “Utils” in the name.

    Difficulty: Medium (takes practice to split responsibilities).

Chapter 11: Systems

    Core idea: Separate construction (how objects are created) from runtime use (how they behave). Use dependency injection, not hardcoded new.

    Actionable rule: Pass external dependencies (file readers, filter banks) as parameters to functions/constructors, not imported directly.

    Example:
    python

    # Bad – hardcoded
    def process_pipeline(raw_path):
        reader = MNEReader()
        epochs = reader.read(raw_path)
        # ...

    # Good – injected
    def process_pipeline(raw_path, reader: EEGReader, filter: Filter):
        epochs = reader.read(raw_path)
        return filter.apply(epochs)

    Anti‑pattern: Using a global configuration dict imported everywhere — makes testing and parallel processing impossible.

    Difficulty: Hard (requires rethinking script‑style coding).

Chapter 12: Emergence

    Core idea: Simple design emerges from passing all tests, expressing intent, removing duplication, and keeping small elements. Do not add premature abstractions.

    Actionable rule: Refactor only when you see the same EEG transformation repeated three times.

    Example: First time you copy‑paste a bandpass filter definition, leave it. Second time, note it. Third time, extract a create_bandpass function.

    Anti‑pattern: “Let’s build a flexible framework for all possible EEG montages” before you even have two real datasets.

    Difficulty: Medium (tempting to over‑engineer).

4. THE DAILY CHECKLIST

WRITING CODE
□ 1. Does the function name say exactly what it returns? (compute_alpha_power not get_value)
□ 2. Is the function ≤ 20 lines? If no, split by “and” in its description.
□ 3. Are there any comments? Could 90% become better function/variable names?
□ 4. Are all external libraries (MNE, SciPy) behind an adapter?
□ 5. Does the function modify input or rely on global state? If yes, redesign.
□ 6. Are errors raised as specific exceptions, not returned as None?

REVIEWING CODE (your own or from AI)
□ 1. Can I understand a single epoch’s path through the pipeline without jumping around?
□ 2. Are there magic numbers (e.g., 8, 12 for alpha band) — replace with named constants.
□ 3. Is the test file for this module > 0 lines? If not, reject.
□ 4. Are there side effects inside __init__ (e.g., loading files)? Move to factory function.
□ 5. Would a neurophysiologist colleague understand the business logic from names alone?

DESIGNING
□ 1. Have I drawn a boundary between “EEG domain concepts” and “file I/O / plotting / UI”?
□ 2. Can I inject a fake reader to test the qEEG algorithm without real data?
□ 3. Are any classes violating Single Responsibility (e.g., “EEGUtils”)? Split.
□ 4. Is dependency direction pointing inward (domain → adapters) not outward?

DEBUGGING
□ 1. Did the error happen inside a side effect (global variable, cache)? Eliminate it.
□ 2. Was a function doing more than one thing, making stack trace misleading? Split and retry.
□ 3. Is the bug in boundary code (adapter to MNE)? If yes, update adapter, not domain logic.
□ 4. Would a simple unit test (added now) have caught this? Write it before fixing.
5. THE DIRTY DOZEN (EEG‑Python Edition)
#	Violation	One‑line fix
1	Function process_eeg(data, fmin, fmax, notch, ref, baseline, reject, ...) has 7 parameters	Replace with a EEGProcessingConfig dataclass.
2	eeg_data = [x * 0.5 for x in eeg_data] modifies global variable	Return a new list: filtered = [x * 0.5 for x in eeg_data].
3	if handle is not None: checks after every file operation	Use try/except and let it fail early.
4	from mne import * pollutes namespace	Import import mne or specific functions.
5	def get_alpha(): – obscure name	Rename to def compute_alpha_power_microvolts_squared():.
6	A single 2000‑line preprocessing.py	Split into filtering.py, artifact.py, montage.py.
7	Hardcoded filter frequencies band(1, 40) repeated in 10 files	Define DELTA = (0.5, 4), THETA = (4, 8), etc., in constants.py.
8	No docstring on public function	Add one describing inputs (units) and outputs.
9	Catching Exception then logging “something went wrong”	Catch specific ValueError, IOError and re‑raise custom EEGProcessingError.
10	Using time.sleep(0.1) to wait for file to be written	Replace with proper file‑lock or event.
11	EEGProcessor class that reads, filters, plots, saves, and emails	Split into 5 classes.
12	Unit test that takes 20 seconds because it loads real 200MB EDF	Replace with tiny synthetic signal (e.g., sine wave + noise).
6. PATTERN QUICK-REFERENCE TABLE
Pattern / Rule	What it solves	Red flag it fixes	One‑line example (EEG)
Dependency Injection	Hidden dependencies make testing impossible	class Analyzer: def __init__(self): self.reader = MNEReader()	def analyze(reader, raw): ... and pass reader from outside.
Adapter Pattern	Vendor lock‑in to MNE or PyEDFlib	mne.io.read_raw_edf used in 15 modules	class EDFAdapter(EEGReader): def read(self, path): return mne.io.read_raw_edf(path)
Command‑Query Separation	Functions that both compute and mutate	result = filter_and_modify_inplace(epochs)	filtered = apply_bandpass(epochs.copy()) (query), then save_to_disk(filtered) (command)
Tell, Don’t Ask	Scattered validation logic	if eeg.get_status() == 'dirty': eeg.clean()	eeg.validate() — internal validation, no external if‑chains
Null Object	Excessive if epoch is None:	Checking for None before every call	class NullEpoch: def alpha(self): return 0.0 – return instead of None
Factory Method	Complex object creation (e.g., with montage, filters)	raw = load(...); raw.set_montage(...); raw.filter(...); raw.resample(...) scattered	EEGPipeline.from_edf(path, config) that builds and returns ready‑to‑use object
Single Level of Abstraction (SLA)	Mixing high‑level pipeline steps with low‑level NumPy loops	A function that contains for i in range(n_channels): and a call to plot_psd()	Extract loop into _filter_channel() helper, keep main function calling helpers
Pure Functions	Unpredictable output because of global random seed or time	def add_noise(signal): np.random.seed(0); return signal + noise	Pass rng = np.random.default_rng(seed) as parameter
7. COMMON MISCONCEPTIONS

Misconception 1: “Clean Code means shorter code.”

    Truth: Shorter often means denser and less clear. a = reduce(lambda x,y: x*y, f, 1) is shorter but worse than a for‑loop with a name. Clarity > brevity.

Misconception 2: “Small functions cause too many function calls (performance overhead).”

    Truth: Python function call overhead is negligible for EEG processing (milliseconds vs seconds of FFT). Only inline after profiling. Prefer readability first.

Misconception 3: “Comments are always bad.”

    Truth: Uncle Bob says comments are “failures,” but in science code you do need comments for references (e.g., # See Klimesch 1999, alpha frequency individually adjusted). Keep them to why not what.

Misconception 4: “Never use else after return.”

    Truth: That’s a style rule, not a law. Guard clauses are great, but sometimes if-elif-else is clearest for mutually exclusive bands (delta/theta/alpha).

Context‑dependent rules you can ignore in early‑stage prototypes:

    Dependency Injection for one‑off analysis scripts. If the script is <150 lines and won’t be reused, hard‑coding MNE is fine.

    Strict unit testing of every small filter function when you’re still exploring the hypothesis (but add tests before sharing with others).

    No comments on private internal functions within a single Jupyter notebook — but if you convert to .py file, add docstrings.

When to ignore “no side effects” – when caching computed results for performance (e.g., memoization with @functools.lru_cache) is acceptable as long as documented.
8. EVOLUTION SINCE PUBLICATION (2008)

Aged well:

    Meaningful names, small functions, single responsibility – timeless.

    The advice on testing and dependency management is even more critical with AI‑generated code.

    Formatting (vertical density, indentation) remains standard (most teams use auto‑formatters like Black).

Superseded or challenged:

    Objects vs data structures – Python’s properties, dataclasses, and NamedTuple blur the line; modern Python uses protocols (duck typing) more than classical encapsulation.

    Exception handling – In data pipelines (EEG, ML), many prefer Result types (e.g., returns library or try‑except‑else with explicit error channels) because exceptions break streaming semantics.

    Long parameter lists – Now often replaced with configuration dataclasses + **kwargs or Pydantic models.

    Explicit dependency injection – Frameworks like FastAPI and DependencyInjector are common, but for small projects simple function arguments are enough (the book over‑emphasized constructor injection).

    Chapter on concurrency – The book used Java threads; in Python you deal with asyncio, threading, or multiprocessing (very different). The core lesson (separate concurrency from business logic) still holds.

New realities since 2008:

    Jupyter notebooks changed how scientists code – Clean Code practices must adapt: use %load_ext autoreload and split into .py modules even from notebooks.

    AI assistants generate code – you need to review for naming, side effects, duplication, and testability (the checklist above becomes an AI‑output validator).

    Type hints and static checkers (mypy, pyright) enforce many Clean Code principles automatically (e.g., no None return codes).

9. COMPANION RESOURCES
Resource	Why it pairs well
“The Pragmatic Programmer” (20th Anniversary Edition) by Hunt & Thomas	Fills Clean Code’s gaps on debugging, prototyping, and tool automation – essential for solo developer building real EEG apps.
“Refactoring” 2nd ed. by Martin Fowler	Shows step‑by‑step how to transform messy EEG‑processing code into Clean Code, with a catalog of refactorings (e.g., “Extract Function”, “Replace Temp with Query”).
“Python Testing with pytest” by Brian Okken	Teaches practical test writing for scientists – fixtures, parametrization, and mocking EEG file I/O.
Clean Code (video series) – Robert Martin	Uncle Bob himself explaining the principles with live coding; helps internalize the “why”.
Tool: pylint + black + mypy (pre‑configured via pre‑commit)	Automates 80% of the checklist – naming conventions, import order, unused variables, type safety.
10. 30‑DAY ADOPTION PLAN (for you & your YouTube series)
Week 1 – Read & Internalize (2h/day)

    Day 1‑3: Read Chapters 2,3,5,7 (Names, Functions, Formatting, Error Handling).
    Output: Write a 2‑page cheat sheet of naming rules (e.g., “use compute_alpha_power not calc_ap”).

    Day 4‑5: Read Chapters 6,8,10 (Objects/Boundaries/Classes).
    Output: Refactor one existing EEG helper module (e.g., utils.py) into small classes with adapters.

    Day 6‑7: Watch Uncle Bob’s “Clean Code” talk on YouTube (1h). Write down 3 takeaways you’ll apply next week.

Week 2 – Audit Existing Code (your own EEG scripts)

    Day 8‑9: Run pylint on your qEEG pipeline – fix all naming and function‑length violations.

    Day 10‑11: Find three functions >20 lines – split each into smaller functions (test by running the same data through both versions).

    Day 12‑13: Identify every place you used mne directly. Wrap it in a simple adapter (EEGReader).

    Day 14: Write one unit test for the most critical function (e.g., alpha power calculation) using synthetic EEG.

Week 3 – Refactor Deeply (and record YouTube examples)

    Day 15‑17: Pick your most complex notebook – convert it into a package with src/eeg_analysis/ and tests/. Use pyproject.toml.

    Day 18‑19: Apply “Single Level of Abstraction” to your main pipeline – extract every for channel in ... loop into _process_channel().

    Day 20‑21: Replace all if result is None: checks with specific exceptions (e.g., raise NoEpochsError).

    *Record a 10‑min video each day showing the “before/after” for your audience.*

Week 4 – Automate & Enforce (CI + tooling)

    Day 22‑23: Set up pre-commit with black, isort, pylint, mypy. Run on every git commit.

    Day 24‑25: Configure GitHub Actions (or similar) to run tests and lints on every push.

    Day 26‑27: Add pytest‑cov – aim for >70% coverage on your core qEEG functions.

    Day 28‑30: Create a template repository for future EEG apps (clean‑code compliant). Include a Makefile with commands: make lint, make test, make format.

Day 31 onward: Every time you ask an AI for code, paste the output through your checklist (Section 4) before committing. Reject any code that fails more than 2 items.

This handbook is designed to be your desk reference as you build EEG apps and teach others. Keep it open while coding; mark the checklist with a dry‑erase marker. The transition from “hobbyist” to “principled architect” happens one small function at a time.
now read this prompts to learn how I vibe coding my projects , and adapt the hand book information for that

We need to process the user's request. The user says: "now read this prompts to learn how I vibe coding my projects , and adapt the hand book information for that"

They previously asked for a Practical Field Handbook of Clean Code for Python, System Design, ML, DevOps, Architecture, with their context: "I write Python backend APIs solo", "I'm neurophysiologist specialist in EEG and a hobbyist programmer in python, now turning in to a prompt programmer because it spare me time. ... create perfect architected EEG apps for Quantitative EEG, and I intend to learn this material while doing 'you tube classes' in python code for neurophysiologists... So probably the content you create about the book will become a 'series' or course in 'you tube' for beginners like me"

Then the assistant produced a comprehensive handbook. Now the user says: "now read this prompts to learn how I vibe coding my projects , and adapt the hand book information for that"

The user has provided a set of prompt files (the 0000 series) that define the PyArchitect Reasoner workflow, governance, delivery protocols, etc. They want the assistant to "read this prompts to learn how I vibe coding my projects" and then "adapt the hand book information for that".

Interpretation: The user is showing the assistant their "vibe coding" style - they use a highly structured, prompt-driven, governance-heavy methodology for Project Reasoner. They want the assistant to take the previously generated Clean Code handbook (which was general) and adapt it to fit this specific "vibe coding" approach. That is, the handbook should be tailored for someone who uses these prompts, follows these delivery protocols, adheres to governance layers, large module refactor protocols, etc.

The assistant needs to produce an adapted version of the handbook, integrating the principles from these prompt files (like source truth hierarchy, box logic, direct ZIP delivery, no install scripts, module size limits, docstring requirements, etc.) into the Clean Code guidance for EEG Python developers.

The assistant should respond by acknowledging the prompt files and producing an adapted handbook section or full adapted handbook, focusing on how the Clean Code principles apply within the context of the Reasoner/prompt-driven development style.

Given the complexity, the assistant should first confirm understanding, then produce the adapted content. The output should be practical, concise, and integrate the key rules from the prompts.

Let me craft the response.

I see. You’ve shared the PyArchitect Reasoner prompt stack – your actual “vibe coding” workflow. This isn’t just a style; it’s a disciplined, evidence‑first, governance‑driven system for building Python projects (like your EEG apps). You’re not a casual solo coder; you’re running a professional software engineering process with prompts as code, sandbox‑delivery, and frozen governance.

So the Clean Code handbook I wrote needs adaptation. It assumed a generic Python developer. Now I’ll rewrite it for you – the neurophysiologist who builds EEG apps using the Reasoner prompt stack.

Below is the adapted Practical Field Handbook – same structure, but now infused with your actual delivery rules, box boundaries, module limits, and governance layers.
CLEAN CODE – ADAPTED FOR REASONER & EEG PROMPT PROGRAMMING
1. ONE‑PARAGRAPH ESSENCE (Reasoner Edition)

Core thesis: In prompt‑driven development, code clarity isn’t just for humans – it’s for AI assistants, governance checkers, and future you. Every Python file in your EEG app must obey box ownership, module size caps (≤400 lines), full docstrings, and delivery as a direct ZIP (no install scripts). For your qEEG apps, this means: static_collector box extracts channel metadata, runtime_collector measures alpha power, v10_gui displays it, and manage_architecture validates no box touches another’s internal state. Why it matters now: Your “prompt programmer” workflow is the engineering process. Ignoring Clean Code means your AI will generate ungovernable, non‑testable EEG pipelines that break your own governance checks.
2. MENTAL MODEL MAP – Reasoner Boxes + Clean Code
text

                    ┌─────────────────────────────┐
                    │   GOVERNANCE HIERARCHY       │
                    │  (source truth > runtime    │
                    │   > canon > prompts)         │
                    └─────────────┬───────────────┘
                                  │
          ┌───────────────────────┼───────────────────────┐
          │                       │                       │
          ▼                       ▼                       ▼
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  STATIC BOX     │     │  RUNTIME BOX    │     │   GUI/READER    │
│ (data_collector)│     │(runtime_collector)│   │ (project_...v10)│
│ • AST parsing   │     │ • instrumentation│     │ • index loader  │
│ • imports/symbols│     │ • state snapshots│     │ • retriever     │
│ • docstring     │     │ • trace events   │     │ • prompt builder│
│   extraction    │     │                  │     │ • AI bridge     │
└────────┬────────┘     └────────┬────────┘     └────────┬────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   HANDOFF CONTRACTS      │
                    │ (typed payloads, no     │
                    │  direct state mutation)  │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   DELIVERY LAYER         │
                    │ • sandbox → ZIP          │
                    │ • _bundle_temp manifest  │
                    │ • no install script      │
                    │ • project‑relative paths│
                    └─────────────────────────┘

Connection: Clean Code’s “small functions” become box‑level single responsibility. “Meaningful names” must satisfy governance checkers (e.g., no temp, utils). “Dependency injection” is enforced by handoff contracts between boxes. “Unit tests” are your test_reasoner_project_canon.py and box‑specific pytest files.
3. CHAPTER‑BY‑CHAPTER FIELD NOTES (Reasoner‑Adapted)

    Note: Each chapter now includes how your prompt stack modifies or enforces the rule.

Chapter 2: Meaningful Names

    Core idea: Names must survive AI‑generated code reviews and governance checks.

    Actionable rule: Use compute_alpha_power_microvolts not get_ap. Avoid data, temp, utils, misc, common, shared in module or function names – your check_public_symbol_ownership.py will flag them.

    Example (EEG + Reasoner):
    python

    # Good – box‑owned, self‑documenting
    # static_collector/channel_metadata.py
    def extract_eeg_channel_names(raw_path: Path) -> list[str]:
        """Return channel names from an EDF header (static collector only)."""

    Anti‑pattern: def do_stuff(x): – your AI will generate this if you don’t guard with pylint and governance.

    Difficulty: Medium – easy to write, hard to enforce without automated checkers (add them to tools/architecture/).

Chapter 3: Functions

    Core idea: Functions ≤20 lines. If >20, extract helpers into same box (not across boxes).

    Actionable rule: Before implementing, ask: “Which box owns this function?” If two boxes need it, create a neutral contract module (e.g., contracts/eeg_types.py).

    Example (EEG + box handoff):
    python

    # runtime_collector/alpha.py – pure measurement
    def compute_alpha_power(epochs: np.ndarray, sfreq: float) -> float:
        """Return average alpha (8‑12Hz) power."""
        ...

    # GUI/reader calls it via typed handoff, never directly imports runtime_collector internals

    Anti‑pattern: A function in v10_gui that reads raw EEG files – that belongs to static_collector.

    Difficulty: Hard – because Reasoner’s box separation is stricter than classic Clean Code.

Chapter 4: Comments

    Core idea: Comments explain why (neurophysiological rationale), not what (code already does that). But docstrings are mandatory for every module, class, function, method (your own governance rule).

    Actionable rule: Every EEG processing function must have a Google‑style docstring with Args and Returns (units included).

    Example:
    python

    def notch_filter(eeg_uv: np.ndarray, line_freq_hz: float = 50.0) -> np.ndarray:
        """Apply notch filter to remove line noise.
        
        Args:
            eeg_uv: Array of shape (channels, samples) in microvolts.
            line_freq_hz: Power line frequency (50 or 60 Hz).
        
        Returns:
            Filtered array, same shape, same units.
        
        Notes:
            Uses IIR notch with Q=30. See 'MNE notch_filter' implementation.
        """

    Anti‑pattern: Comment # add 1 above x + 1. Governance checker can’t catch that, so enforce via code review.

    Difficulty: Easy – but tedious. Use insert_missing_docstrings_gui from your toolchain.

Chapter 5: Formatting

    Core idea: Use Black (enforced by pre-commit). No debate.

    Actionable rule: In your pyproject.toml, set line length = 100. Keep blank lines between box boundaries.

    Example: Not needed – Black does it.

    Anti‑pattern: Mixing tabs and spaces. Your pre‑commit will reject.

    Difficulty: Easy – automate.

Chapter 6: Objects and Data Structures

    Core idea: Use dataclass or TypedDict for handoff contracts between boxes. Never expose internal mutable state.

    Actionable rule: Define all inter‑box payloads in a contracts/ folder owned by no box (neutral infrastructure).

    Example:
    python

    # contracts/eeg_evidence.py
    from dataclasses import dataclass

    @dataclass(frozen=True)
    class ChannelPower:
        name: str
        alpha_uV2: float
        theta_uV2: float

    # Used by static collector (producer) and GUI (consumer)

    Anti‑pattern: static_collector passing its internal _raw object to runtime_collector. Use the contract.

    Difficulty: Medium – requires design upfront.

Chapter 7: Error Handling

    Core idea: Use custom exceptions per box. Never return None to indicate error – your mypy will enforce.

    Actionable rule: Define class EEGFileError(Exception): pass in a shared exceptions.py (neutral contract).

    Example:
    python

    # static_collector/reader.py
    def load_edf(path: Path) -> np.ndarray:
        try:
            return mne.io.read_raw_edf(path)
        except FileNotFoundError:
            raise EEGFileError(f"Missing file: {path}")

    Anti‑pattern: except Exception: pass – silent failure. Your governance checker should flag bare except.

    Difficulty: Easy – but requires discipline across boxes.

Chapter 8: Boundaries

    Core idea: Isolate third‑party libraries (MNE, SciPy) behind adapters in one box only. Other boxes use the adapter’s interface.

    Actionable rule: The static_collector owns the MNE adapter. GUI never imports mne directly.

    Example:
    python

    # static_collector/mne_adapter.py
    class EEGReader:
        def read(self, path: Path) -> Epochs: ...

    # GUI/reader.py
    from static_collector.mne_adapter import EEGReader  # allowed – read‑only interface

    Anti‑pattern: import mne in 5 different files. Your check_layer_boundaries.py will fail.

    Difficulty: Medium – requires planning but pays off when you upgrade MNE.

Chapter 9: Unit Tests

    Core idea: Every box must have its own tests/ folder. Use pytest with synthetic EEG data (no real 200MB files in repo).

    Actionable rule: For static_collector, mock mne; for runtime_collector, use a tiny sine wave. Keep tests under 50ms each.

    Example:
    python

    # tests/static_collector/test_reader.py
    def test_load_edf_returns_epochs():
        mock_raw = MagicMock(spec=mne.io.Raw)
        with patch('mne.io.read_raw_edf', return_value=mock_raw):
            reader = EEGReader()
            epochs = reader.load_edf(Path("fake.edf"))
            assert epochs is mock_raw

    Anti‑pattern: A test that actually reads C:\users\my_big_data.edf. Your CI will time out.

    Difficulty: Hard for researchers – but essential for reproducible qEEG.

Chapter 10: Classes

    Core idea: A class = one box responsibility. No “EEGManager” god class.

    Actionable rule: If you see and in the class name (ArtifactDetectorAndPlotter), split.

    Example:
    python

    # runtime_collector/artifacts.py
    class ThresholdArtifactDetector:
        def detect(self, epoch: np.ndarray) -> bool: ...

    class MovingWindowArtifactDetector: ...

    Anti‑pattern: A class that accesses self._static_data and self._runtime_state – cross‑box coupling.

    Difficulty: Medium – but Reasoner’s box rules force you to think about it.

Chapter 11: Systems

    Core idea: Construction (how objects are created) is separate from runtime. Use dependency injection via function arguments, not hardcoded MNEReader() inside a method.

    Actionable rule: In Reasoner, the GUI box constructs and injects dependencies into other boxes at startup.

    Example:
    python

    # GUI box – main.py
    reader = EEGReader(adapter=MNEAdapter())
    analyzer = AlphaAnalyzer(reader=reader)

    Anti‑pattern: EEGProcessor calling MNEReader() inside __init__. Testability = 0.

    Difficulty: Hard – but your check_layer_boundaries.py can detect hardcoded imports.

Chapter 12: Emergence

    Core idea: Don’t add abstractions until you have three repetitions. Reasoner’s large‑module protocol (≥400 lines) forces refactoring early.

    Actionable rule: When a Python file hits 450 lines, open a refactor task. At 500 lines, stop and invoke 0000 5.7.

    Example: Your artifact_detection.py grows to 550 lines → you must split into _help/ folder with threshold.py, moving_window.py, etc.

    Anti‑pattern: “I’ll refactor later” – later never comes. Use daily_rfctr_report to track.

    Difficulty: Easy to follow (the tool forces you).

4. THE DAILY CHECKLIST (Reasoner & EEG Edition)

WRITING CODE (prompt + sandbox)
□ 1. Which box owns this code? (static/runtime/GUI/contracts/tools)
□ 2. Is the module line count ≤400? If >400, plan split per 0000 5.7.
□ 3. Does every function have a docstring (Args, Returns)?
□ 4. Are all external imports (mne, numpy) behind an adapter in the owning box?
□ 5. Is the function pure (no global state, no I/O) unless it’s the I/O box?
□ 6. Are error exceptions custom and box‑specific?
□ 7. Is there a unit test for this new function (synthetic data)?

REVIEWING CODE (AI‑generated or your own)
□ 1. Does it pass pylint, mypy, black, isort? (pre‑commit must pass)
□ 2. Are there any wildcard imports (from x import *)? Reject.
□ 3. Is the __all__ explicit and bound? Governance checker enforces.
□ 4. Does any function modify its input argument? If yes, document or change.
□ 5. Does the code import from another box’s internal module? That’s a layer violation.
□ 6. Is the file >500 lines? If yes, stop – invoke large‑module protocol.

DESIGNING (before writing prompts)
□ 1. Have I defined the handoff contract (typed payload) between boxes?
□ 2. Is there a neutral contract module for shared types?
□ 3. Does the design respect PROJECT_ROOT dynamic (no hardcoded E:\eeg_data)?
□ 4. Will this change require updating REASONER_PROJECT_CANON.json? If yes, wait for freeze.

DEBUGGING (when something breaks)
□ 1. Did the error occur in sandbox or after delivery?
□ 2. Is the bug in a cross‑box handoff (mismatched type)? Add schema validation.
□ 3. Does a module exceed 500 lines and hide the bug? Split it per protocol.
□ 4. Is the bug only in the runtime collector? Mock it in tests and isolate.
5. THE DIRTY DOZEN – Reasoner Edition
#	Violation	One‑line fix (Reasoner style)
1	Python file >500 lines	Invoke 0000 5.7, split into _help/ with one class/function per file.
2	Missing docstring on a function	Run insert_missing_docstrings_gui before commit.
3	import mne inside GUI box	Move MNE adapter to static_collector/mne_adapter.py; GUI uses the adapter.
4	Function modifies global PROJECT_ROOT	Treat PROJECT_ROOT as read‑only config; pass as argument.
5	Using except: bare	Replace with except EEGFileError: and re‑raise custom exception.
6	__all__ = ['foo', 'bar'] but foo not defined in module	Define foo or remove from __all__. Governance checker will fail.
7	Two boxes both define normalize_path()	Move to contracts/path_utils.py (neutral).
8	GUI box reads EDF file directly	Delegate to static_collector via typed handoff.
9	No unit test for new EEG filter	Create synthetic sine wave test in tests/runtime_collector/.
10	Hardcoded path E:\eeg_study\patient1.edf	Use PROJECT_ROOT / "data" / "patient1.edf" (configurable).
11	class EEGUtils with 30 unrelated methods	Split into EEGReader, EEGFilter, EEGPlotter – each in its own box.
12	Delivery ZIP requires running install.bat	Re‑deliver as direct ZIP with final paths, no script.
6. PATTERN QUICK‑REFERENCE (Reasoner + EEG)
Pattern / Rule	What it solves	Red flag	One‑line example (Reasoner)
Box‑owned adapter	Isolate MNE to one box	import mne in 3 places	static_collector/mne_adapter.py – only box that imports mne.
Neutral contract module	Type‑safe handoff between boxes	def process(data): – unknown type	contracts/eeg_payloads.py with @dataclass(frozen=True).
Sandbox + ZIP delivery	No install scripts, atomic updates	User asked to run setup.py	AI creates files in sandbox, zips with project‑relative paths.
Governance checker	Enforce Clean Code rules automatically	Human forgets to check __all__	check_public_symbol_ownership.py runs in CI.
Large‑module split (_help/)	Keep files ≤400 lines	artifact.py hits 500 lines	Create artifact_help/threshold.py, artifact_help/moving_window.py.
Source‑preserving facade	Stabilize huge module before deep refactor	800‑line file with risky API	Old file becomes facade, real code in _preserved/ shards.
7. COMMON MISCONCEPTIONS (in Reasoner context)

Misconception 1: “I can ignore docstrings in private helpers.”

    Truth: Your own insert_missing_docstrings_gui and governance checkers will fail. Every function needs a docstring.

Misconception 2: “Small modules (≤400 lines) are over‑optimization for a solo project.”

    Truth: Your daily_rfctr_report will flag large files. More importantly, AI assistants (including me) produce better code when files are small and focused.

Misconception 3: “Comments are evil – never write them.”

    Truth: In EEG code, you must comment the neurophysiological why (e.g., # Alpha band 8‑12Hz based on IFCN 2017). Code cannot express that.

Context‑dependent rules to ignore in early prototyping:

    Strict box separation – when you’re exploring a new qEEG metric in a Jupyter notebook, it’s fine to mix boxes. But once you commit to developer_tools, refactor into boxes.

    Full unit testing – for one‑off analysis scripts. But if you share the code, tests are mandatory.

    No install script – for internal tools only. If you distribute to colleagues, a pip install -e . is fine.

8. EVOLUTION SINCE PUBLICATION – Reasoner supersets Clean Code

What aged well (and your stack enforces):

    Small functions, meaningful names – your governance checkers automate this.

    Unit testing – your test_reasoner_project_canon.py is a model.

What Reasoner adds (beyond Clean Code):

    Strict module size limits (400/500 lines) – Clean Code only suggested “small”.

    Docstring mandatory – Clean Code was lax.

    Box ownership & handoff contracts – Clean Code’s “classes” don’t scale to multi‑box systems.

    No install script delivery – Clean Code didn’t address deployment.

    Governance as code – Clean Code is advisory; your stack makes rules executable.

Superseded by modern Python + Reasoner:

    Manual dependency injection – you use contracts/ and PROJECT_ROOT dynamic config.

    Chapter on concurrency – you use asyncio or multiprocessing with runtime_collector owning the event loop.

9. COMPANION RESOURCES (Reasoner‑optimized)
Resource	Why it pairs with your workflow
0000 5.7 LARGE MODULE REFACTOR PROTOCOL	Your official way to split files over 500 lines – essential for EEG pipelines that grow organically.
0000 3.6 PROFESSIONAL GOVERNANCE LAYER	Use when you change schema, AI bridge, or retrieval – your high‑risk work needs this.
pytest + pytest‑cov	Your test_reasoner_project_canon.py uses it; extend to all boxes.
pre‑commit with black, isort, pylint, mypy	Automates 80% of your Clean Code checklist.
“Evidence‑Grounded Engineering” (your own blog)	You’re inventing this – the idea that prompts + source truth + governance = reliable AI‑assisted coding.
10. 30‑DAY ADOPTION PLAN (for you, the Reasoner user)
Week 1 – Embed Clean Code into your prompts

    Day 1‑2: Add a new governance rule to REASONER_PROJECT_CANON.json:
    “Every Python function must have a docstring with Args/Returns.” Update check_reasoner_project_canon.py to enforce it.

    Day 3‑4: Write a small tools/architecture/check_module_size.py that fails if any .py file >500 lines (exclude tests, _help/).

    Day 5‑7: Refactor your largest EEG module (likely artifact_rejection.py) into _help/ following 0000 5.7. Deliver via ZIP.

Week 2 – Audit your existing EEG codebase

    Day 8‑10: Run pylint on kanda_reasoner_app/ – fix all naming, import, and docstring issues.

    Day 11‑12: Identify any cross‑box imports (e.g., GUI importing runtime collector). Replace with handoff contracts.

    Day 13‑14: Write synthetic data unit tests for your top 3 qEEG functions. Use pytest with mocker.

Week 3 – Refactor a real EEG pipeline (record YouTube)

    Day 15‑17: Choose your most complex notebook (“spectral analysis”). Convert it to a package with static_collector, runtime_collector, contracts.

    Day 18‑19: Apply “command‑query separation” – separate functions that compute from those that plot.

    Day 20‑21: Add pre-commit to your repo. Run black, isort, pylint, mypy on every commit.

Week 4 – Automate & Govern

    Day 22‑24: Set up GitHub Action that runs check_reasoner_project_canon.py, pytest, and size checker on every PR.

    Day 25‑27: Write a tools/delivery/validate_zip.py that ensures a delivered ZIP has no install scripts and uses correct paths.

    Day 28‑30: Freeze your new governance rules: run 0000 4.11 to update the four canon files. Deliver the governance ZIP.

Ongoing: Every time you ask an AI (including me) for EEG code, prepend: “Follow Reasoner Clean Code: docstrings, ≤400 lines per file, box ownership, no wildcard imports, deliver as direct ZIP.”

Final Note: You’re not just “vibe coding”. 
You’ve built a professional, repeatable, AI‑assisted engineering system. 
Clean Code is a subset of what you already enforce. 
This handbook shows you how to extend your own governance to produce EEG apps that are 
scientifically reproducible, maintainable, and safe – all while teaching neurophysiologists 
on YouTube.

Now, if you want me to convert this adapted handbook into a ZIP delivery
(following your 0000 0.8 protocol), or produce a YouTube script outline based on it,
just tell me which prompt files to load next.

