Kanda Bundle-Gated Development Workflow v1.0
Status
Reusable engineering methodology for AI-assisted software development.
Short name
Kanda Bundle-Gated Development
Full name
Kanda Bundle-Gated Development and Validate-Before-Freeze Workflow
Core sentence
Kanda Bundle-Gated Development is a staged engineering workflow where every AI-generated change is delivered as a focused installable bundle, paired with focused tests, installed locally by the user, validated through project gates, and only then accepted as the next baseline.
Fundamental rule
Nothing becomes baseline until it is installed locally and validated.
Purpose
This method exists to make AI-assisted programming safer, more auditable, and less likely to damage working code.
The method protects against:
silent source corruptionlarge accidental rewritescross-box regressionsunvalidated claimsbroken importsstale generated artifactshidden architecture warningsloss of context between sessions
It is slower than direct editing, but it is much safer for a complex project.

1. Main philosophy
The AI does not directly mutate the trusted project baseline.
Instead, the AI produces a controlled patch bundle.
The human installs the bundle locally.
The project itself decides whether the patch is valid through tests and validators.
The cycle is:
plan -> bundle -> install -> validate -> accept or repair -> freeze
The AI may suggest, generate, explain, and package.
The project validators decide whether the result is acceptable.
The human decides whether to continue, repair, or freeze.

2. The workflow cycle
Step 1 - Define the box
Before coding, identify the active box.
A box is a responsibility area, such as:
architecture governanceworkflow governanceTab 3 docstring toolingTab 9 prompt libraryruntime collectionGUI layerengineering safety
The active rule is:
Modify only the active box by default.
Cross-box changes are allowed only when necessary and must be declared.
A cross-box change is allowed when:
another box blocks the current box from workinganother box invades the current boxthe current box invades another boxshared canonical infrastructure must be updated
When crossing boxes, declare:
current boxexternal boxwhy the external touch is requiredexact files touchedvalidation for current boxvalidation for external box

Step 2 - Create a small, focused patch
Each bundle should do one thing.
Good examples:
add one report schemarepair one validation warningadd one focused GUI buttonmove one runtime helperupdate one governance freeze
Bad examples:
refactor unrelated tabschange GUI and governance and AI runtime togethermodify source and tests and prompt library without a single reasonbundle multiple speculative features
A bundle should be understandable from its name.
Example:
T3R026_tab3_runtime_relocation_markers.zipES001_engineering_safety_schema.zipGOV014_t3r026_governance_freeze.zip

Step 3 - Include focused tests
Every code bundle should include at least one focused test.
The focused test should answer:
Did this exact patch do what it claims?Did this exact patch preserve the safety boundary?Did this exact patch avoid the known regression?
Examples:
tests/test_t3r026_tab3_runtime_relocation_markers.pytests/test_es001_engineering_safety_schema.pytests/test_t3r024_tab3_refactor_collision_repair.py
The test should be narrow.
It should not pretend to validate the whole system.

Step 4 - Package as an installable ZIP
The AI delivers a ZIP bundle.
The bundle contains only the files needed for the current step.
The bundle should not contain unrelated files.
The bundle should include:
modified source filesfocused testsbundle manifestoptional documentation or status notes
A governance-only bundle is stricter. It should contain only governance files.

Step 5 - User installs locally
The user installs the bundle into the real project root.
Standard install pattern:
$root = "E:\developer_tools"$zipPath = "E:\BUNDLE_NAME.zip"Expand-Archive -Path $zipPath -DestinationPath $root -ForceWrite-Host "Bundle installed."
Important Windows note:
Expand-Archive -Force overwrites existing files, but does not delete files absent from the ZIP.
Therefore, if a patch needs to remove files, the safer method is either:
provide explicit PowerShell delete commands
or:
overwrite stale files with tested relocation markers

Step 6 - Run focused validation
After installation, run the focused test first.
Example:
cd E:\developer_toolspython tests\test_t3r026_tab3_runtime_relocation_markers.py
The focused test must pass before global validation matters.
If the focused test fails, the patch is not stable, even if global validation passes.

Step 7 - Run architecture validation
Then run architecture validation.
Example:
python kanda_reasoner_app\manage_architecture\manage_architecture.py --root E:\developer_tools --validate
Expected clean state:
No validation issues.
If warnings or errors appear, the next bundle should be a repair bundle, not a new feature.

Step 8 - Run workflow validation
Then run workflow validation.
Example:
python kanda_reasoner_app\manage_workflows\manage_workflows.py --root E:\developer_tools --validate
Expected clean state:
Summary: pass=8 fail=0 warn=0 skip=3
The exact expected values can evolve, but the principle remains:
the workflow baseline must not degrade silently

Step 9 - Report actual result
The user reports the local validation output.
The AI must treat local validation as the source of truth.
The AI must not claim success based only on sandbox tests.
Valid statuses:
installed and focused test passedinstalled but focused test failedarchitecture cleanarchitecture has warningsarchitecture has errorsworkflow greenworkflow failed

Step 10 - Decide: continue, repair, or freeze
If validation passes:
continue to the next small step
If validation fails:
create a repair bundle
If a milestone is complete and the user explicitly approves:
create a governance freeze bundle
No governance freeze should happen silently.

3. Validation hierarchy
The order is important.
1. focused test2. architecture validation3. workflow validation4. optional GUI/manual validation5. optional governance checker
A patch is stable only if the relevant gates pass.
For GUI work, manual validation is also required.
For governance work, the canon checker and governance tests are required.

4. Bundle types
A. Feature bundle
Adds a new feature.
Must include:
source filesfocused testsbundle manifest
Must not include unrelated governance changes.

B. Repair bundle
Fixes a failed validation state.
Must include:
the smallest repair possiblefocused regression testclear explanation of what failed
Repair bundles should not add new features.

C. Refactor bundle
Changes structure without changing behavior.
Must include tests proving:
public imports are preservedruntime behavior is preservednew ownership is clearerarchitecture warnings/errors are reduced

D. Governance bundle
Updates official canon.
Must contain only governance files.
Typical five-file governance bundle:
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\REASONER_PROJECT_CANON.md_project_reference\ACTIVE_PROJECT_ GOVERNANCE\REASONER_PROJECT_CANON.json_project_reference\ACTIVE_PROJECT_ GOVERNANCE\check_reasoner_project_canon.py_project_reference\ACTIVE_PROJECT_ GOVERNANCE\test_reasoner_project_canon.py_project_reference\ACTIVE_PROJECT_ GOVERNANCE\accepted_warning_baseline.json
Governance bundles require explicit user approval.

E. Handoff bundle or note
Used when work is not finished.
A handoff is continuity evidence.
It is not canon.
It should say:
what was donewhat passedwhat failedwhat is pendingnext safe stepfiles to request next

5. Naming convention
A good bundle name should contain:
gate IDbox or featurepurpose
Examples:
T3R026_tab3_runtime_relocation_markers.zipES001_engineering_safety_schema.zipES002_risk_change_radar_mvp.zipGOV014_t3r026_governance_freeze.zip
Recommended pattern:
<TRACK><NUMBER>_<short_slug>.zip
Examples:
T3R = Tab 3 RefactorES = Engineering SafetyGOV = GovernanceT9G = Tab 9 GUIT9T = Tab 9 Text

6. Bundle manifest
Each bundle should include a manifest under:
workbench\_bundle_temp\
Example:
workbench\_bundle_temp\BUNDLE_MANIFEST_es001_engineering_safety_schema.txt
Manifest should include:
bundle namescopefiles changedbehavior addedbehavior preservedvalidation performedinstall instructionslocal validation commandsknown limitations
The manifest is a human audit trail.

7. Safety principles
Principle 1 - One concern per bundle
Do not mix unrelated fixes.
A bundle should answer one question:
What exact change is being validated?

Principle 2 - Preserve working behavior
Never break a working feature as a side effect of a cleanup.
If behavior changes, say so explicitly.

Principle 3 - No silent cross-box changes
If a patch touches another box, explain why.

Principle 4 - No source auto-modification without review
Tools may analyze, propose, and draft.
They should not auto-modify production source unless the user explicitly approves and validation gates exist.

Principle 5 - Local validation beats AI confidence
The AI may be confident.
The project may disagree.
The project wins.

Principle 6 - Repair before expansion
If validation fails, stop adding features.
Repair first.

Principle 7 - Governance only after explicit freeze
Governance updates require:
validated workexplicit user approvalbaseline governance filesupdated checkerupdated testsversion increment

8. Standard install and validation block
Use this after each bundle:
$root = "E:\developer_tools"$zipPath = "E:\BUNDLE_NAME.zip"Expand-Archive -Path $zipPath -DestinationPath $root -ForceWrite-Host "Bundle installed."cd E:\developer_toolspython tests\FOCUSED_TEST_NAME.pypython kanda_reasoner_app\manage_architecture\manage_architecture.py --root E:\developer_tools --validatepython kanda_reasoner_app\manage_workflows\manage_workflows.py --root E:\developer_tools --validate
Expected result for the current Kanda Reasoner baseline:
Focused test: passArchitecture: No validation issuesWorkflow: pass=8 fail=0 warn=0 skip=3

9. Standard governance validation block
Use this after a governance bundle:
$root = "E:\developer_tools"$zipPath = "E:\GOVERNANCE_BUNDLE.zip"Expand-Archive -Path $zipPath -DestinationPath $root -ForceWrite-Host "Governance bundle installed."cd E:\developer_toolspython "_project_reference\ACTIVE_PROJECT_ GOVERNANCE\check_reasoner_project_canon.py" --project-root E:\developer_toolspython "_project_reference\ACTIVE_PROJECT_ GOVERNANCE\test_reasoner_project_canon.py"python kanda_reasoner_app\manage_architecture\manage_architecture.py --root E:\developer_tools --validatepython kanda_reasoner_app\manage_workflows\manage_workflows.py --root E:\developer_tools --validate
Expected result:
REASONER PROJECT CANON CHECK: PASSAll governance tests passed.Architecture: No validation issuesWorkflow: pass=8 fail=0 warn=0 skip=3

10. AI behavior rules
When acting under this methodology, the AI must:
give a short plan before complex workcreate a focused bundleinclude focused testsavoid unrelated editsexplain validation performedprovide install commandsprovide local validation commandswait for the user's validation output before claiming final successrepair failed validation before continuing
The AI must not:
claim local validation without user outputhide failed focused testsbundle unrelated changessilently update governancetreat handoff notes as canondelete accepted-warning history without explicit pruning approvalchange box ownership without declaring it

11. Human behavior rules
The human user should:
install the ZIP locallyrun the exact validation commandspaste the terminal output backconfirm manual GUI behavior when applicableapprove or reject governance freezes explicitly
The user should not need to manually inspect every line before testing, because the method relies on focused tests and validators.
However, for high-risk code, manual inspection is still useful.

12. When to freeze
Freeze only after:
the feature or refactor is completefocused tests passarchitecture validation is cleanworkflow validation is cleanmanual validation passes if GUI/runtime behavior changedthe user explicitly approves the freeze
A freeze can be:
handoff freezegovernance freezerelease freezeaccepted-warning freeze
But only governance freeze changes active canon.

13. When not to freeze
Do not freeze when:
focused validation failedarchitecture has new warnings or errorsworkflow validation failedmanual GUI validation is pendingthe result is only a proposalthe user did not approve canon update
In those cases, create a handoff instead.

14. Accepted-warning policy
Accepted-warning baselines are governed ledgers.
They must not be recreated from memory.
They should be preserved unless the user explicitly approves a pruning task.
Rules:
preserve historical entriesappend only with explicit approvaldo not hide new unaccepted warningsdo not use baseline as a trash bineach accepted warning needs classification, rationale, path, code, and stable matcher
If a warning is later fixed, the historical entry can remain.
Pruning is a separate explicit task.

15. Windows install limitation
Important rule learned during T3R025/T3R026:
Expand-Archive -Force does not delete files absent from the ZIP.
Therefore, if a patch must remove files, do one of these:
include explicit Remove-Item commandsor overwrite stale files with harmless tested relocation markers
Do not assume that absence from a ZIP deletes the installed file.

16. Example successful cycle
Example:
T3R026_tab3_runtime_relocation_markers.zip
Process:
1. T3R025 attempted to delete stale relocation shims.2. Windows install did not delete absent files.3. Validation showed remaining dead-code warnings.4. T3R026 repaired by overwriting stale files with tested relocation markers.5. Focused tests passed.6. Architecture validation reported No validation issues.7. Workflow validation reported pass=8 fail=0 warn=0 skip=3.8. Governance was later updated to freeze T3R026.
Lesson:
Validation output decides the next step.

17. Template for future use
Bundle plan
Bundle ID:Active box:Goal:Files expected to change:Files explicitly out of scope:Risk level:Focused tests:Architecture validation command:Workflow validation command:Manual validation needed:Governance update needed:
Bundle result
Bundle:Installed locally: yes/noFocused test result:Architecture result:Workflow result:Manual validation:New warnings:New errors:Decision:Next step:
Freeze decision
Freeze requested: yes/noFreeze approved by user: yes/noGovernance files available: yes/noCanon version update needed: yes/noAccepted-warning baseline change needed: yes/no

18. Final definition
Kanda Bundle-Gated Development is a validate-before-freeze workflow for AI-assisted engineering. Each change is delivered as a narrow installable bundle with focused tests. The user installs it locally, runs focused and global validators, reports the real result, and only then is the change accepted, repaired, continued, or frozen into governance. The method protects box boundaries, preserves working behavior, prevents silent regressions, and makes AI-generated code auditable.
Motto
Bundle first.Validate locally.Repair before expanding.Freeze only with evidence.