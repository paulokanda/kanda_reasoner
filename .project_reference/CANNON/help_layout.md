# Help Layout Reference Note

This file is reference-only. It is not an active KANDA prompt-library artifact, runtime contract, freeze target, or project canon.

The active routed prompt created from the help-layout notes is:

```text
kanda_prompt_workspace/prompt_library/ACTIVE_PROMPTS/12_generalized_project_canons/desktop_help_document_layout_canon.md
```

Active prompt metadata is:

```text
kanda_prompt_workspace/prompt_library/METADATA/desktop_help_document_layout_canon.meta.json
```

Routing and discovery belong to:

```text
kanda_prompt_workspace/prompt_library/ROUTING/
```

Use `.project_reference` only as historical notes or explicit human-provided source evidence. Do not validate project behavior against this file.
