first_prompts_to_ai(42).zip
Zip Archive
prompt_library(42).zip
Zip Archive
tell_AI_read_before_all(42).md
File
zz_read_only_if_modifying_startup_delivery(23).md
File


STARTUP PACK LOAD CHECK

Files recognized:
0. 00_START_HERE_FOR_AI.md - loaded - boot entrypoint defining startup gates, exact routing overrides, readiness checks, and anti-bypass behavior

    01_ai_prompt_request_canon.md - loaded - classifies requests and determines Fast Path, Routed Work Path, required context, and whether work may proceed

    02_prompt_navigation_index.md - loaded - maps prompt codes, identifiers, paths, groups, and routing destinations

    03_GROUP_ASSIMILATION_INDEX.md - loaded - selects the smallest relevant prompt group before specialist-level retrieval

    04_FOLDER_ASSIMILATION_CARDS_INDEX.md - loaded - directs routed work to the appropriate folder-specific assimilation card

    05_start_of_day_master_stack.md - loaded - activates beginning-of-day governance, architecture ownership, module-size, and boundary safeguards

    06_session_start_upload_checklist.md - loaded - verifies startup uploads, project handoff inputs, and session-readiness prerequisites

    07_daily_patch_delivery_guardrails.md - loaded - governs patch packaging, installation, validation, delivery, and terminal behavior

    08_handoff_at_end_of_work.md - loaded - governs end-of-work handoff completeness, evidence, changed files, and next-session continuity

    12_error_memory_ai_formulary_startup_canon.md - loaded - activates compact Error Memory intake and controlled escalation to full error history

    13_durable_document_artifact_routing_canon.md - loaded - routes durable documents and successful validation evidence outside transient daily-work storage

    14_project_tool_boundary_canon.md - loaded - distinguishes the active project root from the KANDA tool root and prevents cross-root leakage

    09_active_project_freeze_context.md - loaded - exposes current protected features, frozen behavior, owner paths, and regression constraints

    README_STARTUP_PROMPT_REQUEST_KERNEL.md - loaded - documents pack purpose, generated-copy rules, canonical sources, and startup use

    STARTUP_PROMPT_REQUEST_KERNEL_MANIFEST.json - loaded - provides build metadata, included-file records, source provenance, and delivery configuration

Beginning-of-day active bridges:

    Code Module Size Bridge - loaded - hard gate: ideal <=400 code lines; maximum <=500 physical lines; PEP 8, SOLID, and DRY are canonical; never compress formatting or duplicate logic to fit; create cohesive helpers when needed; every new or touched code/source module must be <=500 lines after PEP 8 formatting and create, update, modify, refactor, or split work; route above-limit code work to large_module_refactor_protocol on demand

    Box Logic Startup Bridge - loaded - hard gate: identify active box, owner paths, allowed files, out-of-scope files, cross-box touches, public contracts, validation scope, and boundary risks before project work; route boundary-risk work to box_architecture_canon on demand

    No-Leak Logic Bridge - loaded - hard gate: NO_LEAK_LOGIC_V1 prevents wrong-root writes, tool/project leakage, cross-box leakage, private reach-in, hidden mutable state, generated-artifact-as-source leakage, and evidence leakage

    Class 04 Architecture Owner Dispatch - loaded - Brick Wall remains final coding authority; do not load the retired compiled companion; route only the smallest current Box Architecture, Boundary-First, Shielding, Folder Placement, or Stateful Control owner set

    Durable Documentation Artifact Routing Bridge - loaded - daily-work is transient; durable project documents and successful validation evidence belong under the selected project sibling _show_project_to_AI support root

    Terminal Cleanup Bridge - loaded - install success waits about 2 seconds then Clear-Host; validation, freeze, errors, diagnostics, and other terminal blocks use Enter Enter then one final Clear-Host; keep terminal open; freeze-prep evidence merge must use a temp .py helper, not python -c

    Runtime Evidence Escalation Bridge - loaded - request the smallest scenario-specific trace, focused test, logs, or terminal evidence package when actual execution behavior matters; prefer current evidence, do not over-request for source-sufficient questions, and keep static-only conclusions labeled as inference

Startup status:
COMPLETE

Routing behavior:
Simple, source-sufficient explanation work may use Fast Path. Governed, implementation, architecture, prompt-library, freeze, patch, or boundary-sensitive work will use Routed Work Path: first select the smallest relevant group, then the applicable folder card, and finally only the necessary specialist prompt from prompt_library.zip. Missing context, validation evidence, ownership information, or required project files will block implementation rather than being bypassed. The canonical prompt-library ZIP remains unopened until routing identifies a specific prompt_path.

Next action:
Waiting for all files (Project Files) from second_prompt_files folder

kanda_reasoner__source_archive_part01_of_01(44).zip
Zip Archive
kanda_reasoner__png_assets_part01_of_01(43).zip
Zip Archive
kanda_reasoner__error_memory_manifest(44).json
File
kanda_reasoner__error_memory_full(44).zip
Zip Archive
kanda_reasoner__error_memory_ai_prompt(44).md
File
kanda_reasoner__error_lessons_compact(44).json
File
kanda_reasoner__ai_handoff_upload_readme(44).txt
Document
kanda_reasoner__ai_handoff_upload(44).zip
Zip Archive
kanda_reasoner__ai_handoff_all_in_one(43).zip
Zip Archive
_RUN_COLLECTOR_STATUS(44).txt
Document

PROJECT READY CHECK
Project slug: kanda_reasoner
Active project root: E:\kanda_reasoner
KANDA tool root: E:\kanda_reasoner
Same physical root: YES
Compact Error Memory loaded: YES - 10 active lessons; full Error Memory content not loaded because no task currently requires it.
Second-upload handoff loaded: YES - collector and ZIP export reported complete.
Tier-1 gates active: YES - startup routing, Box Logic, No-Leak Logic, module-size limits, exact-source inspection, compact Error Memory preflight, patch-delivery contracts, freeze confirmation, and validation honesty. The handoff validation state is NOT_RUN, so no current validation pass or freeze readiness is claimed.
Next action:
WAIT_FOR_TASK

0076 0049 WEB AI IN KANDA_REASONER.MD
File

does kanda_reasoner is in accordance with uploaded handoff "web ai in kanda_re...."?
do you understantd tool versus project cannon?
is web ai in kanda reasoner respecting tool versus project cannon?
is web ai programmed to work with project not tool ?

Verdict

I understand the Tool-versus-Project canon.

For the current self-hosting case, where both the KANDA Tool and the selected Project are physically located at E:\kanda_reasoner, the Project Web AI behaves correctly in most important respects.

However, I would not classify the implementation as fully compliant with the complete Tool-versus-Project canon yet. There is one concrete boundary defect in the support-root resolver, plus several smaller differences from the uploaded roadmap.
Tool versus Project canon

The correct interpretation is:
Logical owner	Meaning
Tool	Reusable KANDA application code, GUI, provider runtime, validators and orchestration
Project	The repository currently selected by the user
Project Support	Project-specific handoff, Error Memory, Freeze Memory and evidence outside project source
Transient workspace	Disposable project-specific temporary files

In self-hosting mode:

Tool root:    E:\kanda_reasoner
Project root: E:\kanda_reasoner

The physical paths are equal, but the logical owners remain different.

Therefore:

    kanda_reasoner_app\reasoner_engine\project_web_ai_tab.py is Tool-owned code.

    The selected repository is Project-owned source.

    E:\kanda_reasoner_show_project_to_AI is Project Support.

    The Project Web AI may read Project Support but must not treat it as Tool state or source truth.

    The Web AI response remains advisory and cannot modify the selected Project.

That matches the governing principle that self-hosting must not collapse Tool and Project ownership.
Is Project Web AI programmed for the Project rather than the Tool?

Yes.

The application shell owns a global current_project_root. When Project Web AI is loaded, its project_root_edit is bound to that global selection. A project change in another loaded tab is propagated into Project Web AI, and a project change in Project Web AI is propagated back to the shell.

The effective flow is:

Shell current_project_root
        |
        v
ProjectWebAITab.project_root_edit
        |
        v
load_project_web_ai_context(selected_project_root)
        |
        v
selected Project's Project Support

The code does not hardcode kanda_reasoner as the project. It derives the project slug from the selected root name.

Consequently, when KANDA Reasoner itself is selected, it reads:

E:\kanda_reasoner_show_project_to_AI

When another project is selected, it is intended to read that project's corresponding support folder.

This is consistent with the roadmap's desired architecture:

Existing Project Selection
        |
Thin Project Context Bridge
        |
Shared Web Provider Runtime
        |
Project Web AI GUI

The uploaded roadmap explicitly requires a selected-project bridge, bounded handoff context, shared provider runtime, advisory output and unchanged Project source.
What is correctly implemented

The source currently satisfies most of the roadmap's central requirements:

    Uses the shell's selected project.

    Reads an existing *_show_project_to_AI handoff.

    Requires a completed and published collector handoff.

    Reads only allowlisted ZIP members.

    Rejects traversal, absolute names, drive-qualified names, duplicate names, encrypted members, suspicious compression ratios and oversized members.

    Loads compact evidence rather than exact source.

    Excludes source archives, PNG assets and full Error Memory by default.

    Marks generated handoff information as untrusted project evidence.

    Redacts known Project and Project Support absolute roots before remote transmission.

    Requires confirmation before every remote request.

    Supports OpenRouter and Kilo.

    Uses dynamic model catalogs.

    Delegates HTTP and streaming behavior to web_ai_provider_runtime.py; networking is not implemented inside the GUI.

    Uses background workers with cooperative cancellation.

    Rejects stale responses using request ID and context-snapshot ID.

    Keeps conversation history in memory by default.

    Prevents explicit Markdown export into Project source or Project Support.

    Does not execute model output or modify project source.

The static feature validators passed against the uploaded source archive, including:

PROJECT_WEB_AI_CONTEXT_BRIDGE: PASS
UNTRUSTED_PROJECT_EVIDENCE_BOUNDARY: PASS
SAFE_HANDOFF_ZIP_MEMBER_ACCESS: PASS
NO_SECOND_NETWORK_ENGINE_IN_GUI: PASS
PROJECT_SOURCE_READ_ONLY_CONTRACT: PASS
MCARD_STALE_RESULT_GUARD_PRESERVED: PASS
VALIDATION OK
STATUS: IN_SYNC

Main canon defect: support-root resolution

The roadmap requires:

drive_root = root of project_root
support_root =
    <drive_root>\<project_name>_show_project_to_AI

KANDA already has a canonical Tool-versus-Project owner:

canonical_project_support_root(active_project_root)

On Windows, that owner places Project Support at the drive root.

But Project Web AI currently duplicates the logic locally:

support_root = root.parent / (root.name + "_show_project_to_AI")

This works for the present project:

Project: E:\kanda_reasoner
root.parent: E:\
Result: E:\kanda_reasoner_show_project_to_AI

But it becomes incorrect for a nested project:

Project:
E:\Projects\MyApplication

Current Web AI result:
E:\Projects\MyApplication_show_project_to_AI

Canonical result:
E:\MyApplication_show_project_to_AI

Therefore:

    For E:\kanda_reasoner: correct behavior.

    For arbitrary nested Windows projects: potentially wrong support root.

    Architecturally: the Web AI duplicates a boundary decision instead of using the existing canonical public owner.

The smallest proper correction would be for project_web_ai_bridge.py to reuse the canonical Project Support boundary helper, preferably the fail-closed helper that also detects forbidden nested support roots.
Other differences from the uploaded roadmap

These do not necessarily break Tool-versus-Project safety, but they mean implementation is not an exact realization of the Version 1 roadmap:

    Standalone top-level tab.
    The roadmap preferred Project Web AI inside or adjacent to Project Q&A unless impractical. The implementation registers it as an independent top-level tab. This is an architectural variation, not necessarily a boundary violation.

    Conversation export was implemented early.
    Saved conversation export was listed as a Version 1.1 candidate. The current implementation already permits explicit Markdown export. It is safely blocked from Project and Project Support roots, so this does not violate ownership.

    MCard identity is partial.
    Async events carry request_id and snapshot_id, but not the complete roadmap identity including session_id, gateway, model and approval identity.

    Typed failures are less granular.
    Provider failures have several typed classes, but context failures are mostly represented by the general SupportContextError, rather than the complete detailed failure taxonomy in the roadmap.

    Runtime validation is not proven by the uploaded handoff.
    The collector package is complete and published. However, its validation_state records the broader architecture and workflow commands as not run. I could run the static Project Web AI validators successfully, but the real PySide GUI validators could not execute in this environment because PySide6 is unavailable. This is a validation-evidence limitation, not evidence of a functional failure.

Direct answers

Does KANDA Reasoner conform to the uploaded “WEB AI IN KANDA_REASONER” roadmap?

Mostly, but not completely. The central behavior conforms. The support-root resolver and several secondary contracts do not exactly match the roadmap.

Do I understand the Tool-versus-Project canon?

Yes. Tool and Project are logical ownership domains. Equal physical roots during self-hosting do not collapse them.

Is Web AI respecting the Tool-versus-Project canon?

For the current E:\kanda_reasoner project: mostly yes. For full generic compliance: not completely, because its Project Support resolver duplicates canonical logic and will resolve the wrong location for nested Windows project roots.

Is Web AI programmed to work with the Project and not the Tool?

Yes. The Tool owns the Web AI implementation, but the Web AI operates on the dynamically selected Project and reads that Project's external support context. It is not programmed to analyze KANDA Tool source unconditionally.

0030b Project Versus KANDA Reasoner Tool Boundary Canon Addendum(1).md
File
0030b updated Project Versus KANDA Reasoner Tool Boundary Canon Addendum(2).md
File
lets correct , to implement  web ai complete separation tool versus project  cannon uploaded. correct all erroneous  and missing logic exposed above, step by step and coherently.

The correction is implemented as a bounded KANDA Reasoner Tool change. It does not modify another selected Project’s source or convert Project Support into Tool source.

The implementation now follows the canonical four-owner model: reusable Tool code, selected Project source, external Project Support, and disposable project-linked daily work. Self-hosting remains explicit rather than collapsing Tool and Project identities.

The repair includes:

    One canonical Tool/Project identity record, with stable active Project identity, root fingerprint, canonical support root, daily-work root, and explicit self-hosting state.

    Removal of the duplicated root.parent / <slug>_show_project_to_AI resolver.

    Removal of fallback to another Project’s handoff package.

    Fail-closed verification that the handoff belongs to the selected Project.

    Full immutable request identity across Project, session, snapshot, context, gateway, model, and cloud approval.

    Stale-event rejection across Project changes, context reloads, gateway/model changes, chat changes, and approvals.

    Export protection for Tool source, Project source, Project Support, and daily work.

    Updated handoff generators and focused validators.

The implementation preserves the uploaded Web AI architecture: the GUI remains Tool-owned, compact context comes from the selected Project’s external handoff, the shared provider runtime remains the networking owner, and model output remains advisory.

PATCH DELIVERY GATE

ZIP purpose: Install complete Tool-versus-Project separation for Project Web AI, including canonical support-root ownership, project-bound handoff identity, full request cards, and cross-root export protection.

ZIP placement path: Initially place E:\kanda_project_web_ai_tool_project_boundary_v1.zip; the governed installer stages it as E:\kanda_reasoner_delete_after_daily_work\kanda_project_web_ai_tool_project_boundary_v1.zip before extraction.

What this ZIP is: A cumulative transactional source patch for the reusable KANDA Reasoner Tool.

What this ZIP is not: It is not selected-Project handoff data, Project Support state, a completed Freeze entry, or authorization to modify another selected Project.

Install code present: YES - transactional preflight, predecessor hashes, backup, exact payload installation, final-hash verification, and rollback.

Validation code present: YES - focused boundary, bridge, credential, chat, real-widget, architecture, installed-hash, and archive-contract checks.

Expected validation markers: VALIDATION OK: project-web-ai-tool-project-boundary-v1; VALIDATION OK: project-web-ai-bridge-v1; Errors: 0; ZIP CONTRACT: PASS; STATUS: IN_SYNC.

Changed files: 16 manifest-declared Tool files covering canonical boundary/context owners, Web AI contracts/runtime/bridge/reader/workers/tab/conversation owners, and four focused validators. Protected Web AI UI layout owners are unchanged.

Allowed write paths: Manifest-declared files under the KANDA Reasoner Tool source; E:\kanda_reasoner_delete_after_daily_work for transient staging, extraction, backup, and validation helpers; selected Project external freeze intake only after complete validation.

Forbidden write paths: Any different selected active project root; transient install, patch, staging, backup, or validation-helper artifacts inside an active project root; Project Support as reusable Tool source; daily work as durable source or frozen memory; project_freeze_ledger as selected-Project frozen memory.

Freeze/freeze-intake: The root release sidecar is present but is not a completed Freeze. It may enter the selected Project’s external freeze_hint_intake only after full local validation. Preview remains read-only, and Confirm and Write remains explicitly human.

Error Memory payload: None. No new recurring error class was demonstrated; the relevant existing boundary and delivery lessons were applied.

Post-validation steps: Regenerate Show Project to AI for the selected Project because old handoffs lack the new physical Project identity fields. Then open Project Web AI and verify the displayed Tool, Project, support, daily-work, and self-hosting identities.

What not to do: Do not copy payload files manually. Do not validate after an installation failure. Do not reuse an old handoff. Do not hardcode KANDA Reasoner as every selected Project. Do not manually write Freeze memory.

Beginner-safe: YES - use the two PowerShell blocks below in order and stop if installation does not print INSTALL OK.

GATE STATUS: PASS

RECEIVER DELIVERY CHECK

Receiver classification: SOURCE_PATCH

Actual receiver path or action: Install only the manifest-declared reusable Tool files under E:\kanda_reasoner.

Installer stages to receiver: YES

Manual paste required: NO

Storage-only helper: NO

Receiver proof: INSTALL_MANIFEST.json declares every destination, accepted predecessor hash, final SHA-256, and absence policy. Installation verifies final hashes and rolls back all applied files on failure.

RECEIVER STATUS: PASS

Download:

Supporting files:

SHA-256:
057735f657265af91616e56b829a169c6ef57565b34b27b36eb6f2c6a9f858ed
Step 1 - Install

Start at a clean primary PowerShell prompt such as PS E:\kanda_reasoner>. If the prompt shows >>, press Ctrl+C before pasting anything.

Save or move the downloaded file to:

E:\kanda_project_web_ai_tool_project_boundary_v1.zip

Then paste:

$PROJECT_ROOT = "E:\kanda_reasoner"
$PATCH_NAME = "kanda_project_web_ai_tool_project_boundary_v1.zip"
$EXPECTED_ZIP_SHA256 = "057735f657265af91616e56b829a169c6ef57565b34b27b36eb6f2c6a9f858ed"
$DRIVE_ROOT = [System.IO.Path]::GetPathRoot($PROJECT_ROOT)
$PROJECT_NAME = Split-Path -Leaf $PROJECT_ROOT
$DAILY_ROOT = Join-Path $DRIVE_ROOT ($PROJECT_NAME + "_delete_after_daily_work")
$ROOT_PATCH_ZIP = Join-Path $DRIVE_ROOT $PATCH_NAME
$STAGED_PATCH_ZIP = Join-Path $DAILY_ROOT $PATCH_NAME
$BOOTSTRAP_ROOT = Join-Path $DAILY_ROOT "kanda_project_web_ai_tool_project_boundary_v1_bootstrap"

function Invoke-ProjectWebAIPatchStaging {
    try {
        if (-not (Test-Path -LiteralPath $PROJECT_ROOT -PathType Container)) {
            throw "Project root does not exist: $PROJECT_ROOT"
        }

        if (-not (Test-Path -LiteralPath $ROOT_PATCH_ZIP -PathType Leaf)) {
            throw "ZIP is not in root of drive:\ where project is: $ROOT_PATCH_ZIP"
        }

        $RootHash = (
            Get-FileHash -LiteralPath $ROOT_PATCH_ZIP -Algorithm SHA256
        ).Hash.ToLowerInvariant()

        if ($RootHash -ne $EXPECTED_ZIP_SHA256) {
            throw "Drive-root ZIP SHA-256 mismatch."
        }

        New-Item -ItemType Directory -Path $DAILY_ROOT -Force | Out-Null
        Copy-Item -Path $ROOT_PATCH_ZIP -Destination $STAGED_PATCH_ZIP -Force

        if (-not (Test-Path $STAGED_PATCH_ZIP -PathType Leaf)) {
            throw "Staged ZIP was not created."
        }

        $StagedHash = (
            Get-FileHash -LiteralPath $STAGED_PATCH_ZIP -Algorithm SHA256
        ).Hash.ToLowerInvariant()

        if ($StagedHash -ne $EXPECTED_ZIP_SHA256) {
            throw "Staged ZIP SHA-256 mismatch."
        }

        Remove-Item -Path $ROOT_PATCH_ZIP -Force

        Add-Type -AssemblyName System.IO.Compression.FileSystem
        $Archive = [System.IO.Compression.ZipFile]::OpenRead(
            $STAGED_PATCH_ZIP
        )
        $MemberNames = New-Object `
            'System.Collections.Generic.HashSet[string]' `
            ([System.StringComparer]::OrdinalIgnoreCase)

        try {
            foreach ($Entry in $Archive.Entries) {
                $MemberName = $Entry.FullName

                if ([string]::IsNullOrWhiteSpace($MemberName)) {
                    throw "ZIP contains an empty member name."
                }

                if ($MemberName.Contains('\')) {
                    throw "ZIP contains a raw backslash member: $MemberName"
                }

                if (
                    $MemberName.StartsWith('/') -or
                    $MemberName -match '^[A-Za-z]:'
                ) {
                    throw (
                        "ZIP contains an absolute or drive-qualified member: " +
                        $MemberName
                    )
                }

                if ($MemberName.Split('/') -contains '..') {
                    throw "ZIP contains a traversal member: $MemberName"
                }

                if (-not $MemberNames.Add($MemberName)) {
                    throw "ZIP contains a duplicate member: $MemberName"
                }
            }
        }
        finally {
            $Archive.Dispose()
        }

        if (Test-Path -LiteralPath $BOOTSTRAP_ROOT) {
            Remove-Item -LiteralPath $BOOTSTRAP_ROOT -Recurse -Force
        }

        Expand-Archive `
            -Path $STAGED_PATCH_ZIP `
            -DestinationPath $BOOTSTRAP_ROOT `
            -Force

        return $true
    }
    catch {
        Write-Host "INSTALL ERROR"
        Write-Host "PHASE: ROOT-DRIVE STAGING AND SAFE EXTRACTION"
        Write-Host ("ERROR TYPE: " + $_.Exception.GetType().FullName)
        Write-Host ("ERROR MESSAGE: " + $_.Exception.Message)
        Write-Host "LAST SUCCESSFUL MARKER: STAGING NOT COMPLETE"
        Write-Host ""
        Read-Host "Press Enter to clear terminal"
        Read-Host "Press Enter again to clear"
        Clear-Host
        return $false
    }
}

$STAGING_OK = Invoke-ProjectWebAIPatchStaging

if ($STAGING_OK) {
    & (Join-Path $BOOTSTRAP_ROOT "INSTALL.ps1") `
        -ProjectRoot $PROJECT_ROOT `
        -StagedZip $STAGED_PATCH_ZIP `
        -ExpectedZipSha256 $EXPECTED_ZIP_SHA256
}

# INSTALL.ps1 owns the successful install footer:
# INSTALL OK. Terminal will clear in 2 seconds...
# Start-Sleep -Seconds 2
# Clear-Host

Proceed only after the installer prints INSTALL OK: project-web-ai-tool-project-boundary-v1 and INSTALLED HASHES: VERIFIED.
Step 2 - Validate

Return to a clean PS E:\kanda_reasoner> prompt. If >> appears, press Ctrl+C. Then paste:

$PROJECT_ROOT = "E:\kanda_reasoner"
$PATCH_NAME = "kanda_project_web_ai_tool_project_boundary_v1.zip"
$EXPECTED_ZIP_SHA256 = "057735f657265af91616e56b829a169c6ef57565b34b27b36eb6f2c6a9f858ed"
$DRIVE_ROOT = [System.IO.Path]::GetPathRoot($PROJECT_ROOT)
$PROJECT_NAME = Split-Path -Leaf $PROJECT_ROOT
$DAILY_ROOT = Join-Path $DRIVE_ROOT ($PROJECT_NAME + "_delete_after_daily_work")
$STAGED_PATCH_ZIP = Join-Path $DAILY_ROOT $PATCH_NAME
$EXTRACT_ROOT = Join-Path $DAILY_ROOT "kanda_project_web_ai_tool_project_boundary_v1_extract"
$VALIDATION_SCRIPT = Join-Path $EXTRACT_ROOT "VALIDATE.ps1"

# VALIDATE.ps1 runs the governed Python 3.12 validation helper.
& $VALIDATION_SCRIPT `
    -ProjectRoot $PROJECT_ROOT `
    -StagedZip $STAGED_PATCH_ZIP `
    -ExpectedZipSha256 $EXPECTED_ZIP_SHA256

# VALIDATE.ps1 owns the non-install terminal footer:
# Read-Host "Press Enter to clear terminal"
# Read-Host "Press Enter again to clear"
# Clear-Host

Local validation is complete only when the terminal shows:

ZIP CONTRACT: PASS

Errors: 0

VALIDATION OK: project-web-ai-tool-project-boundary-v1

STATUS: IN_SYNC
Step 3 - Regenerate the selected Project handoff

After validation passes, run Show Project to AI again for the currently selected Project.

The new bridge intentionally rejects pre-migration handoffs that do not carry:

    active_project_id

    active_project_root_fingerprint

This fail-closed migration prevents an old or another Project’s handoff from being silently accepted.
Step 4 - Verify Project Web AI

Test these two situations:

    Self-hosting: Tool root and Project root both resolve to E:\kanda_reasoner, while the GUI still reports distinct logical identities and self_hosting_mode = YES.

    Another nested Project: for example, E:\Projects\Example. Project Support must resolve to the canonical drive-level external location, and a KANDA-specific or mismatched handoff must be rejected.

The package passed Python compilation, focused boundary validation, bridge regressions, static GUI contracts, architecture validation with Errors: 0, manifest/payload verification, ZIP member-safety validation, and ZIP CONTRACT: PASS in the sandbox.

It has not been installed or run with real PySide on your Windows environment. Freeze eligibility therefore depends on the Windows-local validation above.




