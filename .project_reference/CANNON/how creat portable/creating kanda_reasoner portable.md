You are continuing the governed creation of the KANDA Reasoner Tool Portable for Windows.

Your objective is to create a valid production Portable without repeating prior errors, without packaging Project content, without writing into another Project, and without accepting false-positive GUI smoke results.

CURRENT PATHS

Tool root:
E:\kanda_reasoner

Tool Support root:
E:\kanda_reasoner_show_project_to_AI

Tool transient daily-work root:
E:\kanda_reasoner_delete_after_daily_work

External Project used for the second smoke:
E:\eeg_kanda

External Project Support:
E:\eeg_kanda_show_project_to_AI

Portable output root:
E:\KandaReasoner_Portable_Output

Expected final folder:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable

Expected final ZIP:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable.zip

Canonical build launcher:
E:\kanda_reasoner\tools\BUILD_KANDA_REASONER_TOOL_PORTABLE.ps1

PRIMARY RULE

A KANDA Reasoner Tool Portable build must begin only after all selected Projects have been unloaded.

Before PyInstaller starts, the Tool registry must report:

SELECTED PROJECT: NONE
SELF-HOSTING MODE: OFF
PROJECT HISTORY PRESERVED: PASS
PROJECT ENVIRONMENT OVERRIDES: ABSENT
TOOL PORTABLE BUILD CONTEXT: ISOLATED
UNLOAD PROJECT FOR TOOL PORTABLE: PASS

Do not build while KANDA Reasoner, EEG Kanda, or any other Project is selected.

Do not infer that self-hosting is off. Verify it from the Tool registry.

Do not package Project source, Project Support, Error Memory, Freeze Memory, generated complete JSON, or Project transient files inside the Tool Portable.

STARTUP AND EVIDENCE REQUIREMENTS

Before changing code or starting the build:

1. Load the KANDA startup pack.
2. Load the KANDA Reasoner second-upload Project handoff.
3. Read Compact Error Memory.
4. Open Full Error Memory only when repeated-error debugging or a conflict with a prior lesson requires it.
5. Inspect the current exact source files and manifests.
6. Treat generated handoff files as evidence, not as source authority.
7. Do not trust a previous conversation's patch, hash, filename, manifest schema, or source line number without comparing it with the current live source.
8. Do not guess whether a file belongs to the Portable runtime allowlist or builder manifest.

PRE-BUILD VALIDATION

Run the current canonical Portable validators before PyInstaller.

At minimum, require successful validation of:

1. Portable builder installation contract.
2. Tool/Project registry boundary gate.
3. Packaged smoke isolation.
4. Governed-root exact backup and rollback.
5. Exact runtime path-and-SHA-256 allowlist.
6. Exact Portable builder member governance.
7. External build-control hash binding and Preview attestation.
8. Tool Portable direct PyInstaller workflow.
9. Functional GUI smoke reporting.
10. No-Project GUI behavior.
11. Local AI Project-root registry authority.
12. Project Structure complete-JSON evidence resolution.

Required principles:

* Runtime membership comes only from the committed runtime allowlist.
* Builder membership comes only from the exact builder manifest.
* Do not modify either manifest unless current source inspection proves the changed file is governed by that exact manifest.
* Do not recursively discover new runtime files.
* Do not insert stale historical hashes.
* Do not manually print success markers.
* A marker counts only when produced by the actual validator after all checks pass.
* No validator may execute a production Portable build unless that validator explicitly owns that behavior.
* Failed installation or validation must roll back every modified source file exactly.

LOCAL AI PROJECT AUTHORITY

The Tool Project Selection Registry is the only authority for the selected Project root.

Local AI must not restore Project authority from a private QSettings value.

When the Tool registry reports Project NONE:

* Local AI Project root must be empty.
* Local AI Project JSON path must be empty.
* A stale saved root such as E:\kanda_reasoner must be ignored.
* No Project Support path may be resolved.
* No Project-owned background task may run.

When an external Project is selected:

* Local AI must use the exact root from the Tool registry.
* The external Project's stable registry identity must be reused.
* The Tool runtime extraction directory must never be treated as a Project owner.

Do not patch project_json_path_resolver.py, the runtime allowlist, or the builder manifest merely because Local AI reports a stale Project root. First identify the actual authority source and initialization order.

BUILD SAFETY

Before building:

1. Close every old source GUI and Portable GUI window.
2. Stop remaining kanda_reasoner processes.
3. Verify that no old packaged smoke process is running.
4. Quarantine or rename any existing final Portable folder and ZIP with a UTC timestamp.
5. Never test a previously generated Portable folder after a new repair.
6. Use only the folder generated by the current build run.
7. Keep the build output outside:

   * E:\kanda_reasoner
   * E:\kanda_reasoner_show_project_to_AI
   * E:\kanda_reasoner_delete_after_daily_work
   * E:\eeg_kanda
   * E:\eeg_kanda_show_project_to_AI
   * any other registered Tool or Project owner root

The output must remain under:

E:\KandaReasoner_Portable_Output

CANONICAL BUILD COMMAND

After all pre-build gates pass, run:

powershell.exe -NoProfile -ExecutionPolicy Bypass -File "E:\kanda_reasoner\tools\BUILD_KANDA_REASONER_TOOL_PORTABLE.ps1" -ToolRoot "E:\kanda_reasoner" -OutputDirectory "E:\KandaReasoner_Portable_Output" -ExternalProjectRoot "E:\eeg_kanda" -ReplaceExisting

Do not use --skip-smoke.

Do not create a Portable directly with an improvised PyInstaller command when the canonical builder exists.

The canonical builder must control:

* Project unloading.
* Registry checks.
* Runtime staging.
* Exact manifests.
* PyInstaller.
* Disposable smoke environments.
* Functional runtime reports.
* Publication.
* Final build-result JSON.

FIRST PACKAGED GUI SMOKE: NO PROJECT

The first packaged launch must use an isolated, disposable, token-bound environment.

It must not inherit:

* the live Tool registry;
* live Project selection;
* live Tool Support;
* live Project Support;
* live transient paths;
* APPDATA;
* LOCALAPPDATA;
* USERPROFILE;
* HOME;
* TEMP;
* TMP;
* cache paths;
* KANDA environment overrides;
* provider credentials.

During the first smoke:

1. Confirm selected Project is NONE.
2. Open Show Project to AI.
3. Confirm the Project Root label and controls remain visible.
4. Confirm the Project Root field is blank.
5. Open Local AI.
6. Confirm Local AI opens without a traceback.
7. Confirm no stale root such as E:\kanda_reasoner is restored.
8. Open Error Memory.
9. Confirm Error Memory shows a safe no-Project state rather than crashing.
10. Close the packaged Tool normally.

The smoke must generate a machine-readable functional runtime report.

Do not accept only:

* process exit code zero;
* the window opening;
* the window closing normally;
* registry state alone;
* visually rendered error panels.

The build must fail if:

* a lazy tab displays FAILED TO LOAD;
* a traceback is shown;
* Local AI reports a stale live Project root;
* Error Memory reports a backend failure;
* a required functional record is missing;
* the report is malformed;
* the token does not match;
* any required tab reports failure.

SECOND PACKAGED GUI SMOKE: EXTERNAL EEG KANDA PROJECT

The second packaged launch must use the isolated external Project configuration.

During the second smoke:

1. Confirm selected Project is EEG Kanda.
2. Confirm the exact existing EEG Kanda stable Project identity is reused.
3. Open Show Project to AI.
4. Open Local AI.
5. Open Error Memory.
6. Open Project Structure 3D.
7. Confirm there are no tracebacks or backend owner-manifest errors.
8. Confirm the Tool runtime extraction directory is not treated as a Project owner.
9. Close the packaged Tool normally.

PROJECT STRUCTURE 3D AND COMPLETE JSON

Complete JSON is external Project Support evidence. It is not Tool Portable content.

Do not package EEG Kanda complete JSON inside the Tool Portable.

If Project Structure 3D must display a real graph:

1. Confirm EEG Kanda is selected.
2. Confirm its external Project Support root is:
   E:\eeg_kanda_show_project_to_AI
3. Confirm a valid canonical complete-JSON ZIP family, loose complete JSON, or preserved local-AI complete JSON exists.
4. If none exists, use Project Structure 3D or the proper Show Project workflow to create the complete JSON.
5. Refresh evidence.
6. Confirm fixture mode is false.
7. Confirm the graph reports real Project evidence.

Evidence priority must remain:

1. Canonical Project Structure complete-JSON ZIP family.
2. Loose <project>__complete.json while present.
3. Preserved <project>__complete_local_AI.json.
4. Deterministic fixture only when no real evidence exists.

Fixture fallback is acceptable only when real complete-JSON evidence genuinely does not exist.

Do not claim that the Portable packaged real Project evidence merely because Project Structure can read it externally.

RELEASE ACCEPTANCE

The build is accepted only when the actual build output includes all of the following:

TOOL PORTABLE PROJECT/SUPPORT CAPTURE: ABSENT
CURRENT STAGE 1-6 PORTABLE BUILDER: PASS
LOCAL AI PROJECT AUTHORITY PRE-BUILD GATE: PASS
PYINSTALLER KANDA REASONER TOOL BUILD: PASS
PACKAGED TOOL FIRST LAUNCH SELECTED PROJECT NONE: PASS
PACKAGED TOOL FIRST FUNCTIONAL TAB SMOKE: PASS
PACKAGED TOOL EXTERNAL PROJECT EEG KANDA: PASS
PACKAGED TOOL EXTERNAL PROJECT STABLE ID REUSE: PASS
PACKAGED TOOL SECOND FUNCTIONAL TAB SMOKE: PASS
TOOL/PROJECT DISTINCTION: PASS
KANDA REASONER TOOL PORTABLE CREATED: PASS
SELECTED PROJECT DURING BUILD: NONE
SELF-HOSTING DURING BUILD: OFF
PYINSTALLER NATIVE WINDOWS BUILD: PASS
PROJECT CONTENT PACKAGED: NO
PRODUCTION PORTABLE REBUILD: PASS
BUILD STATUS: portable_ready

The expected final evidence is:

FINAL PORTABLE ZIP:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable.zip

BUILD RESULT:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable-build-result.json

The final response must also report the SHA-256 of the final ZIP.

FAILURE HANDLING

If any stage fails:

1. Stop immediately.
2. Do not continue to PyInstaller.
3. Do not manually print expected success markers.
4. Confirm transactional rollback.
5. Confirm the original source hashes were restored.
6. Preserve the failing log and diagnostic files.
7. Inspect the exact current source and manifest schemas.
8. Compare the failure with Compact and Full Error Memory.
9. Fix only the demonstrated defect.
10. Do not make speculative changes to unrelated files.
11. Do not modify runtime or builder manifests unless exact membership requires it.
12. Re-run all prior-stage validators after any correction.
13. Do not reuse an old generated Portable folder.
14. Do not label a failed or skip-smoke build as portable_ready.

A successful PyInstaller compilation alone is not a successful Portable release.

A return code of zero alone is not a successful Portable release.

A visible GUI alone is not a successful Portable release.

A manually confirmed tab alone is not sufficient when a machine-readable functional report is required.

PATCH RULES

If a source repair is required before building:

1. Inspect the exact live source.
2. Record baseline hashes.
3. Build a focused governed patch ZIP.
4. Include only required source and validator files.
5. Include transactional installation and rollback.
6. Use the selected Tool root from the registry or explicit argument.
7. Put transient extraction and receipts under:
   E:\kanda_reasoner_delete_after_daily_work
8. Do not extract and install directly from Downloads or Desktop.
9. If the patch ZIP begins at E:, stage it into the daily-work folder and delete the root-drive copy after successful staging.
10. Validate the ZIP contract before providing it.
11. Include root-level KANDA_FREEZE_HINT.json only when the patch is freeze-ready.
12. Do not duplicate KANDA_FREEZE_HINT.json inside the payload.
13. Do not freeze automatically.
14. Preview remains read-only.
15. Confirm and Write remains explicitly human-confirmed.

TERMINAL BEHAVIOR

Successful installation:

* show the success markers;
* wait approximately two seconds;
* run Clear-Host;
* do not ask the user to press Enter.

Validation, diagnostics, build errors, smoke errors, freeze work, and all other non-install-success blocks:

* display the result;
* request Enter;
* request Enter again when required by the governed cleanup pattern;
* run one final Clear-Host;
* keep the terminal open.

Never close the terminal automatically.

FINAL RESPONSE FORMAT

After a successful build, return:

KANDA REASONER TOOL PORTABLE RESULT

Build status:
portable_ready

Selected Project during build:
NONE

Self-hosting during build:
OFF

Project content packaged:
NO

First no-Project functional smoke:
PASS

Second EEG Kanda functional smoke:
PASS

External Project stable identity reuse:
PASS

Tool/Project distinction:
PASS

Portable folder:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable

Portable ZIP:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable.zip

Portable ZIP SHA-256: <actual SHA-256>

Build-result JSON:
E:\KandaReasoner_Portable_Output\KandaReasoner-Windows-Portable-build-result.json

Freeze status:
NOT FROZEN / READY FOR HUMAN-CONFIRMED FREEZE

Do not claim success unless every value above comes from the actual current build evidence.
