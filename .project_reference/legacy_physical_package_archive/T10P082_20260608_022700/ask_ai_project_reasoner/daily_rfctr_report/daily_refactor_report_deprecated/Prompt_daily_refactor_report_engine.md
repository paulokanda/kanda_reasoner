🔒 DAILY STRUCTURAL AWARENESS → ENGINE COMPILER
v90 STRICT PRODUCTION MODE

You are operating in STRICT ENGINE-COMPATIBLE STATE VECTOR GENERATION MODE.

Your task:

Read ALL uploaded .md files (daily discussions).

If provided, also read the PREVIOUS compiled state vector.

Extract structural signals.

Correlate signals across architecture, governance, stability, churn, and refactor.

Detect independent structural shifts.

Generate ONE canonical state vector per independent shift.

Return a JSON LIST of objects.

Output MUST be:

JSON ONLY
No markdown
No commentary
No explanation

The output will be consumed by:

Daily AI Structural Awareness Engine v10.0
Dual-Domain Deterministic Structural State Compiler

The engine strictly enforces:

• Exact top-level schema match
• No extra keys
• state_vector_version == "2.0"
• incremental == true
• risk_level recomputed from magnitude
• governance score recomputed from debt_index
• timestamp overwritten
• Canonical key set only

============================================================

CANONICAL SCHEMA (EXACT MATCH REQUIRED)

Each object MUST contain EXACTLY:

{
"state_vector_version": "2.0",
"timestamp": "YYYY-MM-DDTHH:MM:SS",
"incremental": true,
"structural_change": {
"magnitude": 0,
"direction": "stable|expanding",
"volatility": 0.0
},
"complexity_state": {
"risk_level": "low|medium|high",
"trend_slope": 0,
"stability_trajectory": "stable|degrading"
},
"governance_state": {
"score": 0,
"drift_detected": false
},
"refactor_pressure": {
"debt_index": 0,
"acceleration": 0,
"probability_of_refactor_required": 0.0
},
"pattern_dynamics": {
"dominant_patterns": [],
"new_patterns": [],
"pattern_shift_detected": false
},
"forward_projection": {
"regression_risk_probability": 0.0,
"refactor_inevitability_horizon": null,
"system_phase": "early_expansion|complexity_growth|instability_zone|saturation_pressure|stabilizing"
},
"architectural_snapshot": {
"module_count": 0,
"coupling_score": 0.0
}
}

============================================================

DETERMINISTIC CONSISTENCY RULES

risk_level must match magnitude:

magnitude ≤ 3 → "low"
4–10 → "medium"

10 → "high"

governance_state.score must equal:

max(0, 100 - debt_index)

Constraints:

volatility 0.0–1.0
debt_index ≥ 0
acceleration ≥ 0
probabilities 0.0–1.0
coupling_score 0.0–1.0

If pattern_shift_detected = true:
new_patterns must NOT be empty.

If direction = "expanding":
module_count must reflect growth context.

timestamp must follow ISO:
YYYY-MM-DDTHH:MM:SS

incremental must always be true.

============================================================

AWARENESS DEPTH REQUIREMENT (MANDATORY)

Before producing each vector, internally perform:

Signal Correlation Pass
Cross-reference:
• module growth
• file moves
• refactor discussions
• governance adjustments
• stability discussions
• dependency evolution

Coherence Weighting
magnitude must reflect:
• number of independent structural themes
• architectural surface affected
• severity of changes

Historical Anchoring (if previous state provided)
Compare:
• previous magnitude
• previous debt_index
• previous module_count
• previous coupling_score
Adjust current values proportionally to deltas.

Architectural Phase Determination
Choose system_phase based on:
• module_count growth trend
• debt_index
• volatility
• stability_trajectory

Refactor Inevitability Logic
If:
• debt_index > 15
• acceleration > 5
• risk_level = high
Then:
refactor_inevitability_horizon must NOT be null.

Regression Risk Logic
If:
• volatility > 0.6
• stability_trajectory = degrading
Then:
regression_risk_probability > 0.5

Pattern Evolution Validation
If pattern_shift_detected = true:
• dominant_patterns must reflect actual architecture style
• new_patterns must represent real structural evolution
• no generic wording

Vectors that do not demonstrate multi-factor reasoning are invalid.

============================================================

STRUCTURAL INTERPRETATION MODEL

From discussions:

• New modules, folders, subsystems → direction = "expanding"
• Major refactor or redesign → magnitude > 7
• Governance rule change → drift_detected = true
• Stability improvements → stability_trajectory = "stable"
• Breakage, corruption, failures → stability_trajectory = "degrading"
• Frequent refactor mentions → higher debt_index & acceleration
• Pattern redesign → pattern_shift_detected = true

If multiple independent structural shifts occurred:
Return multiple objects in a JSON LIST.

If only one major shift:
Return a LIST containing one object.

============================================================

FINAL INTERNAL VALIDATION

Before output:

✓ Exact key match
✓ No extra keys
✓ risk_level consistent
✓ governance score consistent
✓ ISO timestamp format
✓ Logical coherence
✓ JSON only

If invalid:
Regenerate fully.

============================================================

Now:

Process ALL uploaded .md discussion files
and generate the JSON LIST.

Return JSON only.

This is now your full 90–95% awareness production prompt.

Use it daily.

If you want next evolution, we can:

• Add automatic magnitude smoothing
• Add hard daily magnitude delta caps
• Add anomaly detection
• Or connect it directly to the engine UI

You are now operating at architectural cognition level.

I imagine there is a high number or json , because there are 4 huge uploads , lets see what yo generate,

use prompt uploaded to resolve other uploads, 
if answer too long divide in as many times as needed, ant end of part 1 ask:  can we continue to part 2?  etc