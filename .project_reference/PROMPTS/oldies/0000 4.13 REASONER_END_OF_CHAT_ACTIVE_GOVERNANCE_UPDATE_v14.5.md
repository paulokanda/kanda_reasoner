# 0000 4.13 REASONER END-OF-CHAT ACTIVE GOVERNANCE UPDATE v14.5

Version: 14.5
Status: Active end-of-chat governance update prompt
Use: Only after a validated, user-approved Reasoner freeze.

This prompt is governance-only.

Goal:
Use the latest uploaded baseline copies of the five active governance files, update
them in place conceptually based only on validated and user-approved decisions
from the current chat, validate them, and return one replacement ZIP.

The replacement ZIP must be safe to extract into the active project root. Do not
hardcode E:\developer_tools in generated Python logic. Validation command
examples may mention E:\developer_tools, but checker and tests must use dynamic
paths.

Active governance files:
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\REASONER_PROJECT_CANON.md
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\REASONER_PROJECT_CANON.json
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\check_reasoner_project_canon.py
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\test_reasoner_project_canon.py
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\accepted_warning_baseline.json

Important path rule:
- Preserve _project_reference\ACTIVE_PROJECT_ GOVERNANCE exactly.
- The folder name contains a space between the underscore and GOVERNANCE.
- Do not normalize it to ACTIVE_PROJECT_GOVERNANCE.
- Do not place files at ZIP root.
- Do not place files under tools\architecture.
- Do not include runtime/source files.
- Do not include _bundle_temp.
- Do not include unrelated manifests.

Baseline requirement:
Before updating governance, the current latest copies of all five governance
files should be uploaded or otherwise available.

If the four canon files are missing, state:
No baseline provided - writing from first principles only, no regression guarantee possible.

If accepted_warning_baseline.json is missing, state:
No accepted warning baseline provided - do not invent or recreate accepted warnings from memory.

When baseline files are available:
- preserve existing content;
- update only what the current chat validated and the user approved;
- do not rewrite from scratch;
- do not weaken prior canon;
- do not remove accepted warning history unless pruning was explicitly approved;
- preserve accepted_warning_baseline.json unless the current chat explicitly
  changed the accepted-warning policy or baseline entries.

Update policy:
Only update governance with decisions, freezes, validations, or rules explicitly
established in the current chat.

Do not:
- invent product behavior;
- promote proposals into frozen canon;
- weaken existing tests;
- rewrite from scratch when baseline exists;
- change runtime/source code;
- generate accepted warnings from memory;
- change accepted_warning_baseline.json unless the current chat explicitly
  approved baseline changes;
- silently delete accepted warning entries because code later changed.

Required output:
Return one ZIP containing exactly these five updated replacement files under the
canonical internal path:

_project_reference\ACTIVE_PROJECT_ GOVERNANCE\REASONER_PROJECT_CANON.md
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\REASONER_PROJECT_CANON.json
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\check_reasoner_project_canon.py
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\test_reasoner_project_canon.py
_project_reference\ACTIVE_PROJECT_ GOVERNANCE\accepted_warning_baseline.json

No other files.

Install instruction for the user:
Extract the ZIP into the active project root to replace:

<PROJECT_ROOT>\_project_reference\ACTIVE_PROJECT_ GOVERNANCE\

The project root is dynamic. Do not assume it is always E:\developer_tools.

Required canon content:
- what the five governance files are;
- governing hierarchy;
- box logic summary;
- box handoff contract rule;
- no-regression policy;
- current chat frozen decisions;
- governance change workflow;
- active governance delivery path;
- semver canon_version;
- accepted warning baseline role and preservation policy.

Governance file role requirements:
- REASONER_PROJECT_CANON.md is the human-readable governance document.
- REASONER_PROJECT_CANON.json is the machine-readable governance state.
- check_reasoner_project_canon.py is the command-line validation gate.
- test_reasoner_project_canon.py is the pytest-compatible regression gate.
- accepted_warning_baseline.json is the machine-readable accepted-warning
  filter baseline. It records intentionally accepted warnings with code, path,
  message hash, classification, decision, and rationale. It must not hide new
  unaccepted warnings or errors.

Accepted warning baseline policy:
- Treat accepted_warning_baseline.json as a governed ledger.
- Preserve it across chats.
- Do not recreate it from scratch.
- Do not recreate it from memory.
- Append or narrowly update only after explicit user approval.
- Do not use it as a trash bin for new warnings.
- New unaccepted warnings must remain visible.
- Any new accepted warning requires explicit rationale, classification, stable
  path, warning code, message hash or equivalent stable matcher, and
  user-approved freeze.
- If a warning was later fixed, keep the historical entry by default.
- Optional pruning or resolved/retired status requires an explicit baseline
  pruning task and updates to Markdown, JSON, checker, tests, and the baseline.

Checker requirements:
check_reasoner_project_canon.py must:
- exit 0 on success;
- exit 1 on failure;
- print human-readable failure reasons;
- validate semver canon_version;
- validate active governance delivery path;
- validate all five required files are present;
- validate governance_file_roles includes accepted_warning_baseline.json;
- validate accepted_warning_baseline.json is valid JSON;
- validate accepted_warning_baseline.json has schema_version;
- validate accepted_warning_baseline.json has accepted_warnings as a list;
- validate each accepted warning entry has code, path, decision,
  classification, rationale, and message_sha256 or another explicit stable
  matcher;
- validate accepted baseline decisions are explicit, not blank;
- be runnable from the governance folder;
- be runnable from any project root with --project-root .;
- use dynamic paths based on Path(__file__).resolve().parent or
  project_root / "_project_reference" / "ACTIVE_PROJECT_ GOVERNANCE".

Test requirements:
test_reasoner_project_canon.py must:
- test semver;
- test required governance sections;
- test JSON/Markdown consistency for new governance rules;
- test all five required files exist;
- test accepted_warning_baseline.json loads as JSON;
- test accepted_warning_baseline.json has schema_version and accepted_warnings;
- test accepted warning entries have classification, code, decision, path,
  rationale, and message_sha256 or another stable matcher;
- test checker validates successfully.

Validation commands:
cd E:\developer_tools

python.exe -m py_compile "_project_reference\ACTIVE_PROJECT_ GOVERNANCE\check_reasoner_project_canon.py"
python.exe -m py_compile "_project_reference\ACTIVE_PROJECT_ GOVERNANCE\test_reasoner_project_canon.py"
python.exe "_project_reference\ACTIVE_PROJECT_ GOVERNANCE\check_reasoner_project_canon.py" --project-root .
$env:PYTHONPATH = "E:\developer_tools"
python.exe -m pytest "_project_reference\ACTIVE_PROJECT_ GOVERNANCE\test_reasoner_project_canon.py" -q

Final rule:
If no validated user-approved freeze occurred, say:
No governance update required.

If a freeze occurred, produce only the five governance files under
_project_reference\ACTIVE_PROJECT_ GOVERNANCE, validate them, and return the
governance ZIP.
